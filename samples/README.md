# Sample datasets

Synthetic demo data used by tests and the live demo. Each set contains a
planted incident chain (e.g. DB latency -> API timeouts -> checkout 500s -> alert).

- `demo_mixed.zip` (TODO): application.jsonl, db.log (syslog), alerts.csv
