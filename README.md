<p align="left"><img src="assets/logo-wordmark.png" alt="Watchover" width="300"></p>

# Watchover

## Proje Adı

**Watchover** — operasyonel gürültüyü sinyale, sinyali gerekçeli incident'a, incident'ı takip edilen aksiyona çeviren SRE karar destek uygulaması.

## Problem

Kesinti anında farklı formatlarda (JSON, CSV, syslog, key=value, düz metin, Alertmanager, ticket) on binlerce satır akar. Aynı hatanın tekrarları gerçek sinyali gömer; "neden bu alarm önemli", "ne zaman başladı, düzeldi mi", "kök neden ne" soruları elle ve kanıtsız cevaplanır; aksiyonlar takip edilmez, aynı hata bir sonraki nöbette yeniden öğrenilir.

## Çözüm

Log / olay / alarm / metrik / ticket verisini dosyadan, HTTP API'den, MCP sunucusundan ya da canlı ajanlardan alır; formatı kendisi tanır, sütunları rollere eşler, tek bir kanonik modele indirger. Tekrarları maskeleme + parmak iziyle tek sinyale indirir, patlamaları ölçer, ilişkili sinyalleri incident'ta toplar, kök nedeni ve önem puanını faktörleriyle açıklar; her karar satır düzeyinde kanıt taşır. Incident flashcard'ı (ne oldu / neden / nerede / ne zaman / nasıl düzeldi / ne yapmalı / daha önce görüldü mü), aksiyon kanbanı, playbook, ITSM ticket ilişkilendirmesi ve canlı operasyon sayfası (CPU / GPU / bellek / disk, SLO / SLA, ortam ve sunucu kapsamı, her kartın detayı) sunar. Çekirdek deterministiktir; çalışma zamanında LLM veya bulut servisi gerekmez, LLM isteğe bağlı zenginleştirmedir.

Jüri özeti: [AI_JURI.md](AI_JURI.md) · Mimari: [docs/mimari.md](docs/mimari.md) · Plan ve fazlar: [docs/plan.md](docs/plan.md), [docs/fazlar.md](docs/fazlar.md) · Prompt'lar: [prompts/](prompts/) · Ekran görüntüleri: [demo/](demo/)

## Ekip

| İsim | Rol | İletişim |
|------|-----|----------|
| Tayfur | Ürün ve geliştirme | tayfurozkaras@gmail.com |

AI kaydı kuralı: bu depodaki her dosya için "hangi AI aracıyla üretildi" aşağıdaki dosya referansında tutulur.
**CF5.1** = Claude Fable 5.1 (claude-fable-5-1, Claude Cowork). Adım adım kayıt: `docs/AI_LOG.md`.

## Kurulum

```bash
git clone <repo-url>
cd <repo>
cp .env.example .env                   # isteğe bağlı; uygulama varsayılanlarla çalışır
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[dev,mcp]"            # dev: pytest · mcp: MCP sunucusu için SDK
```

Windows (PowerShell; adım adım rehber: `docs/KURULUM_WINDOWS.html`, PDF olarak da iletilir):

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1            # "running scripts is disabled" derse: Set-ExecutionPolicy -Scope CurrentUser RemoteSigned
python -m pip install --upgrade pip
pip install -r requirements.txt
pip install -e .                       # motor paketini (src\watchover) ortama tanıtır
python -m pytest -q                    # 30 passed beklenir
streamlit run app.py
```

## Kullanım

```bash
streamlit run app.py                   # http://localhost:8501 ; canlı alıcı :8600'de otomatik açılır
python -m pytest -q                    # 30 test, ağ gerektirmez
watchover data.zip --inspect       # CLI: dosya başına format / roller / sütunlar
watchover data.zip                 # CLI: profil + incident özeti (--json ile makine çıktısı)
python mcp_server.py --http --port 8765   # motor MCP sunucusu olarak (http://localhost:8765/mcp)
docker compose up                      # dashboard :8501 + mcp :8765
```

## Teknoloji Yığını

- Python 3.9+ (3.9 ve 3.11'de doğrulandı)
- Streamlit + Altair (arayüz), pandas (veri), python-dateutil (zaman), pytest (test)
- stdlib: csv / json / re / zipfile / tarfile / gzip / http.server / urllib / sqlite3
- mcp ≥ 2 (isteğe bağlı MCP sunucusu), Docker Compose

## Demo

`demo/` klasörüne bakınız (8 ekran görüntüsü + açıklama). Video linki: _(eklenecek)_

## Lisans

GPL-3.0 (bkz. `LICENSE`)

---

# Uygulama kılavuzu

## 0. S-A1 "Alarm Fırtınası" senaryosu nasıl işleniyor

Veri paketi (alarms.json, alarms.csv, service_dependencies.csv, host_inventory.csv, VERI_SOZLUGU.md) tek bir ZIP olarak Datasets sayfasından yüklenir; paket repoya eklenmez.

1. **Yan tablolar** (`tables.py`): adı `depend` / `inventory` / `sozlugu` / `brifing` geçen dosyalar olay değil referans tablosudur; bağımlılık (kaynak → hedef, tip, kritiklik) ve envanter (host → servis, dc, kabin, kritiklik) motorun içine alınır.
2. **Tekilleştirme** (`pipeline.dedupe`): alarms.json ve alarms.csv aynı 3.000 alarmı taşır; `alarm_id` ile tek kopya tutulur (json tercih), rapor kaç satırın düşüldüğünü yazar. Zorunlu gereksinim 01: 3.000 alarmın tamamı işlenir.
3. **Şiddet ölçeği** (`scenario.SEVERITY_MAP`): sözlükteki 1 = bilgi … 5 = kritik ölçeği DEBUG…CRITICAL'e eşlenir.
4. **Yoğunluk kümeleme** (`storm.py`, `scenario.CLUSTERING = "auto"`: bağımlılık tablosu varsa devreye girer): pencere 5 dk'lık kovalara bölünür; bir servis (veya sunucu) kovada kendi medyan hızının 3 katını (en az 4) aşarsa hücre "sıcak"tır. Sıcak hücreler aynı servis / tanımlı bağımlılık / aynı kabin (ağ tipli alarmlar) ile ve en fazla bir kova arayla bağlanır; bağlı hücreler bir olaydır. Olayın alarmları üye servislerin o zaman aralığındaki alarmlarıdır; kendi normal hızında kalan (servis, alarm tipi) çiftleri gürültüye geri verilir. 15 alarmdan az ya da 3 ERROR+ içermeyen yoğunluk noktaları kart olmaz (LOW-n, gürültü denetiminde listelenir). Kart bütçesi `MAX_INCIDENTS = 15`.
5. **Kök neden** (`analysis.pick_root_cause`, yoğunluk modu): alarm tipine göre nedensellik önceliği (`CAUSE_RANK`: ağ > disk > veritabanı > dış servis > kaynak > uygulama belirtisi) + şiddet + adet + grubun ilk neden-tipi alarmı olma + bağımlılık tablosunda başkalarının ona bağımlı olması − başkasına bağımlı olması − geç başlama. Bir kabinin ≥ 3 sunucusunda ağ alarmı varsa kabin düzeyi ağ olayı sayılır. En iyi 3 rakip hipotez puan ve gerekçesiyle **karşı olasılıklar** olarak karta yazılır.
5b. **Kart puanı** (0–1, `scenario.WEIGHTS`): patlama 0,30 (olayın tüm alarmlarının tepe dakikası / üye servislerin olay dışındaki medyan dakikası), şiddet 0,20 (ERROR+ payı), etki alanı 0,20 (servis + sunucu sayısı / veri setindeki servis + sunucu sayısının yarısı), süre 0,15, iş kritikliği 0,15 (envanterde kritik / yüksek işaretli sunucu payı). Etiket: ≥ 0,6 veya CRITICAL sinyal → critical, ≥ 0,4 high, ≥ 0,2 medium.
6. **İlk aksiyon** (`record_first_actions`): her kart için kök neden şablonuna göre öneri (`scenario.RECOMMENDATIONS`), sahip (`scenario.OWNERS`: DBA / ağ / ödeme entegrasyon / batch / nöbetçi) ve **açık** durumla aksiyon kaydı otomatik açılır; Aksiyonlar sekmesinde durum değiştirilir (gereksinim 04–05).
7. **Gürültü denetimi** sekmesi: ham olay → kart → karttaki / elenen alarm sayıları, indirgeme oranı, her elenen sinyal için neden (normal hız içinde / küçük yoğunluk noktası / kart bütçesi dışı), servis × zaman ısı haritası (sıcak hücreler çerçeveli), kart için küçük kalan gruplar.

8. **Zenginleştirilmiş tek dosya** (`enrich.py`, `watchover paket.zip --enrich alarms_enriched.csv`): her alarm satırı + şiddet etiketi, alarm sınıfı (neden / kaynak / belirti / arka plan) ve nedensellik puanı, mesajda anılan bağımlılık, envanter (dc, kabin, iş kritikliği, envanter–etiket tutarlılığı), bağımlı olduğu / ona bağımlı servisler ve kritiklikleri, altyapı alarmı mı, 5 dk kovası / kovadaki alarm / servis medyanı / sıcak hücre, incident, rol (kök neden / belirti / küçük grup / gürültü) ve gürültü nedeni; yanında `.summary.json` (kart özetleri, kök neden gerekçeleri, karşı olasılıklar, ilk aksiyon ve sahip). Dosya kendi kendine yeter: `tables.from_observations` bağımlılık grafiğini ve envanteri sütunlardan geri kurar, yalnız bu dosya yüklendiğinde aynı 4 kart çıkar (CSV ve JSONL). Format algılayıcı tırnak içindeki virgüllere karşı `csv.reader` ile sayım yapar.

Bu paketle sonuç: 3.000 alarm → 5 kart (yavaş yanma çıkarımı `storm.extract_slow_burns`: şiddeti tırmanan neden-tipi alarm zinciri + o servise yönelik timeout'lar ayrı kart olur; 02:24 session-service bellek sızıntısı → gc → oom, 0,44), diğerleri puan sırasıyla: 01:33 dc1/rack-A kabin ağ olayı (0,79); 02:33 payment-provider-gw dış servis kesintisi (0,77); 02:04 billing-db disk dolu → tablespace (0,71); 03:05 batch penceresi çakışması → subscriber-db bağlantı havuzu (0,44), 1.793 alarm gerekçesiyle elendi, analiz 0,4 sn. Kullanılan ek kütüphane yok; korelasyon tamamen bu depodaki kodla yapılır.

## 1. Kurulum ve çalıştırma

    python3 -m venv .venv && source .venv/bin/activate
    pip install -e ".[dev,mcp]"            # dev: pytest · mcp: MCP sunucusu için SDK
    streamlit run app.py                   # http://localhost:8501 ; canlı alıcı :8600'de otomatik açılır

    python -m pytest -q                    # 30 test, ağ gerektirmez
    watchover data.zip --inspect       # CLI: dosya başına format / roller / sütunlar
    watchover data.zip                 # CLI: profil + incident özeti (--json ile makine çıktısı)
    python mcp_server.py --http --port 8765   # motor MCP sunucusu olarak (http://localhost:8765/mcp)
    pip install -e ".[api]" && python -m watchover.api --port 8000   # HTTP API, OpenAPI: http://localhost:8000/api/docs
    ./watchover.sh install                 # Docker: postgres + dashboard (+ mcp / api profilleri)

Ortam değişkenleri: `ACTIONS_DB` (aksiyon SQLite yolu), `PLAYBOOK_DB` (hata kütüphanesi SQLite yolu), `LIVE_SPOOL` (canlı olay JSONL spool'u), `LIVE_PORT` (alıcı portu).
Streamlit ayarları `.streamlit/config.toml` (1 GB yükleme sınırı, koyu tema). Python 3.9+; 3.9 ve 3.11'de test edildi.

## 2. Sayfalar (sol menü)

| Sayfa | Ne yapar |
|---|---|
| 🏠 Operasyon | Ana sayfa. 2 sn'de bir yenilenir. Kart ızgarasının sağında **Kapsam** kutusu: Ortam açılır listesi (Tümü iken her ortamın erişilebilirlik / hata özeti), altında o ortamın sunucuları radyo listesi olarak; bir sunucu seçilince sayfadaki 10 kartın hepsi (4 altyapı + 6 servis seviyesi), grafikler, olay akışı ve son olaylar o sunucuya daralır, "Filtreyi temizle" ile geri dönülür. **Her kartın altındaki "Detay" düğmesi** canlı bir ayrıntı paneli açar: CPU / GPU / bellek / disk → o metriğin büyük grafiği (eşik çizgisi), host tablosu (son, ort, maks, eşik, durum) ve eşik aşımları; Erişilebilirlik ve SLA → dakika bazında erişilebilirlik (hedef çizgisi), servis bazında ERROR+, oranı düşüren hata kalıpları (kalıp, adet, servis, host, ilk/son, örnek mesaj), host bazında ERROR+; Hata bütçesi → kalan bütçenin zaman içindeki tüketimi ve aynı hata kırılımı; p95 → servis bazında p50/p95/maks tablosu, SLO çizgili çubuk grafik, en yavaş istekler; Olay/dk → servis bazında dakika grafiği, servis ve host tabloları; ERROR+ → son hata olayları. Servis seviyeleri başlığının altındaki not hesaplama dayanağını söyler (seçili kapsamın son 15 dk olayları; erişilebilirlik = 1 − ERROR+/toplam, p95 mesajlardaki `<n>ms`, hata bütçesi = izin verilen hata payının kalanı, hedefler `scenario.SLO` / `SLA`). Sırasıyla: **Altyapı** (host bazlı CPU / GPU / bellek / disk kartları + 15 dk seriler, eşik renkleri), **Servis seviyeleri** (erişilebilirlik vs SLO, p95 gecikme, hata bütçesi, SLA durumu, olay/dk, ERROR+), **olay akışı** (dakika × seviye alan grafiği, en yoğun servisler), **en ilgili 5 ITSM ticket'ı**, **son olaylar** konsolu. Ajan yoksa yerleşik simülatör akar ve SİMÜLASYON rozeti görünür; "Canlı tamponu analiz et" toplanan veriyi tam pipeline'a sokar |
| 🗄️ Datasets | Bir veya daha çok dosya / ZIP / TAR.GZ yükleme (her biri 1 GB; birden çok dosya varsayılan olarak tek veri seti olur, anahtarla her dosya ayrı yüklenebilir; her yükleme kimliğiyle bir kez işlenir, aynı adlı dosyanın yeniden yüklenmesi veya veri seti değiştirmek yeniden yükleme başlatmaz; boş dosyalar atlanır; **yükleme arka planda** çalışır: ayrıştırma ve analiz ayrı bir iş parçacığında sürer, sayfalar arasında gezilebilir, ilerleme yüzde, aşama, dosya adı ve kalan süre tahminiyle sayfadaki çubukta ve kenar çubuğu DURUM kutusunda görünür; ayrıştırma yüzdesi gerçek satır / bayt payıdır, analiz ve profil aşamaları ayrıştırma süresine oranla tahmin edilir; bitince veri seti kaydedilir ve bildirim gelir), demo veri seti, canlı tamponu analiz et. Her set **ayrı tutulur**; **Aktif veri seti** dropdown'ı seçileni açar, "Kaldır" siler. **Veri setlerini karşılaştır**: A / B seçimi, KPI deltaları, seviye dağılımı, göreli zaman çizgisi, ortak / sadece A / sadece B sinyaller, incident listeleri. Aktif set için **log analizi sekmeleri**: Özet, Sinyaller, Incident'lar, Aksiyonlar (aşağıda) |
| 🕸 Hata haritası | Servis / sunucu bağımlılık grafiği, verinin kendisinden çıkarılır. Kaynak: aktif veri seti ya da canlı tampon; incident seçici (tek incident veya tümü), ortam filtresi, sunucu kümelerini açma/kapama. Kaynak canlı tampon ise harita 5 sn'de bir yeniden çizilir, seçim ve yakınlaştırma korunur. Üstte 5 kart (haritadaki servis, bağımlılık bağı, ilişki bağı, kök neden, kapsamdaki ERROR+). Grafik **etkileşimli** (harici kütüphane yok, kendi SVG çizimi): sürükleyerek kaydırma, tekerlekle yakınlaştırma; **bir düğüme tıklayınca** harita ona yakınlaşır, komşuları dışındaki her şey soluklaşır ve yanında detay paneli açılır (tür, ERROR+ sayısı, bağımlı olduğu / ona bağımlı servisler, sunucuları, sinyalleri); boşluğa tıklamak veya "Görünümü sıfırla" geri döndürür. Ortam ve sunucu filtreleri kök nedeni ve etkilenenleri de o kapsama daraltır. Çizim: **kırmızı ok** = hata mesajlarından çıkarılan bağımlılık ("upstream payment-api", "timeout to db-01"; sunucu adı o sunucudaki servise eşlenir), kalınlık = adet; **mavi kesikli** = motorun sinyal ilişkisi (ortak varlık, saniye farkı); **gri ok** = sunucu → servis ERROR+ sayısı, sunucular ortam kümelerinde. Kök neden servisi kırmızı ve ⚑ ile, etkilenenler turuncu. Altında **etki zinciri** (kök nedenden bağımlılık oklarını geriye izleyerek: postgres → payment-api → checkout-api). Altında okuma anahtarı. Incident flashcard'ındaki "Haritada göster" düğmesi bu sayfayı o incident ile açar |
| 📚 Playbook | **Hata kütüphanesi.** Her analizde ERROR+ / patlayan sinyal şablonları ve incident kök nedenleri otomatik işlenir: kaç veri setinde, ilk/son görülme, olay sayısı, kök neden olma sayısı, gözlenen düzelme türleri (restart / kendiliğinden / durdu / devam), servisler, hostlar. Arama; kayıt seçince **Çözüm notu** ve **Runbook** (ilk bakılacak adımlar; `scenario.RECOMMENDATIONS`'tan otomatik tohumlanır) düzenlenip kaydedilir. Kalıcı (`playbook.db`), oturumlar ve veri setleri arası taşınır |
| 🎫 ITSM | Yapılandırma (ServiceNow / Jira Service Management / OneDesk / Generic REST / demo; URL, kullanıcı-şifre veya token, sorgu, alan eşleme), 60 sn otomatik yenileme, **ilişkilendirilmiş tam ticket tablosu** (ilgililik çubuğu, ilişkili sinyal/metrik/incident, gerekçe) ve ticket detayı |
| ⚙️ Connection Settings | **Veri kaynağı**: HTTP API (URL, GET/POST, başlıklar, gövde, JSON yolu) veya MCP sunucusu (URL, araç listesi, araç + argümanlar) → "Bağlan ve çek". **Canlı alım**: alıcı portu, API anahtarı, simülatör anahtarı, ajan komutları. **LLM**: OpenAI uyumlu base URL, model, anahtar, "Bağlantıyı test et" (OpenLLM, Ollama, vLLM, LM Studio, bulut) |
| 👥 Kullanıcılar | Hesaplar (her satır bir form: ad, rol, durum, isteğe bağlı yeni parola, **Kaydet**, onaylı **Sil**; kilitli hesap etkinleştirilince kilit kalkar), Roller ve yetkiler matrisi, Hesap ekle (hemen etkin, doğrulamasız). Yalnız `page.users` yetkisi olan roller görür. |
| 🛠 Sistem | Durum (KPI + arka plan servis kartları), Bakım (önbellek, VACUUM, yeniden başlat), Güncelleme (git / zip), Giriş (yerel / Google / Microsoft / Apple / OIDC), **Bildirimler** (SMTP + SMS kanalları, kişi ve grup alıcıları, uyarı kuralları, teslimat günlüğü), Güvenlik günlüğü. Giriş açıkken yalnız yönetici görür. |
| 📖 README | Bu dosya |

### Log analizi sekmeleri (Datasets sayfasında, aktif set için)

| Sekme | İçerik |
|---|---|
| Özet | **Funnel** tek kart (ham olay → parmak izi → anlamlı sinyal → incident → aksiyon; logaritmik oranlı çubuklar, başlıkta azaltma katsayısı), **olgu şeridi** tek kart (dosya, kayıt, servis, sunucu, hata sınıfı, zaman kapsamı, ortam) ve 12 ayrı "Detay" düğmesi yerine tek **ayrıntı seçici** (`st.pills`); seçilen ayrıntı **modal pencerede** (`st.dialog`) açılır (ham olay, parmak izi, anlamlı sinyal, incident, aksiyon, dosya, kayıt, servis, host, hata sınıfı, kapsam, **ortamlar**: hataların yüzde kaçı prod / test / dev; detayında ortam başına olay, hata, hata oranı, tüm hataların payı, ortam × seviye grafiği, hata kaynakları tablosu, ortam × servis hata grafiği), aktivite zaman çizgisi (incident pencereleri gölgeli), zamana göre seviye, en yoğun servisler, kaynak türleri, öne çıkan incident kartları, Parser tablosu + sütun→rol eşleme düzeltme formu, oynatılabilir **canlı log akışı** (veri setini zaman sırasıyla oynatır; hız, satır, seviye ve metin filtresi) |
| Sinyaller | Filtreler (seviye, patlama, anlamlı, servis, ortam), tabloda ortam ve hata kaynağı sütunları, progress sütunlu tablo, **NEDEN BU SİNYAL?** paneli: gerekçe, şablon, gruplama, patlama detayı, sinyalin dakika grafiği; **Nereden / Neden / Zaman / Ne yapmalı** kartı (kaynak dosyalar ve sayıları, ajanlar, servis/host; parmak izi + seviye + patlama gerekçesi; ilk sinyal, son hata, süre; öneriler); **tıklanabilir kanıt tablosu**: satıra tıklayınca tüm alanlar ve veri setindeki ham satır |
| Arama | **Log arama**: tek kutu, boşluk = VE, `alan:değer` sütun filtresi (service / host / severity / source / environment, Türkçe adları da), `-terim` hariç tutma, `"tam ifade"`, Regex anahtarı. Vektörel `str.contains` ile önceden kurulmuş küçük harfli haystack sütununda arar ve sonucu `st.cache_data` ile önbeller (100 bin satırda on milisaniyeler). Sonuç kümesinin **facet**'leri (seviye, servis, sunucu, dosya) sayılarıyla hap olarak listelenir, tıklayınca filtre olur (facet içinde VEYA, facet'ler arası VE); aktif filtreler çip olarak durur, ✕ ile kalkar. Eşleşmelerin dakika bazında zaman çizgisi. Tablodan bir satır seçilince **kayıt kartı** açılır: ham satır eşleşen terimler vurgulu, kanıt referansı, ait olduğu incident; kartın altında her alan için **+ filtre** ve **− hariç tut** düğmeleri, incident'a gitme düğmesi |
| Incident'lar | Incident **açılır listeden** seçilir (id · seviye · puan · başlık; 99 incident'ta da tek satır). Seçilen incident için **flashcard**: ne oldu (başlık, olay/sinyal sayısı, zincir), neden (kök neden ve gerekçe), nerede, ne zaman (başladı, son hata, süre, sessiz süre), nasıl çözüldü (düzelme türü, saat, kanıt satırı), şimdi ne yapmalı, daha önce görüldü mü. Aynı flashcard Özet'teki incident detay panelinde satıra tıklayınca da açılır ("Incident sekmesinde aç" ile geçiş). Kök neden kartı, **Nereden** kartı (dosyalar, servisler, hostlar, ajanlar, parser, tür) ve **Zaman ve düzelme** kartı (ilk sinyal, son hata, hata süresi, ERROR+ sayısı, son hatadan beri sessiz süre; düzelme türü: restart sonrası / kendiliğinden / durdu-kanıt yok / devam ediyor; düzelme kanıtı satırı ve saati), **📚 Daha önce görüldü** kartı (kök neden şablonu playbook'ta varsa: kaç kez, hangi veri setleri, son görülme, düzelmeler, çözüm notu, runbook; benzer şablon için token örtüşmesi ≥ 0.6 ile bulanık eşleşme; "Playbook'ta aç"), yayılım Gantt'ı, skor faktör grafiği, zaman çizgisi, korelasyon bağları, tıklanabilir kanıt tablosu (tam kayıt + ham satır), önerilerden tek tıkla aksiyon, özel aksiyon formu, postmortem indir, LLM prompt popover'ı, **🤖 LLM ile açıkla** |
| Aksiyonlar | **Kendiliğinden düzelenler** bölümü (restart veya normal trafiğin dönmesiyle düzelen incident'lar, ne olduğu ve kanıtı); bu incident'lar için analiz anında otomatik olarak **done** durumunda bir aksiyon kaydı açılır ("Kendiliğinden düzeldi: … · kanıt", öneri: kalıcı düzeltmeyi doğrula; veri seti + incident başına bir kez). Öncelik renkli kanban: open / in_progress / done / suppressed |

Dil: sol üstte 🇹🇷 / 🇬🇧 anahtarı; arayüz, anlatı, gerekçe ve öneriler iki dilde.

## 3. Veri akışları

    dosya / API / MCP / ajan  ─►  loader  ─►  format_detector  ─►  parsers/*  ─►  Observation (kanonik model)
        ─►  analysis: template_of → fingerprint → Signal (+burst) → correlate → Incident (kök neden, skor, faktörler, kanıt, öneri)
        ─►  actions (SQLite)  ─►  app.py sayfaları

1. **Dosya:** Datasets sayfasından yükle → aynı pipeline → aktif veri seti.
2. **API / MCP:** Connection Settings → Veri kaynağı → "Bağlan ve çek".
3. **Canlı:** ajanlar `POST /ingest` ile olay **ve metrik** gönderir (herhangi bir format). Metrik için `{"host":..,"cpu":..,"gpu":..,"memory":..,"disk":..}` ya da `{"metric":"cpu","value":..}`.

        python agent.py --url http://<dashboard>:8600/ingest --key SECRET --tail /var/log/app.log      # log takibi
        python agent.py --url http://<dashboard>:8600/ingest --key SECRET --metrics --interval 10     # host CPU/GPU/bellek/disk
        python agent.py --url http://<dashboard>:8600/ingest --key SECRET --file export.csv            # tek seferlik
        python agent.py --url http://<dashboard>:8600/ingest --key SECRET --simulate                   # sentetik trafik
        curl -X POST http://<dashboard>:8600/ingest -H "X-API-Key: SECRET" --data-binary @app.log      # ajan olmadan

4. **ITSM:** ITSM sayfasından çekilen ticket'lar canlı hata sinyalleri, metrik eşik aşımları ve incident'larla ilişkilendirilir.
5. **MCP sunucusu:** `mcp_server.py` motoru 8 araçla herhangi bir MCP istemcisine açar.

## 4. Dosya referansı (her .py dosyası)

### Uygulama ve giriş noktaları

| Dosya | AI aracı | Ne yapar | Nasıl çalışır (fonksiyonlar) |
|---|---|---|---|
| `app.py` | CF5.1 | Streamlit arayüzü, tüm sayfalar | `wide()` Streamlit sürümüne göre `width="stretch"` / `use_container_width` seçer. `pill`, `kpi`, `kpi2` HTML kart/rozet üreticileri. `store()` / `live_store()` / `receiver()` / `simulator()` `st.cache_resource` ile süreç başına tekil nesneler (aksiyon DB, canlı tampon, HTTP alıcı, simülatör). `llm_cfg()` oturumdan LLM ayarı. `load()` bir veri setini alır → `Analysis` + `profile` → `st.status` adımlarıyla kayıt defterine (`datasets`) yazar; `activate_dataset()` seçilen seti görünümlere yansıtır; `record_auto_recoveries()` kendiliğinden düzelen incident'lara done aksiyon kaydı açar. `show_row()` bir gözlemin tüm alanları + ham satırı; `evidence_table()` satır seçimli kanıt tablosu (+ satır seçme kutusu); `recovery_label()`; `incident_flashcard()` (tek kartta ne oldu / neden / nerede / ne zaman / nasıl çözüldü / ne yapmalı / daha önce görüldü), `incidents_table()` (satır seçimli incident tablosu → flashcard); `page_playbook()`, `playbook_card()`. `minute_chart()` incident pencereli aktivite grafiği. Sol menü: dil, sayfa radyosu, 5 sn'de bir yenilenen durum satırı (`_status`). `fetch_tickets()` seçili ITSM sistemine göre çeker. `live_signals()` canlı tampondaki ERROR+ olaylardan şablon bazlı hızlı sinyaller. `correlated_tickets()` ticket'ları sinyal + eşik aşımı + incident ile ilişkilendirir; `tickets_df()` tabloya çevirir. `picker()` "Tümü" girişli açılır liste ("Tümü" → None, değer oturumda kalır). `_set_scope()` düğme geri çağrısı (widget'lar kurulmadan önce ortam / host anahtarlarını günceller). `scope_panel()` kartların sağındaki Kapsam kutusu (ortam listesi + ortamın sunucuları, sıfırlama düğmesi) → `(env, host)`; `ops_tile()` "Detay" şeritli kart (`ops_detail` oturum anahtarını açar/kapar), `metric_chart()` eşik çizgili host grafiği, `ops_detail_panel()` tıklanan kartın canlı ayrıntı paneli (metrik / erişilebilirlik / SLA / bütçe / p95 / olay hızı / hatalar); `metrics_block`, `slo_block`, `events_block`, `tail_block` operasyon sayfasının parçaları. `live_analysis()` canlı tampon üzerinde önbellekli Analysis (alınan olay sayısına göre yenilenir), `page_map()` Hata haritası sayfası (kaynak seçimi; canlı kaynakta 5 sn fragment; incident / ortam / sunucu seçimi, 5 kart, `to_html` çıktısını `st.iframe` ile gömme, etki zinciri rozetleri, okuma anahtarı); `page_ops()` (2 sn fragment; ortam ve host seçimi `ops_env_pick` / `ops_host_pick` oturum anahtarlarında tutulur, tüm bloklara `env` / `host` filtresi olarak geçer), `page_itsm()` (yapılandırma + 5 sn fragment tablo + detay), `page_conn()` (üç sekme), `datasets_controls()` (yükleme, demo, canlı tampon, dropdown, kaldır, karşılaştır), `compare_view()` (A/B görünümü). Sayfa dağıtıcı `page` değerine göre çalışır. Analiz sekmeleri: `frames()` (cache'li DataFrame), `tile()` (tıklanabilir kart), detay panelleri, sinyal / incident / aksiyon sekmeleri, `live_panel` (oynatılabilir log akışı fragment'ı) **Tema:** `.streamlit/config.toml` (lacivert zemin `#0e1420`, yüzey `#151d2b`, kenar `#243044`, vurgu turkuaz `#2dd4bf` / mavi `#60a5fa`, Inter + JetBrains Mono, 10 px köşe, minimal araç çubuğu, ayrı kenar çubuğu rengi) + `CSS` bloğu (kart / KPI / zaman çizgisi / aksiyon stilleri, marka SVG logosu) + `_wo_altair()` grafik teması (şeffaf zemin, soluk eksenler, marka kategori renkleri); `upper()` Türkçe'ye duyarlı büyük harf (İ), KPI etiketleri CSS yerine Python'da büyütülür **Kenar çubuğu**: marka + TR/EN anahtarı, gezinti listesi (radyo düğmesi CSS ile gerçek nav satırlarına çevrildi, aktif satır vurgulu), durum kartı (alıcı, veri seti, ticket'lar), sürüm damgası |
| `agent.py` | CF5.1 | Uzak ajan CLI | `host_metrics()` loadavg / `/proc/meminfo` / `shutil.disk_usage` / `nvidia-smi` ile CPU, bellek, disk, GPU yüzdeleri (psutil gerekmez). `post()` `X-API-Key`, `X-Agent`, `X-File-Name` başlıklarıyla gönderir. `tail()` `tail -f` gibi dosyayı izler, 1 sn'lik partiler. `main()` `--tail` / `--file` / `--simulate` / `--metrics` modları; `--env` (veya `AGENT_ENV`) metrik kayıtlarına ortam etiketi ekler |
| `web/` | CF5.1 | Yeni arayüz (React 18 + TypeScript + Vite), Streamlit'ten çıkışın 2. fazı | `src/api.ts` (bearer token, 401'de çıkış), `auth.tsx` (giriş, MFA adımı, `can(perm)`), `i18n.ts` (TR / EN sözlük), `styles.css` (tasarım belirteçleri, cam yüzeyler, duyarlı yerleşim), `components/ui.tsx` (Card, Kpi, Badge, Table, Modal, Tabs, Confirm, Spark), `components/Layout.tsx` (daraltılabilir cam kenar çubuğu, yetkiye göre menü, dil, ilk girişte parola değiştirme), sayfalar: `Login` (e-posta + parola, MFA kodu, SSO düğmeleri), `Ops` (KPI, dakikalık grafik (recharts), servisler, metrikler, son olaylar, simülasyon), `Anomalies` (kartlar, adım kutuları, sorumlu / not, durum, aksiyon), `Datasets` (sürükle-bırak çoklu yükleme, iş ilerlemesi, liste), `DatasetDetail` (huni, dosyalar, incident kartları ve detay penceresi: kök neden, anlatı, skor bileşenleri, zaman çizelgesi, öneriler, aksiyonlar, postmortem; sinyaller, gürültü, CSV), `Actions`, `Playbook`, `Connections` (ajanlar + kayıt anahtarı + kurulum komutu, kaynaklar + test / çekim, envanter + CSV), `Knowledge` (arama, not, kurallar), `Notify`, `Users` (hesaplar, roller matrisi, güvenlik günlüğü), `System`. `npm run build` → `web/dist`, API tarafından `/` altında sunulur; Dockerfile'da node aşaması derler |
| `web/src/pages/Ops.tsx` | CF5.1 | Canlı operasyon sayfası, tıklanabilir KPI ve metrik kartları | Ortam + sunucu çipleri `?env=&host=` süzgecine dönüşür; her kart `DetailPanel` açar (`/live/slo?detail=true`, `/live/discoveries`, `/live/files`): hız (alan grafiği + servis çubukları), ERROR+ (son hatalar), erişilebilirlik / SLA / bütçe (dakikalık çizgi, servis ve sunucu dağılımı, rakamı düşüren şablonlar), p95, CPU / bellek / disk / GPU (sunucu bazında çizgi + eşik + tablo), log dosyaları (keşifler, dosya tablosu, `TailModal` ile kuyruk ve dışa aktarma); kartlar kapanabilir, düzen değişmez |
| `web/src/theme.ts` | CF5.1 | Tema: koyu / sistem / açık | `applyTheme` `html.light` sınıfını ve `data-theme`'i ayarlar; `main.tsx` seçimi `wo_theme`'de saklar ve `prefers-color-scheme` değişimini dinler; `ui.tsx` `LangTheme` düğme grubu (TR / EN + 🌙 🖥 ☀️) `Layout` ve `Login`'de; `styles.css` `.light` altında aynı token'ları yeniden tanımlar, tasarım çizgisi korunur |
| `web/src/pages/Readme.tsx` | CF5.1 | README sayfası (`/readme`) | `GET /api/system/readme` metnini küçük bir Markdown dönüştürücüyle (başlık, liste, tablo, kod, kalın, bağlantı; ham HTML satırları atlanır) gösterir; yetki `page.readme` |
| `web/src/pages/Sources.tsx` | CF5.1 | Veri kaynakları sayfası (`/sources`, Bağlantılar › Kaynaklar sekmesiyle aynı panel) | Streamlit'teki Kaynaklar sayfasının karşılığı: giriş metni, platform kataloğu (Elasticsearch / OpenSearch, Grafana Loki, Splunk, Graylog, HTTP: ne gerekir, hangi loglar gelir), kaydetmeden test (`POST /api/sources/test`), gelişmiş alanlar (site, TLS, geriye bakış, zaman alanı, ek başlıklar), listede duraklat / test / sorgula / sil ve son hata; Veri setleri › Kayıtlı kaynak seçeneği `POST /api/datasets/from-source` ile seçilen pencereyi veri seti yapar |
| `web/src/pages/Inventory.tsx` | CF5.1 | Envanter sayfası (`/inventory`, Bağlantılar › Envanter sekmesiyle aynı panel) | Streamlit'teki Envanter sayfasının karşılığı: özet çipler (sunucu, kritik, DC, canlı eşleşme), Sunucular (arama, satıra tıklayınca düzenleme), Sunucu ekle (21 alanlı form: takma adlar, kritiklik, DC / kabin / VLAN / alt ağ, OS, rol, uygulama, küme, sahip, depolama, üretici, seri, izleyen, durum, etiket, not), Keşfedilenler (kritiklik seçip ekle), CSV içe / dışa aktarma ve şablon (`GET /api/inventory/template`) |
| `web/src/components/Logo.tsx` | CF5.1 | Watchover logosu React bileşeni | `assets/logo.svg` işareti (göz + sinyal yayları, turkuaz→mavi) inline SVG olarak; `Wordmark` gradyan yazı + slogan; kenar çubuğunda (daraltılınca yalnız işaret), giriş kartında ışıltılı 64 px, README sayfasında; `web/public/favicon.svg` ve `logo-256.png` (apple-touch-icon) |
| `src/watchover/coverage.py`, `samples/corpus/` | CF5.1 | Parser v2 adım 1: biçim korpusu ve kapsama ölçümü | 38 log ailesinden birer gerçekçi dosya (syslog 3164 / 5424, CEF, LEEF, GELF, Apache, nginx, IIS, Windows Event XML, journald, Docker, Kubernetes CRI, OTel, Cisco ASA, Fortinet, Palo Alto, Oracle, SQL Server, PostgreSQL, MySQL, Java / Python / .NET / Go, HAProxy, Postfix, Redis, MongoDB, Kafka, CloudTrail, ALB, ECS, logfmt, CSV, Alertmanager, düz metin, tarih varyantları). `measure_file` üretim `_parse_one` ile ayrıştırır; zaman (komşudan doldurulmamış), seviye (satırdaki seviye sözcüğüyle eşleşen; sözcüksüz satır sayılmaz), sunucu ve servis çıkarılan olay yüzdesi, puan = dört alanın ortalaması. `python -m watchover.coverage [--json|--update-baseline]`; `baseline.json` kabul edilen puanlar, `tests/test_coverage.py` gerilemeyi düşürür; `GET /api/system/coverage` ve Sistem › Parser kapsaması kartı |
| `src/watchover/parsers/{access,cef,winevt,w3c}_parser.py`, `family.py` | CF5.1 | Parser v2 adım 2: yeni biçim modülleri ve log ailesi | `access` (Apache / nginx common + combined, AWS ALB: durum sınıfı → önem, vhost → servis), `cef` (ArcSight CEF ve QRadar LEEF, syslog başlığı arkasında da; `rt` / `devTime` zaman, `dvchost` sunucu, ürün → servis, 0-10 önem), `winevt` (Windows Event XML: TimeCreated, Computer, Provider, Level 1-5, EventData), `w3c` (IIS / Exchange `#Fields:`), `family` (25 aile: PostgreSQL, MySQL, SQL Server, Oracle, Oracle EBS, Dynamics AX / 365 / NAV-BC, SAP, Redis, nginx, Apache, Kafka, HAProxy, Postfix, IIS, Windows, Java, Python, .NET, Go…: kayıt servis taşımıyorsa aile adı servis olur) |
| `src/watchover/grok.py`, `parsers/grok_parser.py`, `api/routers/grok.py`, `web/src/pages/Parser.tsx` | CF5.1 | Parser v2 adım 3: grok kütüphanesi | Logstash uyumlu desen dili (`%{AD:alan[:tip]}`, iç içe, `BASE` 90+ temel parça: sayı, IP, host, URI, tarih biçimleri, syslog parçaları, Java yığını), 27 hazır satır paketi (`LINES`: syslog, Apache common / combined / error, Tomcat, Squid, BIND, iptables / ufw, Nagios, sshd, sudo, cron, dhcpd, ESXi, RabbitMQ, dockerd, Exim, Dovecot, F5 BIG-IP, Junos, Check Point, Zeek, Windows metin, Ruby, Node, SAP CCMS) rol haritasıyla; `detect()` örnek satırların ≥ %60'ıyla eşleşen deseni seçer, algılayıcı sabit biçimlerden sonra ve kv / metinden önce dener; `GrokParser` rolleri uygular, eşleşmeyen devam satırlarını katlar, `_grok` özniteliği. Kullanıcı desenleri `<home>/grok.json` (her işçi süreç okur, mtime önbelleği), hazır paketlerden önce denenir. API: `GET/POST/PATCH/DELETE /api/grok`, `/grok/base`, `/grok/test`, `/grok/unparsed/{key}` (zaman ya da açık seviye çıkarılamayan satırlar şablona göre gruplu). Sayfa: Kütüphane (korpus eşleşme sayıları, aç / kapat, kopyala, düzenle), Test (satır satır alanlar, desen olarak kaydet), Ayrıştırılamayan satırlar (örneği teste al), Temel parçalar; yetkiler `page.parser` / `act.parser` (yerleşik roller sonradan eklenen yetkileri otomatik alır) |
| `src/watchover/drain.py`, `profiles.py` | CF5.1 | Parser v2 adım 4: Drain şablon madenciliği ve öğrenen sütun eşlemesi | `Drain` (He ve ark. 2017): satır token'lara ayrılır, belirgin değişkenler (sayı, IP, hex, UUID, zaman, yol, URL, e-posta, `anahtar=değer`) `<*>` olur, sabit derinlikli önek ağacı (token sayısı → ilk token → yaprak) ve benzerlik eşiği (%50) ile kümelenir; `TemplateStore` kümeleri `drain_clusters` tablosunda tutar, canlı akış (her anomali taramasında yeni pencere) ve yüklenen her veri seti besler; `propose_grok` bir kümeden tipli grok deseni üretir (IP / sayı / miktar / yol / URI / UUID / ISO zaman, noktalama korunur). `profiles`: biçim + sütun adı imzasıyla roller `<home>/profiles.json`'da; yüklemede verilen eşleme "kullanıcı" olarak kalıcı, otomatik tahmin "auto" ve kullanıcıyı ezmez; `_parse_one` eşleme verilmediyse öğrenileni uygular (`report.profile`). API `/grok/templates` (+ `/propose`, sil), `/grok/profiles` (düzenle, sil); Parser sayfasında "Öğrenilen şablonlar" ve "Öğrenilen eşlemeler" sekmeleri |
| `src/watchover/memory.py`, `api/routers/memory.py` | CF5.1 | Kurumsal hafıza: sürekli beslenen, gömme destekli bilgi tabanı | `Memory` bilgi tabanını (`knowledge.py`) dört otomatik kaynakla besler: canlı akış penceresi (`LiveLearner`, `learn_min` dakikada bir kök neden desenleri), yüklenen veri setleri, yeni açılan anomaliler (`AnomalyTracker.on_open` → `anomaly` kaydı, tekrarlar `occurrences` artırır) ve kapatılan aksiyonlar (`resolution` kaydı). Vektörler: yapılandırılmış gömme modeli (bge-m3 vb.) varsa o, yoksa deterministik `hash_embed` (kelime/ikili/karakter üçlüsü → 256 boyut, model ve ağ gerektirmez); her satır hangi modelle gömüldüğünü `embed_model` sütununda taşır, arama her satırı kendi modelinin sorgu vektörüyle puanlar, `reindex` model değişince arka planda yeniden gömer. LLM incelemesi (`/datasets/{k}/review`) kanıt paketine `MEMORY` bölümü ekler (`[M<id>]` atıf). API `/memory/stats`, `/search?q&kinds`, `/timeline`, `/runs`, `/learn` (şimdi öğren), `/reindex` (başlat / durum). Hafıza sayfası: KPI'lar (kayıt, vektör kapsamı, son öğrenme, onaylı kural), tür filtresi, günlük öğrenme tablosu |
| `src/watchover/scanner.py`, `data/advisories.json`, `api/routers/scan.py` | CO4.8 | Envanter sunucularına savunma amaçlı zafiyet taraması | Üç geçiş: (1) **danışma eşleşmesi** — envanter kaydındaki yazılım/sürüm (`os`, `application`, `vendor_model`, `tags`, `notes`) küratörlü CVE kataloğuyla (`data/advisories.json`, 41 kayıt; kullanıcı `<home>/advisories.json` ile ekler) eşleştirilir, sürüm düzeltilen sürümün altındaysa bulgu üretir, sürüm bilinmiyorsa "doğrulanmadı" işaretiyle listelenir; paket gönderilmez; (2) **yapılandırma kontrolleri** — EOL işletim sistemi, izlemesiz kritik sunucu, sahipsiz kritik varlık; deterministik; (3) **erişilebilirlik kontrolü (isteğe bağlı, varsayılan kapalı)** — açık portlara TCP bağlantısı + pasif başlık okuması, `act.scan` gerektirir, yalnız kullanıcının kendi sunucularında. Bulgular `scan_results` tablosunda tutulur. API `/scan/stats`, `/results`, `/results/{host}`, `/runs`, `/catalog`, `/host/{host}`, `/all`. Menüde "Zafiyet taraması" (`page.scan`) |
| `docker-compose.yml` (llm profili), `watchover.sh llm` | CO4.8 | CPU'da yerel LLM (Ollama) | GPU'suz kurumsal sunucu için `llm` compose profili: `ollama/ollama` servisi compose ağında `http://ollama:11434`, modeller `watchover-ollama` hacminde kalıcı (cache temizliği silmez), CPU/bellek/thread sınırları `watchover.sh` tarafından makineye göre otomatik yazılır (Docker'ın gördüğü tüm CPU'lar, belleğin %80'i; `WATCHOVER_SIZING=manual` ile elle). `./watchover.sh llm on` profili açar ve modelleri indirir; zaten tanımlı bir LLM bağlantın varsa ona dokunmaz, gömülü Ollama ek seçenek olur (bağlantı yoksa varsayılan olarak onu kullanır) (sohbet/inceleme qwen2.5:7b, hızlı 3b, gömme bge-m3); `llm pull <model>`, `llm list`, `llm off` (modeller korunur). Uygulama motoru LLM'siz de çalışır |
| `src/watchover/training.py`, `scripts/train_lora.py`, `autolearn.training_bundle` | CF5.1 | Kendi modelin: hafızadan eğitim verisi ve CPU LoRA (`watchover-ops`) | `training_examples` hafızadaki her kaynağı sohbet biçimine çevirir (kök neden desenleri, anomaliler, çözümler, notlar, runbook parçaları, kayıtlı sohbetler, onaylı kurallar, beğenilen yanıtlar), (soru, yanıt) üzerinden tekilleştirir; `training_bundle` deterministik %10 eval ayrımı ve kaynak sayıları verir. `python -m watchover.training export` JSONL yazar, `modelfile` Ollama Modelfile üretir (FROM taban + ADAPTER, ya da birleştirilmiş HF dizini). `scripts/train_lora.py` CPU'da PEFT LoRA eğitir (Qwen 2.5 1.5B/3B, r=8, q/k/v/o, yalnız asistan turunda kayıp, batch 1 × birikim 8, float32); `--dry-run` torch'suz veri doğrulama + Modelfile; `--gate` yeni modeli deterministik kök neden benchmark'ında (`llm_eval`) eşiğe ve taban modele karşı sınar (PASS/FAIL çıkış kodu). API `/llm/training-stats`, `/llm/training-export?part=all|train|eval`; LLM › Kalite sekmesinde "Kendi modelin" kartı; `./watchover.sh llm export|import <dizin>|use <model>` Docker akışı; import yalnız ekler, `use` seçer |
| `src/watchover/autotrain.py`, `lora.py`, `Dockerfile.trainer`, compose `trainer` | CF5.1 | Gözetimsiz öğrenme: hafıza dolunca kendi kendine eğitir, kalite kapısından geçeni `watchover-ops` yapar | `trainer` servisi (`llm` profili, CPU/bellek sınırlı, `watchover-hf` ağırlık önbelleği ve `watchover-models` hacmi) dakikada bir paylaşılan ayarları okur (`train_auto`, `train_every_h`, `train_min_examples`, `train_min_new`, `train_base`, `train_gate_min`, `train_use_ops`); eşik dolunca ya da "Şimdi eğit" bayrağı (`<home>/train.now`) varsa: dışa aktar → `lora.train` (PEFT, yalnız asistan turunda kayıp) → gömülü Ollama'da aday model (`ollama.create_from_adapter`: blob API, eski sürümde Modelfile + paylaşılan hacim) → `lora.gate` (kök neden benchmark'ı; canlı akışta ≥3 incident varsa o, yoksa demo seti; eşik + taban modele karşı tolerans) → PASS: `watchover-ops` + tarihli kopya, FAIL: aday silinir, önceki kalır. Her koşu `train_runs` satırı; `Services.llm_cfg("ops")` yalnız geçen model varken ve "kullan" açıkken incident incelemesi/Watchover'a sor için onu döndürür, sohbet bağlantısı (`llm_cfg()`) hiç değişmez. API `/llm/autotrain` (durum), `PUT` (ayarlar), `POST /run`; kalp atışı `<home>/trainer.alive` |
| `live.py` (`attach_db`, `capacity`), `history.py` saklama, Sistem › Kapasite | CF5.1 | Faz 1 ölçek: canlı pencere veritabanında, kapasite göstergesi, saklama ayarları | `LiveStore.attach_db(kb)`: her ajan paketi `live_batches` tablosuna tek satır (JSONL yük, UTC `ts_min/ts_max`, indeks); `snapshot(since=…)` halka tamponun en eski olayı `since`'ten yeniyse pencereyi veritabanından okur (`window_max_events` ile en yeni N'e sınırlar, hata halinde halkaya düşer), yeniden başlatma sonrası pencere korunur; `prune()` `live_retention_h` saatten eski paketleri siler (her 200 pakette bir ve ayarla). `capacity()`: olay/sn (60 s ve 5 dk), halka doluluğu ve kapsadığı süre, anomali penceresini kapsıyor mu, DB paket/olay/bayt ve en eski kayıt, spool ve disk, uyarılar (`ring_short`, `ring_short_no_db`, `disk_low`, `db_growth`). Ayarlar `live_retention_h`, `live_window_max_events`, `live_ring`, `live_spool_mb`, `history_retention_days` (kaydedince anında uygulanır, `Services.apply_capacity_settings`); `/system/status.capacity`; `watchover.sh backup --keep N` yedek rotasyonu |
| `scripts/loadtest.py`, `docs/LOAD_TEST.md`, ayrıştırma ipucu önbelleği | CF5.1 | Yük testi ve 2.000+ olay/sn için alım optimizasyonu | 100 sanal ajan korpustan tazelenmiş ERP ağırlıklı satırlar basar, alıcı/DB penceresi/tarama ölçümlerini toplar (`loadtest-report.json`). Ölçüme dayalı iyileştirmeler: `LiveStore._detect` aynı ajan+dosya için biçim/aile tespitini önbelleğe alır ve `ingest_bytes(hint=…)` ile `_parse_one` tespiti atlar (`FAMILY_HINT`/`FAMILY_SEEN` contextvar), paket başına 20 ms → 2,4 ms; `AnomalyTracker._window` pencereyi tarama başına bir kez okur; `_scan_patterns` mesaj başına şablonu bir kez çıkarır; gelecek tarihli olaylar varış zamanına çekilir; `capacity()` olay/sn'yi gözlenen süreye böler ve tampon süresini uç 500'er olaydan alır. Sonuçlar: 2.000 satır/sn'de POST p50 10 ms, 4.000'de alım sürer, tarama 12,6 s (Faz 2 gerekçesi) |
| `web/src/components/LlmReview.tsx`, `POST /api/datasets/{key}/review` | CF5.1 | LLM incelemesi: incident, veri seti ya da log satırları için kurulu modelin görüşü | Kanıt paketi (`_review_bundle`: incident kartı + zaman çizelgesi + sinyal kanıtları; veri seti hunisi + incident'lar + en sık sinyaller; arama sorgusuna uyan ilk 80 satır) dil bazlı sistem istemiyle (`REVIEW_SYSTEM` tr / en: özet, kök neden değerlendirmesi, etki, aksiyonlar, açık sorular; referans zorunlu) `chat_messages`'a gider; cevap `llm_eval.grounding` ile atıf oranı taşır. Incident kartında "🤖 LLM yorumu" kutusu (model adı, ek soru), veri seti sayfasında "LLM incelemesi" sekmesi (kapsam: tüm set / satırlar + sorgu); LLM yoksa LLM sayfasına yönlendirir; deterministik karar değişmez |
| `web/src/components/ChatWidget.tsx`, `NotifyBell.tsx` | CF5.1 | Sağ alt sohbet balonu ve üst çubuk zili | `ChatWidget`: `/assist/ask` ile soru-cevap, `sessionStorage` `wo_chat` geçmişi Assist sayfasıyla ortak, veri seti seçimi, 👍 / 👎 (`/assist/rate`); `NotifyBell`: `/anomalies?status=active` 15 sn, görülmemiş kimlikler `wo_seen_anom`, sekme başlığı, `Notification` API |
| `src/watchover/parsers/text_parser.py`, `syslog_parser.py` (v2) | CF5.1 | Metin ve syslog parser'ları yeniden yazıldı | Metin: 30+ zaman biçimi (ISO + saat dilimi, Go, Redis, RFC 2822, epoch s / ms, `20260922T091700Z`, `22-SEP-2026`, AM / PM), CRI (`ts stdout F`) ve Redis (`pid:M`) ön ekleri, MySQL iş parçacığı kimliği, seviye sözcükleri (Serilog `INF/WRN/ERR/FTL`, `Note/System`), köşeli parantez uygulama adı (`[payment-api]`, kod `[MY-010055]` ayrı), Python logger, tireli uygulama adı (`payment-api gateway…`), Dynamics / EBS gömülü zaman, PostgreSQL `[pid] LEVEL:` ve DETAIL / STATEMENT satırlarının üstteki hataya katlanması, SQL Server `Severity: N`; zaman damgasız satırlar (stack trace, ORA- blokları) üstteki olaya katlanır (`_lines`). Syslog: RFC 5424 (`<PRI>1`, yapısal veri), Cisco ASA `%ASA-4-…` (rakam = önem), PAN-OS CSV yükü, program alanı olmayan başlık; sunucu adı seviye sözcüğü olamaz |
| `docker/Caddyfile`, compose `edge` profili, `watchover.sh edge` | CF5.1 | Uygulamayı şirket ağına https ile açmak | Caddy dashboard'un önünde: `tls internal` (kendi CA'sı, kökü `edge cert` ile dışa aktarılır) ya da `./certs/watchover.crt+key`; 80→443, HSTS ve diğer başlıklar, `/ingest` alıcıya, gerisi arayüz + API; `edge on <ad[,ad2,ip]>` düz 8501'i yayınlamaz (`docker-compose.edge.yml`, `COMPOSE_FILE`), `edge off` geri alır; sertifika verilen tüm adlara ve Mac'in IP'sine kesilir (`WATCHOVER_EDGE_HOSTS`), `.local` adlar macOS'ta `dns-sd -P` ile Bonjour üzerinden duyurulur (`.edge-mdns.pid`, `edge off` durdurur). Ajan `--ca` / `--insecure` ile https alıcıya gönderir; uvicorn vekil başlıklarını okur (gerçek istemci IP'si). Rehber bölüm 8 |
| `src/watchover/api/routers/{map,assist,llm,itsm}.py` | CF5.1 | Faz 3 router'ları | `map` (build_map + to_html, yerelleştirilmiş etiketler, canlı analiz önbelleği), `assist` (ask / rate / save / propose-rules), `llm` (ayarlar maskeli anahtarla, test, modeller, Ollama listeleme / arka planda indirme / silme, kalite ve kıyas), `itsm` (yapılandırma `x_itsm_*` anahtarlarında, sırlar şifreli; ticket çekimi 5 dk önbellekli, canlı sinyal + metrik + incident ile korelasyon); `live` eklemeleri `/lines`, `/report`; `datasets` eklemeleri `/search`, `/compare`; `system` eklemeleri `/settings`, `/channels`, `/update`, `/snapshots/{id}/rollback`, `/restart`; `auth` eklemeleri `/options`, `/register`, `/verify`; `services` LLM yapılandırması (ilk açılışta Ollama keşfi), gömme modeli, alıcı varsayılan açık, kendini izleyen ajan |
| `src/watchover/api/` | CF5.1 | HTTP API (FastAPI), Streamlit'ten çıkışın 1. fazı | `create_app()` / `python -m watchover.api --port 8000`; `services.py` motoru Streamlit'siz kurar (paylaşımlı veritabanı, bütün depolar, canlı akış + rollup, uyarı motoru, anomali takibi, kaynak çekici, isteğe bağlı alıcı) ve `DatasetRegistry` (arka planda yükleme işleri, ilerleme yüzdesi, ilk aksiyonlar, playbook ve bilgi tabanı kaydı); `security.py` stdlib HS256 JWT (`WATCHOVER_API_SECRET` ya da `data/.api.key`), `current_user`, `require(perm)` RBAC; router'lar: `auth` (login, e-posta MFA, me, parola), `datasets` (yükleme / demo / işler / özet / incident + anlatı / postmortem / sinyal / gürültü / kanıt / dışa aktarma), `live` (istatistik, metrik, SLO, sunucular, olaylar, geçmiş, simülasyon, tampon → veri seti), `actions`, `anomalies`, `playbook`, `agents`, `sources`, `inventory`, `knowledge`, `notify`, `users` / `roles`, `system`; OpenAPI `/api/docs`; KeyError → 404, ValueError → 400; SSO: `/auth/providers`, `/auth/oidc/{google\|microsoft\|oidc}/start` (keşif belgesi, PKCE, imzalı state) ve `/callback` (kod takası, userinfo / id_token, `users.sso_login`, `#sso=<token>` ile uygulamaya dönüş); `web/dist` varsa `/assets` ve SPA yedek yolu |
| `mcp_server.py` | CF5.1 | Motor MCP sunucusu olarak | HTTP modunda `http_app()`: `MCP_API_KEY` verilirse `Authorization: Bearer` / `X-API-Key` zorunlu (401), `/health` açık; göreli veri seti yolları `WATCHOVER_DATASETS` altında aranır (Docker: `./datasets` bağlaması); aksiyonlar `DATABASE_URL` ile dashboard'un veritabanına yazılır. Araç fonksiyonları düz Python (test edilebilir): `analyze_dataset(path)`, `list_incidents()`, `get_incident(id)`, `list_signals(limit)`, `evidence(ref)`, `postmortem(id)`, `create_action(...)`, `list_actions()`. `build_server()` `mcp>=2` `MCPServer`'a araçları kaydeder; `main()` `--http` ile streamable HTTP (DNS rebinding koruması kapalı, Docker / uzak istemci için) ya da stdio |
| `samples/make_demo.py` | CF5.1 | Sentetik demo veri seti üreticisi | `samples/demo_mixed.zip`: 916 olay, 4 dosya (JSONL, düz log, syslog, CSV); 45 dk arka plan trafiği; gömülü zincir DB gecikmesi → payment timeout → checkout 500 → alarm → 14:36 PostgreSQL restart (systemd stop + "ready to accept connections"); ayrı disk dolu incident'ı |

### Motor (`src/watchover/`)

| Dosya | AI aracı | Ne yapar | Nasıl çalışır (fonksiyonlar) |
|---|---|---|---|
| `models.py` | CF5.1 | Kanonik veri modeli | `Observation` (timestamp, message, kind, severity, service, host, **environment**, **origin**, attributes, source, line_no, parser, parser_confidence, template, fingerprint, **raw** = veri setindeki ham satır; `ref` = `dosya:satır`), `Signal` (`evidence`, `why()` kanıt + gerekçe + güven), `Factor` (ağırlık, değer, katkı, ham veri), `Incident` (kök neden, skor, faktörler, kanıt, zaman çizgisi, bağlar, öneriler, gerekçe kodları, `origin`, `timing`, `recovery`), `Action`. `SEV_RANK` seviye sırası |
| `loader.py` | CF5.1 | Universal loader | `decode()` utf-8-sig → utf-16 → latin-1; `iter_bytes()` ZIP / TAR(.GZ) / GZ'yi özyinelemeli açar, ikili dosyaları atlar; `iter_path()` dosya veya klasör |
| `format_detector.py` | CF5.1 | Format tespiti | `detect_delimiter()` `, ; \| tab`; `detect_format()` ilk 200 satırdan json (tek satırlık belge dahil) / jsonl / syslog / csv / kv / text, güven skoru |
| `normalize.py` | CF5.1 | Zaman, seviye, şema auto-mapper | `parse_timestamp()` epoch s/ms → `fromisoformat` → şekil imzasına göre önbelleklenen strptime → dateutil; hep tz-aware UTC. `normalize_severity()` warn/err/P1/major/syslog 0-7 → DEBUG..CRITICAL; `severity_from_text()`. `auto_map()` açık mapping > isim ipucu (İngilizce + Türkçe + Prometheus) > sonek > değer bazlı tahmin; roller: timestamp, severity, service, host, **environment** (env / stage / tier / ortam; değerleri prod/test/dev… olan sütun otomatik), **origin** (source / origin / component / subsystem / category / kaynak), message; sayısal sütunu mesaj seçmez. `normalize_environment()` Production / PRD / prod-eu → prod, `infer_environment()` sütun yoksa host / servis / dosya adı / mesajdan (prd-api-01 → prod, dev-worker → dev). `flatten()` iç içe JSON'u `a.b` anahtarlarına açar |
| `parsers/base.py` | CF5.1 | Parser arayüzü | `Parser.records(text)` → `(satır_no, dict)`; `parse()` bunları Observation'a çevirir |
| `parsers/common.py` | CF5.1 | Kayıt → Observation | `records_to_observations()` rolleri ilk 50 kaydın anahtar birleşiminden çözer, her kayda ham satırı (`raw`) ekler (JSON dizisinde kaydın kendisi), zamansız satırları işaretler, dosya adına göre kind (alert / incident), rol dışı alanları `attributes`'a koyar |
| `parsers/json_parser.py` | CF5.1 | JSON | `find_records()` belgede iç içe ilk dict listesini bulur (Alertmanager `data.alerts`) |
| `parsers/jsonl_parser.py` | CF5.1 | JSONL / NDJSON | satır başına nesne, bozuk satırı atlar |
| `parsers/csv_parser.py` | CF5.1 | CSV / TSV | ayraç tespiti, başlık satırı 1, kayıtlar 2'den başlar |
| `parsers/syslog_parser.py` | CF5.1 | Syslog | RFC3164 + `<PRI>` (PRI % 8 → seviye), host, program, pid |
| `parsers/sap_parser.py` | CF5.1 | SAP log ailesi | Tek durumlu satır parser'ı, uzantıdan bağımsız (`.log .lst .jvm .out .trc .file` ya da uzantısız `dev_w0`): **ABAP kernel developer trace** (`M Wed Sep 16 02:14:09:501 2026` zaman satırı sonraki satırlara miras; `*** ERROR =>` / `*** WARNING =>` / `*** LOG Q0I=>`; `[Thr n]`, bileşen harfi, `sid` → servis `sap-prd`, `profile` → host, pid, release), **HANA trace** (`[thr]{conn}[tx/user] zaman i/w/e/f/d bileşen kaynak(satır) : mesaj`), **NetWeaver Java defaultTrace / applications.log** (`#2.0 #yyyy mm dd hh:mm:ss:ms#+0300#Error#logger#` çok satırlı kayıt, kategori `BC-JAS-DPL`, son alan mesaj), **java.util.logging iki satır** (`Sep 16, 2026 2:14:20 AM logger method` + `SEVERE: mesaj`), **JVM GC** (birleşik `[ts][..][info][gc]` ve klasik `ts: n: [Full GC ...]`; duraklama ≥ 1 sn WARN, ≥ 5 sn ERROR, `gc.pause_ms`), **tp / R3trans / SUM** (`4 ETW000 date&time : 16.09.2026 - 02:14:35` zaman taşıma, `2EETW125` E/W harfi, `exit code : "12"`), **transport ALOG / SLOG** (`ERR imp all PRD 0008 20260916021445`), **SM21 sistem günlüğü dışa aktarımı** (`02:14:51 UPD 003 100 USER TCODE R6 8 mesaj`; başlıktaki tarih taşınır, mesaj sınıfı rakamı → seviye), **sapstartsrv erişilebilirlik günlüğü** (`Unavailable 24.10.2021 18:51:33 - 24.10.2021 19:34:33` → başlangıç zamanlı ERROR / INFO, `avail.duration_min`), **SAP JVM özellik anlık görüntüsü** (`bootstrap.jvm`, `datcol.jvm`: `# created at …` + `key = value` → dosya başına tek INFO kaydı, `jvm.*`), **class prefetch listesi** (`class_prefetch.lst` → tek DEBUG kaydı, paket sayımı; 1.000 sahte olay üretmez). Gerçek NetWeaver 7.50 `deploy.log` doğrulandı: `#2.0<BS>#` sürüm alanı (backspace karakteri), `<!--LOGHEADER-->` başlıkları meta olur (`sap.log_name` → sid / instance), üç satırlık kayıt boş satırda biter, `sap.msg_id` / `sap.category` / `sap.application` / `sap.user` alanları. `sap_score()` format tespitinde kullanılır (`trc file:`, `<!--LOGHEADER`, `# created at` gibi güçlü işaretler tek başına yeter; yoksa satırların ≥ %50'si SAP ailesinden olmalı). Tüm alanlar `sap.*` / `gc.*` attribute'larında kanıt olarak kalır |
| `parsers/kv_parser.py` | CF5.1 | key=value (logfmt) | satır başı zaman / seviye / logger önekini de alır, tırnaklı değerler |
| `parsers/text_parser.py` | CF5.1 | Düz metin | öncü zaman (ISO, `2026/09/16`, `16.09.2026 02:14:07`, `Wed Sep 16 02:14:07 2026`, `Sep 16, 2026 2:14:07 AM`), seviye (SEVERE / CONFIG / FINE dahil), logger; kalan mesaj |
| `anomaly.py` | CF5.1 | Anomali takibi | `AnomalyTracker(kb, live)`: her dakika (`start()`) ya da elle `scan()`; her sunucu **kendi** yakın geçmişiyle karşılaştırılır (rollup'lardan son 24 saatin dakikalık değerlerinin medyanı + 1.4826·MAD; metrikler için canlı serinin son 3 saati), dayanıklı z-skor ≥ 4 ve en az 2 kat / 15 puan fark. Türler: `errors` (ERROR+ sıçraması), `rate` (log fırtınası), `silence` (normalde log üreten sunucu sustu), `pattern` (daha önce görülmemiş, tekrar eden şablon; `anomaly_templates` belleği, 1 saat ısınma), `metric` (cpu / bellek / disk / gpu). Aynı anahtar tekrar tetiklenince satır tazelenir (`hits`), tetiklemeyi bırakınca `cleared_at` (normale döndü) ama operatör kapatana kadar açık kalır. Her tür için operasyonel adım listesi (`STEPS`): `set_step` (ilk adım = üstlenme), `attach_action` (aksiyon takibine bağ), `set_status` (open / ack / resolved / ignored, sorumlu, not), `list` / `stats`. Bildirim kuralı koşulu `anomaly` yeni açılan anomalileri duyurur |
| `db.py` | CF5.1 | Ortak veritabanı katmanı | `Database`: `DATABASE_URL` ile PostgreSQL (psycopg 3, otomatik commit, iş parçacığı kilidi, kopan bağlantıda bir kez yeniden bağlanma), yoksa gömülü SQLite; `?` yer tutucuları çevrilir, `pk` / `blob` lehçe farkları, `tables` / `columns` / `count` / `size_bytes`, `dump` (bütün tablolar JSON) ve `restore`, `copy_table` (çakışan anahtar atlanır), `reset_sequence`; `describe()` bağlantıyı parolasız gösterir |
| `migrate.py` | CF5.1 | SQLite → PostgreSQL taşıma | `python -m watchover.migrate [--source DIR] [--target URL] [--dry-run]`: üç `.db` dosyasındaki bütün tablolar aynı adlı tablolara kopyalanır (şema önce bütün depolar açılarak kurulur, var olan anahtarlar atlanır, tekrar çalıştırılabilir; dokunulmamış yerleşik yönetici SQLite hesaplarıyla değiştirilir, dolu bir `users` tablosuna yeni e-postalar yeni kimlikle eklenir, OTP ve hatırlama token'ları taşınmaz); `auto(kb)` uygulama PostgreSQL'e ilk bağlandığında `/data` yanındaki dosyaları bir kez kopyalar ve `.migrated-to-postgres` işaretini yazar |
| `knowledge.py` | CF5.1 | Bilgi tabanı | `Knowledge(Database)` (PostgreSQL / SQLite): `record()` kök neden örüntüsü başına tek ders + tekrar sayacı, `add_note()`, `add_doc()` (parçalama), `add_feedback()` (karar → kural önerisi), `search()` hibrit sıralama (kapsama + örtüşme + embedding), `related()`, `propose()` / `decide()` / `apply_rules()` (scenario katmanı), `stats()` (boyut) |
| `assistant.py` | CF5.1 | Watchover'a sor | `build_context()` incident satırları + kanıt + dersler + kurallar, `answer()` LLM ile (kaynak zorunlu sistem istemi, son 6 mesaj) ya da `fallback_answer()` deterministik derleme; `rule_context()` + `propose_rules()` modelden JSON kural önerisi, `validate_rules()` veriyle doğrulama |
| `assets/` | CF5.1 | Marka | `logo.svg` (göz + radar süpürmesi + sinyal yayları, turkuaz → mavi), `logo-wordmark.svg`, PNG türevleri (64 / 256 / wordmark). Kenar çubuğu markası, sekme simgesi, sohbet avatarı, Watchover'a sor başlığı ve README bu dosyaları kullanır |
| `llm.py` | CF5.1 | Sağlayıcıdan bağımsız LLM | `LLMConfig(provider, base_url, model, api_key, embed_model)`, `kind` (URL'den tahmin), `chat_messages()` / `chat()` / `embed()` / `list_models()` dört uç biçimi için, `set_sink()` çağrı metrikleri |
| `ollama.py` | CF5.1 | Yerel model yöneticisi | `discover()`, `installed()`, `running()`, `pull()` (ilerleme akışı), `remove()`, `RECOMMENDED` |
| `llm_eval.py` | CF5.1 | LLM kalitesi | `grounding()` atıf doğrulama, `run_benchmark()` kök neden testi (deterministik karıştırma, motorla uyum) |
| `scripts/install_linux.sh`, `scripts/watchover.service` | CF5.1 | Linux kurulum | venv + isteğe bağlı Ollama + modeller + systemd |
| `settings.py` | CF5.1 | Kalıcı ayarlar | `load()` / `save()` (0600), `setup_done()`, `SESSION_KEYS` oturum varsayılanları; `WATCHOVER_HOME` |
| `agents.py` | CF5.1 | Ajan kimliği | `AgentRegistry`: `enroll()` (tek seferlik token), `verify()` (sha256, önbellek), `revoke()` / `rotate()` / `delete()` / `touch()`; alıcı `make_handler(store, api_key, registry)` kimliği çözer |
| `scripts/install.sh`, `scripts/watchover-launcher.sh` | CF5.1 | Tek satır kurulum | macOS / Linux kullanıcı kurulumu, Ollama + modeller, `watchover` komutu (`update [zip]` ile git olmadan güncelleme), LaunchAgent / user systemd; launcher launchd / systemd farkında (`restart` servisi yeniden başlatır), üretimde hot-reload kapalı, `update` launcher'ı da yeniler |
| `src/watchover/sources.py` | CF5.1 | Çekme kaynakları | `KINDS` kataloğu, `row_from_record` (ECS / serbest alan eşleme), `fetch_elasticsearch` / `fetch_loki` / `fetch_splunk` / `fetch_graylog` / `fetch_http` (imleçli), `SourceStore` (bilgi tabanı DB'sinde `sources` tablosu), `Poller` (arka plan iş parçacığı, tekrar eleme, hata kaydı), `test_source` |
| `src/watchover/report.py` | CF5.1 | SLO raporu | `slo_report()`: kapsam + dönem → kendi başına HTML (yönetici özeti, KPI, SVG grafik, tablolar, incident'lar), TR / EN, yazdırma CSS'i |
| `src/watchover/auth.py` | CF5.1 | Kullanıcılar ve giriş | `Users` (bootstrap: yerleşik yönetici `admin@watchover.local`, sabit başlangıç parolası + zorunlu değişim `must_change`, dokunulmamış yöneticiyi yeniden anahtarlama; `apple_client_secret` ES256 JWT; `remember_issue / remember_lookup / remember_revoke / remember_revoke_all` (beni hatırla belirteçleri); `otp_issue / otp_verify` (e-posta MFA kodları); `secrets_toml` google / apple / microsoft / oidc blokları; register / login / sso_login / roller / durum / parola), scrypt karma, `write_oidc_secrets` |
| `src/watchover/admin.py` | CF5.1 | Sistem yönetimi | `version_info`, `apply_zip` (korunan yollar), `git_update`, `restart_app` (launcher / çıkış), `db_info`, `vacuum`, `tail_log` |
| `src/watchover/inventory.py` | CF5.1 | Envanter | `Inventory`: `upsert / get / list / delete`, `import_csv` (takma adlı sütunlar) / `export_csv` / `csv_template`, `index / resolve` (ad, kısa ad, takma ad, IP), `enrich(o)` olay zenginleştirme, `as_engine()`, `discovered()`, `stats()` |
| `src/watchover/useradmin.py` | CF5.1 | Terminalden hesap yönetimi | `watchover user list / add EMAIL --admin / promote / password / unlock / enable / disable / delete`; uygulamayla aynı veritabanını kullanır, kimse giriş yapamadığında kurtarma yoludur (`Users.unlock` kilidi kaldırır) |
| `src/watchover/rbac.py` | CF5.1 | Roller ve yetkiler | `PERMISSIONS` kataloğu (page.* sayfalar, act.* işlemler, sys.* sistem sekmeleri), `BUILTIN` yönetici / operatör / izleyici varsayılanları, `Roles` (`list / get / perms / can / save / delete / reset`, `roles` tablosu); yönetici daraltılamaz, atanmış rol silinemez |
| `src/watchover/selfmon.py` | CF5.1 | Bu makinenin izlenmesi | `ensure_token` (kayıt defterinde `hostname` adlı ajan, şifreli saklanan token, kaybolursa döndürme), `command` (`agent.py --auto --metrics [--journal]` yerel alıcıya), `SelfMonitor` (kilitli `start`, `ensure` ile yeniden başlatma, `stop`, `data/self-agent.pid` ile eski ajanın değiştirilmesi) |
| `src/watchover/release.py` | CF5.1 | Sürümleme ve sürüm notları | `current / display / next_version / bump / entries`; `python -m watchover.release minor|patch|major "özet" [--commit] [--push]` sürümü `__init__.py` + `pyproject.toml`'a yazar, `CHANGELOG.md`'ye girdi ekler, `vX.Y` etiketi atar; `watchover version` komutu |
| `watchover.sh` | CF5.1 | Kurumsal Docker kurulumu, tek komut | `install` (`.env` üretimi: rastgele PostgreSQL parolası ve MCP anahtarı, 600 izin; imaj sunucuda kaynak koddan derlenir; postgres sağlıklı olunca dashboard; adres ve ilk giriş bilgisi basılır), `update` (yeni paket üstüne açıldıktan sonra yeniden derle + yenile), `start` / `stop` / `restart` / `status` / `logs`, `mcp on\|off`, `backup` / `restore` (pg_dump + veri birimi + .env), `user …` (useradmin), `migrate` (SQLite → PostgreSQL), `save` / `load` (kapalı ağ için imaj tgz), `shell`. GitHub ya da imaj kaydı gerektirmez; sahte Docker ile testlenir (`tests/test_docker_script.py`) |
| `Dockerfile`, `docker-compose.yml`, `.env.docker.example`, `.github/workflows/docker.yml` | CF5.1 | Docker dağıtımı | Root olmayan kullanıcı, tini, sağlık denetimi, `/data` birimi (ayarlar, veritabanları, tampon, anlık görüntüler, log), derlemede gömülen git kimliği; compose: `postgres:16-alpine` zorunlu servis (sağlık denetimi, `depends_on: service_healthy`, `watchover-pg` birimi, yalnız localhost'a açılan 5433) + dashboard + isteğe bağlı `mcp` profili, `DATABASE_URL` otomatik kurulur, kaynak sınırları (`WATCHOVER_CPUS` / `WATCHOVER_MEMORY`, `WATCHOVER_PG_*`), log döndürme, `restart: unless-stopped`; Actions: testler geçince `ghcr.io/bannercheck/watchover` (`:latest`, `:main`, `:X.Y`), amd64 + arm64 |
| `src/watchover/vault.py` | CF5.1 | Sırların şifreli saklanması | `encrypt / decrypt` (Fernet: AES-128-CBC + HMAC-SHA256, `cryptography`), anahtar `data/.vault.key` (0600) ilk kullanımda üretilir, `enc:v1:` önekli değerler; `settings.SECRET_KEYS` (LLM / alıcı / Google / OIDC / SMTP / SMS sırları) ve `sources` tablosundaki kimlik bilgileri diske hep şifreli yazılır, eski düz değerler ilk kayıtta şifrelenir |
| `src/watchover/notify.py` | CF5.1 | E-posta ve SMS uyarıları | `send_email` (smtplib; STARTTLS / SSL / düz), `send_sms` (`SMS_PRESETS` kataloğu: Netgsm, İleti Merkezi, Verimor, Mutlucell, JetSMS, Posta Güvercini, Twilio, Turkcell / Türk Telekom / Vodafone kurumsal SMS şablonları, genel HTTP; `format_number` +90 / 90 / 0 biçimleri; JSON / XML / form kaçışlı `{to} {msg} {from} {user} {password} {token} {account}` yer tutucuları; sağlayıcının 200 içindeki hata kodu `fail` regex'iyle yakalanır), `Notifier` (kişiler, gruplar, dağıtım listesi adresi, `resolve()` hedef çözümü, kurallar, `dispatch()` bekleme süreli gönderim, teslimat günlüğü `alerts`), `AlertEngine` (her dakika canlı akışa karşı SLO / hata oranı / metrik / ajan sessiz / kaynak hatası / yeni incident deseni koşulları) |
| `src/watchover/history.py` | CF5.1 | SLO geçmişi | `History`: `add()` ingest kancası (dakika rollup + saatlik servis), `flush()` upsert, `prune()`, `figures(start, end, env, host, bucket)` → rapor figürleri, `coverage()` |
| `src/watchover/autolearn.py` | CF5.1 | Canlı öğrenme | `LiveLearner` (N dakikada bir son 60 dk analiz → `kb.record`, `learn_runs` tablosu), `training_export()` sohbet biçimi JSONL |
| `agent.py` | CF5.1 | Sunucu ajanı | Standart kütüphane; `Sender` (Bearer token, 0700/0600 disk spool, kilitli drain, kalıcı 4xx düşürülür), `hello()` / `--test`, `tail_loop()` (`tail -F` gibi: glob, yeniden adlandırma ve copytruncate rotasyonu, yalnız tam satır), `metrics_loop()`, iş parçacığı ölürse çıkış (servis yeniden başlatır), ortam değişkenleriyle yapılandırma (`WATCHOVER_URL/TOKEN/LOGS/METRICS/INTERVAL/SPOOL`) |
| `scripts/agent-install.sh` | CF5.1 | Ajan kurucu | Alıcıdan indirilir; python3 kontrolü, `/opt/watchover-agent`, tırnaklı `agent.env` 0600, olmayan log yolu uyarısı, bağlantı testi, systemd (gerçekten çalışıyorsa; yeniden kurulumda restart) / macOS root'ta LaunchDaemon / arka plan (eski pid öldürülür), `--uninstall` |
| `stamp.py` | CF5.1 | Tekrarlanabilirlik damgaları | `engine_id()` paketteki tüm .py dosyalarının sha256'sı (kural değişince değişir), `scenario_id()` senaryo ayarları, `git_rev()` checkout sürümü (git çalıştırmadan), `file_id()` dosya içeriği (klasör, `\` / `/` ve satır sonu bağımsız), `input_id()` yüklenen setin kimliği (sıra bağımsız), `result_id()` incident listesinin kimliği, `stale_package()` içe aktarılan motorun repo `src/` kopyası olmadığını yakalar. Kenar çubuğunda `v0.3.0 · motor · git · senaryo`, veri seti satırında `girdi · motor · sonuç`; CLI ve `.summary.json` aynı damgayı taşır |
| `pipeline.py` | CF5.1 | Ingest orkestrasyonu | `ingest()` loader → detector → parser, dosya raporu (format, güven, satır, tür, anahtarlar, roller), `scenario.MAPPING`; `fill_missing_timestamps()` zamansız satırlara veri setinin en erken zamanı; `ingest_path()`, `ingest_bytes()` |
| `profiler.py` | CF5.1 | Dataset profiler | `bucket_for()` aralığa göre 1 dk / 5 dk / 1 sa / 1 gün; `profile()` pandas ile dosya, kayıt, kaynak türleri, servis/host, hata sınıfı, seviye, ortam başına olay/hata/hata oranı, hata kaynakları, zaman aralığı, zaman serisi, dosyalar arası ilişki önerisi (id/host/service benzeri kolonlarda ≥ %50 değer örtüşmesi); `profile_text()` iki dilli özet |
| `analysis.py` | CF5.1 | Deterministik motor | `template_of()` tek birleşik regex ile uuid / ip / hex / zaman / url / yol / sayı maskeleme (+ `scenario.EXTRA_MASKS`); `entities_of()` servis / host / ip / id varlıkları; `fingerprint()` sha1(şablon + seviye + servis); `build_signals()` + `score_burst()` tepe vs tüm aralıktaki medyan (sessiz dakikalar sayılır ama liste olarak üretilmez; 4 yıllık SAP günlüğünde 60 sn → 1 sn); `interesting()` WARN+ veya burst ≥ 0.5; `correlate()` union-find: onset'ler pencere içinde VE (ortak varlık VEYA ikisi de burst); `pick_root_cause()` en erken onset (-3/dk), altyapı kelimesi (+1.5), fan-out, seviye; `build_incident()` (başlık şablon boşsa ham satırdan) `WEIGHTS` (burst .35, severity .25, blast_radius .25, duration .15) ile faktör katkıları, seviye, zaman çizgisi, kanıt, öneriler; `explain_incident()` her incident için **origin** (dosya sayıları, servis, host, ajan, parser, tür), **timing** (ilk sinyal, son hata, süre, sessiz süre, hata sayısı) ve **recovery** (son hatadan sonra ≥ 2 dk sessizlik; `RESTART_RE` ile restart/başlatma satırı → `restart`; INFO trafiğin dönmesi → `self_healed`; kanıt yoksa `stopped`; hata sona kadar sürüyorsa `ongoing`; kanıt satırı ve saati); `postmortem_md()`, `llm_prompt()`; `Analysis` sınıfı uçtan uca akış, `funnel()`, `signal_by_id`, `incident_by_id`, `obs_by_ref` indeksleri; `signal_dict()`, `incident_dict()` |
| `compare.py` | CF5.1 | İki veri setini karşılaştırma | `kpis()`; `compare()` KPI deltaları (Δ, Δ %), seviye dağılımı, ortak / sadece A / sadece B şablonlar (sayı ve patlama farkı), incident listeleri, göreli dakika zaman çizgisi |
| `actions.py` | CF5.1 | Aksiyon takibi | `ActionStore` (paylaşımlı veritabanı, PostgreSQL; tek başına SQLite yolu da alır): `list`, `create` (öncelik P1-P4, sorumlu, öneri, kanıt), `update` (open / in_progress / done / suppressed), `delete`, `get` |
| `live.py` | CF5.1 | Canlı alım | `LiveStore` olay halka tamponu + metrik tamponu + JSONL spool; `ingest()` gelen gövdeyi aynı parser'lardan geçirir, `metric_of()` ile cpu / gpu / memory / disk kayıtlarını ayırır; `snapshot()` / `stats()` / `metric_stats()` / `slo()` / `tail()` isteğe bağlı `env` ve `host` filtresi alır; `stats()` dakika × seviye matrisi, servisler, ajanlar; `metric_stats()` host bazlı son değer, dakikalık seri (ortam sütunlu), eşik aşımları, özet, `per_host` (host → ortam + son değerler); `slo()` erişilebilirlik, mesajlardaki `<n>ms` ile p95, hata bütçesi, SLO / SLA durumu; `slo_detail()` servis seviyesi kartlarının dayanağı (servis / host / kalıp bazında ERROR+, servis bazında p50/p95/maks gecikme, en yavaş istekler, dakika bazında erişilebilirlik ve kalan bütçe, hedef altı dakika sayısı, son hatalar); `environments()`, `hosts()` (host → ortam), `env_summary()` (ortam başına olay / hata / erişilebilirlik / sunucu, `ENV_ORDER` sırasıyla); `to_dataset()`; HTTP alıcı `make_handler` / `start_receiver` (`POST /ingest` X-API-Key veya Bearer, `GET /health`); `simulate_batch()`, `simulate_metrics()` (5 host + 2 GPU host, `SIM_ENV` ile prod / staging / dev / qa etiketi, ~2 dk'da bir hata dalgası), `start_simulator()` |
| `itsm.py` | CF5.1 | ITSM entegrasyonu | `Ticket`; `fetch_servicenow()` Table API (basic / token, sysparm_query), `fetch_jira()` REST v3 (JQL, ADF açıklama), `fetch_onedesk()`, `fetch_generic()` (JSON yolu + alan eşleme), `demo_tickets()`; `correlate()` ticket metnindeki servis / host / anahtar kelimelerin canlı sinyaller, metrik eşik aşımları, incident'larla örtüşmesi (0.65) + açılış zamanının bir patlamaya ±30 dk yakınlığı (0.35) → ilgililik 0..1 ve gerekçeler, eşitlikte yeni ticket önce |
| `connectors.py` | CF5.1 | Veri kaynağı bağlayıcıları | `fetch_http()` GET / POST, başlıklar, JSON yolu (`dig()`), içerik türünden dosya adı; `McpClient` MCP streamable-HTTP JSON-RPC (initialize → notifications/initialized → tools/list, tools/call; Mcp-Session-Id, SSE cevap); `fetch_mcp()` bir aracın çıktısını veri seti yapar; `mcp_tools()`; `parse_headers()` |
| `llm.py` | CF5.1 | LLM istemcisi | `LLMConfig` (base_url, model, api_key); `list_models()` `/models`; `chat()` `/chat/completions`; `test_connection()`. Çekirdek buna bağlı değil |
| `graph.py` | CF5.1 | Hata haritası motoru | `DEP_RE` hata mesajlarından bağımlılık hedefi çıkarır (upstream / to / from / calling + servis adı, db-01, postgres, redis…); `dependency_edges()` (servis → hedef) sayaçları; `build_map(analysis, incident_id, env, host, with_hosts)` düğümler (tür: root / affected / erroring / clean, ERROR+ sayısı), bağımlılık bağları (sunucu adı o sunucudaki incident servisine eşlenir), incident `links`'ten servis-servis ilişki bağları, sunucu → servis bağları (ortamıyla), kök nedenden geriye yürüyen `chain`; `layout()` katmanlı yerleşim (sunucular ortam gruplarında solda, bağımlılar → kök neden sağda); `to_html()` harici kütüphanesiz etkileşimli SVG sayfası (kaydırma / yakınlaştırma, düğüme tıklayınca odaklanma + komşuları vurgulama + detay paneli, seçimi `window.name` ile yeniden çizimler arasında koruma); `to_dot()` Graphviz DOT dışa aktarımı |
| `enrich.py` | CF5.1 | Zenginleştirilmiş tek dosya | `enrich(analysis)` alarm başına 30 sütun (envanter, bağımlılık, sınıf, sıcak hücre, incident, rol, gürültü nedeni), `to_csv` / `to_jsonl`, `summary()` kart özetleri; CLI `--enrich` |
| `storm.py` | CF5.1 | Alarm fırtınası kümeleyici | `cluster()`: 5 dk'lık kovalarda servis ve sunucu başına sayım, medyanın 3 katını aşan sıcak hücreler, aynı servis / bağımlılık / aynı kabin ile bağlama (union-find), olay alarmları ve gürültü; `prune_background()` küme içinde normal hızındaki (servis, tip) çiftlerini gürültüye geri verir |
| `tables.py` | CF5.1 | Yan tablolar | `is_side_table()` dosya adından referans tablosu tespiti, `read_table()` CSV / JSON okuma, `dependencies()` (kaynak → hedef, tip, kritiklik; sütun adları `scenario.DEP_COLUMNS`), `inventory()` (host → servis, dc, kabin, ortam, kritiklik) |
| `i18n.py` | CF5.1 | TR / EN metinler | `STRINGS` sözlüğü; `t()` aktif dil; `reason_text()`, `link_text()`, `narrative_text()`, `factor_value()`, `recommendation_text()` motor kodlarını dile çevirir; `RECOMMENDATIONS_TR` |
| `cli.py` | CF5.1 | Komut satırı | `inspect()` dosya başına format, roller, zaman aralığı, seviye, sütun profili; `summary()`; `main()` `watchover <path> [--inspect] [--json]` |
| `scenario/__init__.py` (S-A1 ayarları: SEVERITY_MAP, SIDE_TABLES, DEDUP_KEY, MAX_INCIDENTS, CLUSTERING / BUCKET_MIN / HOT_MIN / HOT_RATIO / HOST_HOT_MIN / PAD_MIN / INFRA_TYPES / RACK_MIN_HOSTS / MIN_CLUSTER_ALARMS / MIN_CLUSTER_ERRORS / PRUNE_BACKGROUND, CAUSE_RANK, DEP_PATTERNS, RECOMMENDATIONS, OWNERS) | CF5.1 | Yapılandırma | `MAPPING`, `EXTRA_MASKS`, `EXTRA_DEPENDENCY_WORDS`, `WINDOW_MIN`, `WEIGHTS`, `RECOMMENDATIONS`, `SLO`, `SLA`, `METRIC_THRESHOLDS` (cpu 85, gpu 95, memory 90, disk 90). Veri setine özel her ayar burada |

### Testler (`tests/`)

| Dosya | AI aracı | Kapsam |
|---|---|---|
| `test_pipeline.py` | CF5.1 | Format tespiti, 7 parser (SAP ailesi: 12 örnek dosya, 12 biçim, gerçek deploy.log kesiti), aralıktan bağımsız patlama puanı, değer bazlı auto-map, açık mapping, yardımcılar, ZIP + TAR.GZ, gerçek veri şekilleri (iç içe JSON, Türkçe CSV, kv öneki, zamansız satır), profil, patlama, incident zinciri / kök neden / gerekçe, aksiyon deposu, iki set karşılaştırma |
| `test_smoke.py` | CF5.1 | Streamlit `AppTest`: demo yükleme, dil geçişi, çoklu veri seti kaydı ve karşılaştırma |
| `test_connectors.py` | CF5.1 | HTTP bağlayıcı, MCP istemcisi (sahte JSON-RPC sunucu, SSE, oturum başlığı, hata), `dig`, MCP sunucu araçları |
| `test_live_llm.py` | CF5.1 | Alıcı kimlik doğrulama ve formatlar, simülatör, ajan dosya modu, sahte OpenAI uyumlu sunucuyla LLM istemcisi |
| `test_api.py` (+ SSO, SPA) | CF5.1 | HTTP üzerinden: sağlık, giriş / yanlış parola / bozuk token, me ve yetkiler, OpenAPI; demo seti, incident + anlatı, postmortem, sinyal, kanıt, gürültü, CSV dışa aktarma, aksiyon CRUD, çoklu dosya yükleme işi ve ilerleme; kullanıcı ekleme, izleyicinin 403'leri, özel rol ile yetki kazanma, parola değişimi; anomali adımları / aksiyon / durum, playbook notları, bilgi tabanı not / arama / kural; canlı istatistik, simülasyon, envanter, kaynak (sır maskesi), bildirim alıcı / kural, ajan kayıt / döndürme / iptal, sistem durumu ve anlık görüntü |
| `test_anomaly.py` | CF5.1 | Dayanıklı taban çizgisi ve z-skor; hata sıçraması tek satır (tekrar = hits), adımlar / üstlenme / aksiyon bağı / çözme / normale dönüş; sessizlik, yeni örüntü (ısınma) ve metrik türleri; `anomaly` bildirim kuralı yalnız ilk açılışta; Operasyon sayfasında bölüm ve adım kutusunun durumu değiştirmesi (AppTest) |
| `test_postgres.py` | CF5.1 | `WATCHOVER_TEST_DATABASE_URL` verildiğinde gerçek PostgreSQL'de: yer tutucu / `%` kaçışı, dersler, kullanıcılar (kayıt, giriş, OTP, hatırlama), roller, bildirim alıcıları, ajanlar, kaynaklar, envanter, rollup upsert, aksiyon + playbook aynı veritabanında, `dump` / `restore` ve sıra devamı, SQLite → PostgreSQL taşıma (tekrar çalıştırma kopyalamaz), uygulamanın demo setiyle açılıp Sistem sayfasında `postgresql` göstermesi |
| `test_playbook.py` | CF5.1 | Kayıt, veri seti başına tekil sayım, düzelme türleri, runbook tohumlama, notlar, arama, bulanık eşleşme, silme |
| `test_itsm_metrics.py` | CF5.1 | Sahte ServiceNow / Jira / genel REST sunucularıyla fetch, ticket ilişkilendirme sıralaması, metrik alımı + eşik aşımı + SLO |

### Diğer dosyalar

| Dosya | Ne yapar |
|---|---|
| `pyproject.toml` | Paket tanımı, bağımlılıklar (streamlit, pandas, python-dateutil), `watchover` CLI, `dev` / `mcp` ekstraları |
| `requirements.txt` | Aynı bağımlılıkların düz listesi (`pip install -r requirements.txt` + `pip install -e .`); Windows adımları README Kurulum bölümünde |
| `Makefile` | `make run / test / demo / inspect DS=x / analyze DS=x` |
| `.streamlit/config.toml` | 1 GB yükleme sınırı, koyu tema |
| `Dockerfile`, `docker-compose.yml` | `postgres` (ürün veritabanı), `dashboard` (8501 / 8600) ve `mcp` (8765) servisleri; `watchover-data` → `/data`, `watchover-pg` → veritabanı |
| `samples/demo_mixed.zip` | Üretilmiş demo veri seti |
| `docs/bulgular.md` | S-A1 paketi bulgu raporu: özet tablo (5 kart, ilk aksiyon, sahip, müdahale sırası) + olay başına ayrıntı (ne oldu, nerede, kök neden gerekçesi, karşı olasılıklar, zaman aşımı hedefleri, nasıl bitti, ilk aksiyon), gürültü analizi, bağımlılık / envanter katkısı, kısıtlar |
| `docs/plan.md`, `docs/fazlar.md`, `docs/mimari.md`, `docs/AI_LOG.md` | Hedef / kapsam / riskler / demo akışı; fazlar ve etkinlik günü saatleri; mimari, veri modeli, ADR; AI kullanım kaydı |
| `AI_JURI.md`, `submission.json` | Jüri için yapılandırılmış özet; teslim künyesi |
| `prompts/` | Uygulamayı şekillendiren 9 kritik prompt (amaç, model, tarih, prompt, çıktı) |
| `demo/` | Ekran görüntüleri ve açıklamaları |
| `.env.example` | Ortam değişkeni örnekleri (hiçbiri zorunlu değil) |
| `CLAUDE.md` | Bu depoda çalışan Claude için proje kuralları |

## 5. Teknolojiler

| Katman | Teknoloji | Neden |
|---|---|---|
| Arayüz | Streamlit + Altair | Tek süreç, build yok; `st.fragment(run_every)` ile canlı yenileme; Altair Streamlit ile gelir |
| Veri | pandas | Profil, gruplama, zaman kovaları |
| Ayrıştırma | stdlib `csv` / `json` / `re` / `zipfile` / `tarfile` / `gzip`, python-dateutil | Bağımlılık yok, her ortamda çalışır |
| Zaman | `fromisoformat` + strptime önbelleği + dateutil (son çare) | 300k satırda ~6 sn |
| Depolama | SQLite (aksiyonlar), JSONL spool (canlı olaylar) | Kurulumsuz |
| Bağlantılar | stdlib `urllib` (HTTP API, MCP JSON-RPC istemcisi, ITSM REST, OpenAI uyumlu LLM) | API anahtarı başlıkla, TLS varsayılan |
| MCP sunucusu | `mcp>=2` (`MCPServer`), streamable HTTP / stdio | İstemciden bağımsız |
| Ajan | stdlib (`urllib`, `/proc`, `shutil`, `nvidia-smi`) | Her host'ta çalışır |
| Test | pytest + `streamlit.testing.v1.AppTest` + in-process sahte HTTP sunucular | 27 test, ağ gerektirmez |
| Konteyner | Dockerfile, docker-compose | postgres + dashboard + mcp |
| Veritabanı | PostgreSQL 16 (psycopg 3); DATABASE_URL yoksa gömülü SQLite | kullanıcılar, roller, bilgi tabanı, aksiyonlar, playbook, ajanlar, kaynaklar, envanter, bildirimler, rollup'lar |

Performans: 300.000 satırlık JSONL ~15 sn (yükleme 6, analiz 7, profil 2).

### Watchover'a sor: sohbet, bilgi tabanı, kurallar

Motor karar verir; asistan açıklar, hatırlar ve önerir. Üç parça:

- **Bilgi tabanı** (`knowledge.py`; ürün veritabanı PostgreSQL, `DATABASE_URL` boşsa gömülü SQLite). Her analizden **kök neden örüntüsü başına tek ders** yazılır: aynı örüntü tekrar görülünce satır çoğalmaz, `occurrences` artar ve (veri seti, incident) referansı eklenir. Ham alarm asla kopyalanmaz; ders yalnız kanıt referansı (`dosya:satır`) ve girdi kimliği taşır. Notlar ve belgeler içerik hash'iyle tekilleştirilir; belgeler ~900 karakterlik parçalara bölünür. Embedding isteğe bağlıdır ve float16 olarak ders satırının yanında durur (768 boyut ≈ 1,5 KB). Bin incident'lık bir yıl birkaç yüz satır, birkaç MB'dir. Arama hibrittir: sorgu kapsaması + token örtüşmesi, embedding varsa kosinüs, tekrar sayısı için küçük bir öncelik.
- **Öğretme.** Not ekle, runbook / postmortem yükle, "bilgi öğret" formuyla sahip / neden önceliği / gürültü tipi / bağımlılık / ilk aksiyon önerisi. Incident kartının altında **Daha önce görüldü** (bilgi tabanından benzer dersler) ve **karar** paneli: 👍/👎, doğru kök neden (karşı olasılıklardan seçim, "gürültü", "başka"), not. 👎 kararlar kural önerisi üretir.
- **Kurallar.** Üç kaynaktan gelir: ekip kararları, elle öğretilen bilgiler ve **modelin kendi önerileri** (Kurallar › "Modelden öneri iste": model alarm tiplerini, servisleri, incident'ları ve karşı olasılıklarını, ekip kararlarını, notları ve mevcut kuralları okur, JSON döndürür; `validate_rules` bilinmeyen alarm tipi / servis, aralık dışı değer ve izinsiz tür içerenleri eler; kalanlar `llm:<model>` kaynağıyla **öneri** olarak kaydedilir, 🤖 rozetiyle listelenir). Öneriler bir insan onaylayana kadar uygulanmaz. Onaylanan kural `scenario` üzerine bellek içi bir katman olarak biner (`apply_rules`: OWNERS, CAUSE_RANK, NOISE_TYPES, NOISE_TEMPLATES, EXTRA_DEPENDENCIES, RECOMMENDATIONS) ve yüklü tüm veri setleri yeniden analiz edilir. Geri alınan kural etkisiyle birlikte kalkar (her uygulama temiz tabandan). Gürültü kuralıyla elenen alarmlar huni ve denetimde `n_rule` gerekçesiyle görünür.
- **Sohbet** (`assistant.py`). Soru → bağlam (yüklü veri setinin incident satırları; soruda geçen incident'ın kanıt satırları ve karşı olasılıkları; bilgi tabanından geri alınan dersler; aktif kurallar) → OpenAI uyumlu LLM (Ollama, vLLM, LM Studio, bulut). Sistem istemi her iddiaya kaynak zorunlu kılar: `[INC-3]`, `[L12]`, `[dosya:satır]`. Cevabın altında kaynak kartları ve modele gönderilen bağlam görülebilir; cevap ders olarak kaydedilebilir. LLM bağlı değilse aynı bağlam deterministik olarak derlenir, sohbet hiç kapanmaz. Embedding modeli Bağlantı ayarları › LLM'de (`/embeddings`).

### Kaynaklar: Elasticsearch, OpenSearch, Loki, Splunk, Graylog, HTTP'den canlı çekim

Sol menüdeki **Kaynaklar** sayfası (`src/watchover/sources.py`) kurumun zaten kullandığı log platformunu düzenli aralıkla sorgular ve yeni kayıtları canlı akışa bir ajan göndermiş gibi ekler; sunucuya ajan, platforma değişiklik gerekmez. Her platform tek bir `fetch()` fonksiyonudur: Elasticsearch / OpenSearch (`_search`, `@timestamp` aralık sorgusu, ECS veya serbest alanlar), Grafana Loki (`query_range`, etiketler → host / servis, JSON satırlar açılır), Splunk (`jobs/export`, `_raw` + host / sourcetype), Graylog (`search/universal/absolute`), genel HTTP / JSON (JSON yolu, `{since}` yer tutucusu, düz metin satırları biçim tespitine gider). Kimlik: yok / kullanıcı + parola / API anahtarı / bearer; ek başlıklar (X-Scope-OrgID), TLS doğrulama seçeneği. Kaynak başına **imleç** (son zaman damgası) veritabanında tutulur, yeniden başlatma aynı aralığı ikinci kez almaz; son 5.000 satırın hash'i sınır tekrarlarını eler. Arka plan `Poller` her etkin kaynağı kendi aralığında sorgular, hata alan kaynağı işaretler ve diğerlerine devam eder. Sayfa: platform kataloğu (ne gerekir, hangi loglar gelir), ekle → **Test** (örnek satırlar) → kaydet, canlı durum satırları (çevrimiçi / bekleniyor / hata / duraklatıldı, olay ve sorgu sayısı), duraklat / sil. Her kaynak ortam / site ile damgalanır; Operasyon filtreleri ajanlar gibi ayırır.

### Operasyonda çoklu sunucu kapsamı, SLO raporu (haftalık / aylık), canlı öğrenme

Operasyon sayfasındaki sunucu filtresi çoklu seçimdir: birden çok sunucu seçilince metrik kartları ve sparkline'lar seçilenlerin **ortalamasını**, olay ve SLO kartları birleşik kapsamı gösterir (`live.host_match`, `graph.build_map` tek ad veya ad kümesi kabul eder). SLO kartlarının detay penceresinde **Raporu dışa aktar**: `report.slo_report()` seçilen kapsam ve dönem (15 dk / 1 sa / 4 sa / 24 sa) için kendi başına açılan, yazdırılabilir bir HTML üretir: yönetici özeti (deterministik cümleler), SLO / SLA karşısında KPI kutuları, dakikalık erişilebilirlik ve hata bütçesi grafiği (satır içi SVG), servise / sunucuya göre hata dağılımı, servis gecikmeleri, en sık hata şablonları, mevcut analizdeki öne çıkan incident'lar (kök neden + önerilen aksiyon). Tarayıcıda Yazdır → PDF. Dönemler: 15 dk / 1 sa / 4 sa / 24 sa canlı tampondan; **son 7 gün** (saatlik) ve **son 30 gün** (günlük) `history.py` rollup'larından. "Tüm yapı" kutusu sayfa filtresini yok sayar (tüm ortam, sunucu ve platformlar). Rapor ayrıca hataların **kaynağa** (ajan / platform) dağılımını içerir.

**Servis seviyesi geçmişi** (`src/watchover/history.py`): canlı akıştaki her olay `LiveStore.on_ingest` kancasıyla dakika × ortam × sunucu × gönderici rollup'ına (toplam, hata, 9 kovalı gecikme histogramı) ve saat × ortam × servis tablosuna toplanır; arka plan iş parçacığı 30 sn'de bir upsert eder, 400 gün saklar. Böylece haftalık / aylık raporlar bellek tamponundan bağımsızdır ve yeniden başlatmada kaybolmaz.

**Canlı öğrenme** (`src/watchover/autolearn.py`, LLM sayfası › Kalite): her N dakikada (varsayılan 15, ayarlardan `learn_min`) deterministik motor canlı akışın son 60 dakikasını analiz eder ve incident kalıplarını bilgi tabanına yazar (kök neden başına bir ders, tekrarlar sayacı artırır). "Şimdi öğren" elle çalıştırır, çalışma geçmişi tabloda. Modelin ağırlıkları değişmez; Watchover'a sor, kural önerileri ve LLM bağlamı bu hafızaya dayanır. **Eğitim seti (JSONL)**: dersler, notlar, onaylı kurallar ve 👍 alan yanıtlardan sohbet biçiminde ince ayar dosyası (`training_export`), kendi modelini eğitmek isteyen ekipler için.

### Envanter: CMDB-lite ve logların envanterle eşleşmesi

Sol menüdeki **Envanter** sayfası (`src/watchover/inventory.py`) kurumun sunucu listesini tutar: sunucu adı, IP, takma adlar (FQDN, eski ad), ortam, veri merkezi, kabin, VLAN / ağ, alt ağ, işletim sistemi, rol, uygulama, küme, sahip ekip, kritiklik (critical / high / normal / low), depolama (SAN / NAS / LUN), üretici / model, seri no, izleyen (ajan / kaynak), durum (active / maintenance / decommissioned), etiketler, notlar. Üç doldurma yolu: form, **CSV içe aktarma** (herhangi bir CMDB dışa aktarımı; sütun adları Türkçe / İngilizce takma adlarla eşlenir, var olan sunucular güncellenir; şablon indirilebilir) ve **Keşfedilenler** sekmesi (canlı akışta veya ajan kaydında görülen ama envanterde olmayan sunucular tek tıkla eklenir). **Eşleşme:** gelen her olay `LiveStore.enricher` kancasıyla sunucu adı, kısa ad, takma ad veya IP üzerinden envantere çözülür; IP olan host sunucu adına dönüşür (IP `attributes["ip"]`'de kalır), eksik ortam doldurulur, `inv_dc / inv_rack / inv_vlan / inv_owner / inv_criticality / inv_role / inv_application / inv_cluster` ve `site` olayla taşınır. Motor (`Analysis(..., inventory=…)`) veri setinde envanter tablosu yoksa ürün envanterini kullanır: fırtına kümeleme (dc / kabin) ve kritiklik puanı buradan beslenir; canlı öğrenme de aynı envanterle analiz eder. Devre dışı (decommissioned) sunucular eşleşmeden çıkar. Sayfa çipleri: sunucu sayısı, kritik sayısı, DC sayısı, canlı akıştaki sunucuların ne kadarının eşleştiği.

**Liquid glass tema** (`GLASS` CSS katmanı, `.streamlit/config.toml`): zemin koyu slate üzerine yumuşak turkuaz / mavi ışık lekeleri (sabit), tüm yüzeyler yarı saydam katmanlı cam (`backdrop-filter: blur + saturate`, 1 px yarı saydam kenar, üstte ince ışık çizgisi, yumuşak gölge); kenar çubuğu, kartlar, KPI kutuları, formlar, genişleticiler, diyaloglar, girişler, düğmeler, sekmeler, tablolar ve kaydırma çubukları aynı dili paylaşır. Kontrast uzun nöbetler için korunur (metin `#e3e9f2`, yüzey opaklığı %4–8), `prefers-reduced-transparency` isteyen sistemlerde bulanıklık kapanıp düz yüzeye döner. README sayfası wordmark'ı base64 olarak gömer (Streamlit göreli resim yollarını sunmaz).

**Telefon ve tablet uyumu** (`GLASS` içindeki medya sorguları): 1180 px altında (iPad) satırlar sarılır, hiçbir sütun üçte birden dar olmaz, kenar çubuğu 250 px'e iner, sarılan sütun (Kapsam paneli) kartların altına tam genişlikte geçer; 760 px altında (iPhone / Android) kartlar ikişer sıralanır, iki sütunlu satırlar alt alta gelir, başlıklar küçülür, sekmeler yatay kaydırılır, diyaloglar ekran genişliğinde açılır; dokunmatik cihazlarda düğmeler 42 px yükseklikte ve giriş alanları 16 px (iOS odaklanınca yakınlaştırmaz). Kenar çubuğu küçük ekranda kapalı başlar (`initial_sidebar_state="auto"`), sol üstteki `»` ile açılır. Tarayıcıda "Ana ekrana ekle" ile uygulama simgesi olarak kullanılabilir; yerel push bildirimi yoktur (yol haritasındaki bildirim adımı).

**Bilgi düğmeleri:** sayfalardaki açıklama paragrafları artık ℹ️ düğmesinin (popover) arkasındadır; ekranlar sade kalır, basınca açıklama açılır.

### Simülasyon modu, Sistem sayfası, kullanıcı girişi ve SSO

- **Simülasyon modu** artık yalnız Operasyon sayfasındaki bir anahtardır ve varsayılan olarak kapalıdır. Açılınca sentetik sunucular canlı akışı besler (demo); kapatılınca simülatör durur ve ürettiği her şey tampondan silinir (`LiveStore.purge`). Gerçek kurulumda canlı sayfa yalnız ajan ve kaynak verisini gösterir.
- **Sistem** sayfası (`src/watchover/admin.py`, sol menüde 🛠): KPI kutuları ve servis kartları ile sürüm bilgileri (git, motor, Python, Streamlit, çalışma süresi, launcher), arka plan servislerinin durumu (alıcı, kaynak sorgulayıcı, rollup yazımı, canlı öğrenme, simülatör, envanter eşleşmesi), veritabanı tabloları ve boyutu, uygulama günlüğü; **Bakım** (her işlem kart üzerinde ne yaptığını açıklar: analiz sonuçlarını yeniden hesapla, canlı tamponu boşalt, oturumu sıfırla, veritabanını sıkıştır); **Güncelleme** (sürüm zip'ini uygulama ya da `git pull`, sonra launcher üzerinden yeniden başlatma; `data/`, `.env`, veritabanları ve `.venv` korunur); **Kullanıcılar** (avatar, rol ve durum rozetleri, rol değiştirme, devre dışı, silme, ekleme); **Giriş** (sağlayıcı kartları); **Güvenlik günlüğü**.
- **Giriş** (`src/watchover/auth.py`): sağlayıcılar birlikte kullanılabilir. **Yerel hesaplar** (e-posta + parola), **Google** (Google / Google Workspace hesabı, Google Cloud Console'da OAuth istemcisi) ve **kurumsal SSO** (herhangi bir OpenID Connect sağlayıcısı: Entra ID, Okta, Keycloak, Auth0). Google ve OIDC Streamlit'in yerel `st.login()` akışıyla çalışır (`Authlib`); Sistem › Giriş sekmesi `.streamlit/secrets.toml` içine `[auth]`, `[auth.google]`, `[auth.oidc]` bloklarını 0600 izniyle yazar, yeniden başlatma etkinleştirir. Giriş paneli kenar çubuğundaki **Hesap** kartından açılır (diyalog); giriş yapan kullanıcı adı, e-postası ve rolüyle orada görünür, aynı yerden çıkış yapılır. Giriş açıkken sayfalar kilit ekranı gösterir; Sistem sayfası yalnız yöneticilere açıktır. **Parola güvenliği:** parola hiçbir zaman saklanmaz, yalnız scrypt karması (N=2^15, r=8, p=2, 32 baytlık anahtar, hesap başına 16 baytlık rastgele tuz; parametreler karma dizisinde tutulur ve eski karmalar bir sonraki başarılı girişte yükseltilir), sabit zamanlı doğrulama (bilinmeyen hesapta da aynı süre), politika (en az 10 karakter, harf + rakam), 15 dakikada 5 başarısız denemede 15 dakika kilit, her deneme `auth_events` güvenlik günlüğünde (Sistem › Güvenlik günlüğü). İlk yerel hesap yönetici olur, son yönetici düşürülemez / silinemez. Sınır: yerel oturum tarayıcı sekmesi süresince tutulur; kalıcı oturum için Google / SSO.

### İnternetsiz kurulum: tek dosyalık paket (v1.31)

- `./watchover.sh bundle` internetli makinede imajları (dashboard, trainer, PostgreSQL, Caddy, Ollama), indirilmiş Ollama modellerini, eğitim taban ağırlıklarını, kaynak kodu tek `.tar`a toplar; `./watchover.sh unbundle <dosya>` kurumsal sunucuda imajları ve model hacimlerini yükler, `install` build yapmadan başlatır. İzlenen sunucular Watchover'a 8600 (edge ile 443) üzerinden bağlanır; ajan Watchover'dan indirilir, internet gerekmez. Rehberde "İnternetsiz sunucu" bölümü.

### Yük testi: 2.000 olay/sn ERP karışımı ve alım optimizasyonu (v1.30)

- 100 sanal ajanla ölçüldü (4 vCPU, PostgreSQL): önce 1.200 olay/sn'de tavan, paket başına 85 ms; tespit önbelleği ve tarama iyileştirmeleriyle 2.000 satır/sn'de paket başına 7 ms, POST p50 10 ms, sıfır hata. 4.000 satır/sn'de alım sürüyor, dakikalık tarama 12,6 s ve tek süreçte alımı saniyeler boyunca duraklatıyor: Faz 2'deki ayrı worker'ın gerekçesi. Tablo ve yorum `docs/LOAD_TEST.md`.

### Faz 1 ölçek: 100+ sunucu için canlı pencere veritabanında (v1.29)

- Halka tampon (50.000 olay) 1.000 olay/sn'de yalnız saniyeleri tutar; artık her ajan paketi veritabanına yazılır ve anomali taraması, canlı analiz ve öğrenici pencereyi gerektiğinde oradan okur. Yeniden başlatma sonrası pencere kaybolmaz. Saklama süreleri (canlı olay saati, rollup günü, halka boyutu, spool) Sistem › Ayarlar'dan değiştirilir ve anında uygulanır.
- Sistem › Kapasite paneli: olay/sn, halka doluluğu ve kapsadığı süre, pencere kaynağı (bellek / veritabanı), veritabanı penceresinin boyutu, spool ve disk; 100+ sunucuda ilk zorlanacak yerler uyarı olarak işaretlenir.
- Yedek rotasyonu (`backup --keep N`), gece cron örneği, soğuk yedek sunucu ve LLM'i ayrı kutuya alma adımları kurulum rehberinde.

### Düzeltme: kaynak sınırları makineye göre (v1.28.1)

- CPU ve bellek sınırları artık sabit değil: `watchover.sh` her install/update/start'ta Docker'ın gördüğü CPU sayısını ve belleği ölçer; LLM ve eğitim servisine tüm CPU'ları (10 çekirdekli dizüstünde 10, 32'lik sunucuda 32, 64'lükte 64) ve belleğin büyük kısmını tavan olarak yazar, dashboard/Postgres için de makineyi aşmayan değerler seçer. "range of CPUs is from 0.01 to 10.00" hatası böylece kalkar. Kendi değerlerin için `.env` içinde `WATCHOVER_SIZING=manual`.

### Gözetimsiz öğrenme: eğitim için çaba gerekmez (v1.28)

- `./watchover.sh llm on` artık bir `trainer` servisi de kaldırır: canlı akıştan, anomalilerden ve kapatılan aksiyonlardan biriken hafıza eşiği (varsayılan 200 örnek, son modelden beri en az 50 yeni, 24 saatte bir) geçince CPU'da LoRA eğitir, adayı kalite kapısından (kök neden benchmark'ı, taban modele karşı) geçirir ve geçerse `watchover-ops` olarak devreye alır. Geçmezse önceki model kalır. Her şey LLM › Kalite › "Kendi modelin" kartında: ilerleme çubuğu, eşikler, otomatik/kullan anahtarları, "Şimdi eğit", koşu geçmişi.
- Sohbet için seçtiğin LLM bağlantısı hiç değişmez; `watchover-ops` yalnız incident incelemesi ve Watchover'a sor'da, "Geçen modeli kullan" açıkken kullanılır.

### Düzeltme: LLM komutları ek, belge dili (v1.27.1)

- `./watchover.sh llm on/import/off` mevcut sohbet modellerine ve LLM bağlantına dokunmaz: `on` yalnız hiç bağlantı tanımlı değilse gömülü Ollama'yı seçer, `import` eğitilen modeli mevcutların yanına ekler (seçmez), seçim `./watchover.sh llm use <model>` ya da LLM › Bağlantı ile yapılır, `off` yalnız `on`'un kendi yazdığı ayarı geri alır.
- Sayfa ilk yüklendiğinde belge dili arayüz diliyle eşitlenir; İngilizce etiketler artık Türkçe büyük harf kuralına takılmaz (İNSTALLED → INSTALLED).

### Kendi modelin: eğitim verisi ve CPU LoRA (v1.27)

- Hafızadaki her şey (kök neden desenleri, anomaliler, çözümler, notlar, runbook'lar, onaylı kurallar, beğenilen yanıtlar) sohbet biçiminde eğitim verisi olur: LLM › Kalite › "Kendi modelin" kartından indirilir ya da `./watchover.sh llm export` ile alınır (all / train / eval).
- `scripts/train_lora.py` GPU'suz sunucuda Qwen 2.5 1.5B/3B üzerine LoRA eğitir; `./watchover.sh llm import models/watchover-ops` adaptörü Ollama'ya `watchover-ops` olarak mevcut modellerin yanına ekler (hiçbir şeyi değiştirmez; denemek için `./watchover.sh llm use watchover-ops` ya da LLM › Bağlantı); `--gate` kalite kapısı yeni modeli kök neden benchmark'ında taban modele karşı sınar, düşüşte FAIL verir. `pip install -e ".[train]"` yalnız eğitim makinesinde gerekir.

### CPU'da yerel LLM: Ollama compose profili (v1.26)

- GPU gerektirmeyen kurumsal sunucu için `llm` profili eklendi: `./watchover.sh llm on` Ollama'yı compose ağında ayağa kaldırır (tanımlı bir LLM bağlantın varsa ona dokunmaz, gömülü Ollama ek seçenektir) ve Qwen 2.5 7B (sohbet/inceleme), 3B (hızlı) ve bge-m3 (gömme) modellerini indirir. Modeller kalıcı hacimde tutulur, cache temizliği silmez.
- Kaynak sınırları makineye göre otomatik (v1.28.1: Docker'ın gördüğü tüm CPU'lar ve belleğin %80'i tavan; `WATCHOVER_SIZING=manual` ile `WATCHOVER_LLM_CPUS`, `WATCHOVER_LLM_MEMORY`, `WATCHOVER_LLM_THREADS` elle). `llm pull <model>` / `llm list` / `llm off` (off modelleri korur). Motor LLM'siz de deterministik çalışmaya devam eder.

### Güvenlik sertleştirmesi ve zafiyet taraması (v1.25)

- **Zafiyet taraması** yeni menü: envanter sunucularının yazılım sürümleri küratörlü güvenlik danışma kataloğuyla (41 kayıt) eşleştirilir, yapılandırma zafiyetleri (EOL OS, izlemesiz kritik sunucu, sahipsiz varlık) kontrol edilir; isteğe bağlı, varsayılan kapalı bir ağ erişilebilirlik kontrolü açık portları pasif tespit eder. Bulgular önem sırasına göre listelenir, tek tıkla sunucu detayı açılır.
- **Kapsamlı red team düzeltmeleri:** SPA statik sunucusunda yol aşımı (path traversal) kapatıldı; dış istek çekmede SSRF koruması (yalnız http/https, metadata/loopback/özel-IP engeli; yönetici tanımlı iç kaynaklar hariç); `file://` yerel dosya okuma engellendi; ilk yönetici parolası değiştirilene kadar API'nin tüm uçları 403 döner; bildirim güncellemesinde SQL enjeksiyonu (sütun beyaz listesi); grok ve arama regex'lerinde ReDoS koruması; arşiv yüklemede sıkıştırma bombası sınırları; OIDC `next` açık yönlendirme kapatıldı; ITSM yeniden yapılandırmada kimlik bilgisi sızıntısı engellendi (`act.itsm`); yetki yükseltme kapatıldı (yönetici hesaplarına sadece yönetici dokunur, kimse sahip olmadığı izni veremez); MCP sunucusu anahtarsızken yalnız loopback'e bağlanır ve dosya erişimi `WATCHOVER_DATASETS` ile sınırlanır; güvenlik başlıkları (CSP, X-Frame-Options, nosniff) eklendi. Docker imajında pip/setuptools/wheel yükseltildi ve `apt-get upgrade` eklendi (tarama bulguları). Commit edilmiş `.api.key` ve `secrets.toml` git izleminden çıkarıldı ve gitignore'a alındı.

### Kurumsal hafıza (v1.24)

- Bilgi tabanı artık "Hafıza": canlı akış, veri setleri, anomaliler ve kapatılan aksiyonlar kendiliğinden öğrenilir; gömme modeli olmadan da anlamsal arama çalışır (yerel hash gömme), bge-m3 tanımlanınca "Yeniden indeksle" ile arka planda geçilir. LLM incelemesi ve sohbet geçmiş olaylara atıf yapar.
- Menü sadeleştirme: Bağlantılar sayfasından çoklanan Kaynaklar ve Envanter sekmeleri kaldırıldı (kendi menüleri var); Parser desenleri LLM sayfasının içine sekme olarak taşındı (`/parser` oraya yönlendirir).

### Parser v2 · adım 4: Drain ve öğrenen eşleme (v1.23)

- Sistem artık kendi kendine öğreniyor: canlı akıştaki ve yüklenen her veri setindeki satırlar Drain ile kümelenir (`<*>` değişkenli şablonlar), kümeler kalıcıdır; Parser desenleri › Öğrenilen şablonlar'da sayılarıyla listelenir, tek tıkla grok deseni önerilir ve kaydedilir.
- Bir kez yapılan sütun eşlemesi aynı biçimdeki (aynı sütun adlı) sonraki dosyalara kendiliğinden uygulanır; Öğrenilen eşlemeler sekmesinde roller düzeltilir, düzeltme sonraki yüklemelerde geçerlidir.

### Sunucu eşik uyarıları (v1.22)

- Anomali takipçisi taban çizgisi sapmalarına ek olarak sabit eşikleri de izler: bir sunucunun son CPU / bellek / disk / GPU değeri eşiği aşınca (varsayılan 85 / 90 / 90 / 95 %, Sistem › Ayarlar'dan değişir) hemen `metric` türünde anomali açılır; başlık sunucu, ortam, metrik ve değeri taşır. Aynı hatta girdiği için zilde, Anomaliler sayfasında, tarayıcı bildiriminde ve e-posta / SMS kurallarında görünür; değer eşiğin altına inince "düzeldi" işaretlenir, operatör kapatır.

### Watchover'a sor sohbet balonu (v1.21)

- Watchover'a sor menüden çıktı; her sayfanın sağ altında sohbet balonu (`ChatWidget`): soru-cevap, veri seti / canlı akış seçimi, LLM durumu, 👍 / 👎, geçmiş oturumda saklanır ve tam sayfa görünümle (`/assist`, ⤢) paylaşılır.

### Sade menü, açılır kapanır Sistem, bildirim zili (v1.20)

- Kenar çubuğu sadeleşti: Bildirimler ve README artık Sistem sayfasının sekmeleri (`/system?tab=notify|readme`); Sistem'i açamayan ama bildirim ya da README yetkisi olan roller için eski menü öğeleri durur.
- Sistem › Durum: her kart açılır kapanır (`Collapsible`, durumu tarayıcıda hatırlanır), iki sütunlu ızgara üstten hizalı (kısa kart komşusunu uzatmaz); Bakım, SSO ve sürüm geçmişi varsayılan kapalı.
- Üst çubukta 🔔 zil: açık anomaliler 15 sn'de bir çekilir, görülmemişler rozet sayısı ve sekme başlığında `(n)` olarak görünür; panel son 15 anomaliyi listeler, "Hepsini gördüm" işaretler; "Tarayıcı bildirimi aç" ile yeni anomaliler işletim sistemi bildirimi olarak da düşer (sayfa başka sekmedeyken bile).
- Docker'daki "9 biçim": korpusun `.log` dosyaları `.gitignore` yüzünden depoya girmemişti; artık izleniyor, kapsama kartı 52 biçimi gösterir.

### Daha sıkı yazı ölçeği (v1.19.1)

- Gövde 13 px, başlıklar 17 / 14 px, KPI değerleri 22 px, tablo 12.5 px, düğme ve sekmeler 13 px; kart ve modal dolguları daraltıldı; incident listesi satırları 13 / 11.5 px. Tasarım çizgisi aynı, ekrana daha çok bilgi sığıyor.

### LLM incelemesi (v1.19)

- Incident kartının üstünde "🤖 LLM yorumu": kurulu Ollama modeli ya da API, kartın kanıtını okuyup özet, kök neden değerlendirmesi, etki, aksiyon ve açık soruları referanslarla yazar. Veri seti sayfasında "LLM incelemesi" sekmesi tüm seti ya da aranan log satırlarını inceletir; ek soru sorulabilir, cevaplar oturumda listelenir ve kopyalanır. Watchover'a sor sayfası soru-cevap için durmaya devam eder.

### Parser v2 · adım 3: grok kütüphanesi (v1.18)

- Grok motoru (Logstash sözdizimi) ve 27 hazır satır paketi; kullanıcı desenleri Parser desenleri sayfasından yazılır, test edilir, kaydedilir ve dosya algılamada hazır paketlerden önce denenir. "Ayrıştırılamayan satırlar" sekmesi bir veri setinde zaman / seviye çıkarılamayan satırları gruplar ve teste taşır. Korpus 52 dosya (+ Squid, BIND, iptables, Nagios, ESXi, Tomcat, RabbitMQ, dockerd, sudo / cron); puan 79.7 (52 dosya; yeni dosyalar sıkı seviye ölçütüyle girdi), gürültü hunisi değişmedi (demo 70.5×, alarm fırtınası isabet %97.3).

### Envanter sayfası, incident kartı dayanıklılığı, İngilizce rozetler (v1.17.1)

- Envanter menüye ayrı sayfa olarak döndü (Streamlit'teki tüm alanlar ve sekmeler). Incident kartı: uç nokta yan panellerde (aksiyon, playbook) hata olsa da kartı döner, NaN / sonsuz sayılar null olur (`_json_safe`), kök neden sinyali bulunamazsa başlıktan üretilir; arayüz yükleme yerine hatayı gösterir. Rozetler `lang="en"`: Türkçe sayfada CSS büyük harf dönüşümü "CRİTİCAL" üretmez. Hata haritası "Kök neden" kartı sayı gösterir, liste kısaltılmış alt satırda (üzerine gelince tamamı); KPI değerleri taşmaz.

### Parser v2 · adım 2: yeni biçimler, çok satırlı olaylar, ERP aileleri (v1.17)

- Kapsama puanı 44.6 → **80.8** (zaman %99.6, seviye %83, sunucu %44, servis %96.6); gürültü hunisi değişmedi (demo 70.5×, alarm fırtınası isabet %97.3). Korpus 43 dosya: ERP için Oracle EBS (concurrent manager ve FND), Dynamics AX AOS, Dynamics 365 batch, Dynamics NAV / Business Central eklendi; SAP aileleri zaten vardı.
- Yeni modüller: erişim logları (Apache / nginx / ALB), CEF / LEEF, Windows Event XML, IIS W3C, OpenTelemetry OTLP, CloudTrail; journald, GELF, ECS, MongoDB, Docker json-file alan adları; metin ve syslog parser'ları yeniden yazıldı (yukarıdaki tablo).
- Seviye ölçütü sıkılaştı: sadece bir alandan ya da satırdaki seviye sözcüğünden gelen önem "bilinir" sayılır, tahmin (ORA-, Traceback → ERROR; timeout, denied → WARN) sayılmaz; `_sev_src` özniteliği kaynağı taşır. Otomatik eşleme aynı role uyan sütunlardan dolu olanı seçer.

### Parser v2 · adım 1: korpus ve kapsama raporu (v1.16)

- Amaç "her logu anlayan" parser; ilk adım ölçmek. `samples/corpus/` altında 38 log ailesinden birer dosya, `python -m watchover.coverage` her birini gerçek pipeline'dan geçirip zaman / seviye / sunucu / servis çıkarım oranını ve puanı verir. İlk ölçüm: genel puan 44.6 (zaman %58.6, seviye %91.4, sunucu %21.1, servis %23.2); syslog, CSV, logfmt, GELF, HAProxy, Postfix tam puan; Apache / nginx erişim, ALB, Cisco ASA, LEEF, CEF, RFC 5424, IIS, Redis ve çok satırlı stack trace'ler sıfıra yakın. Bu tablo sonraki adımların (standart biçim modülleri, çok satırlı birleştirme, grok kütüphanesi, Drain) yol haritası.
- Gürültü oranı da raporda: örnek setlerde ham olay → parmak izi → sinyal → incident hunisi, azaltma katsayısı (demo 70.5×, alarm fırtınası 5.3×, SAP 1.1×), elenen olay yüzdesi ve alarm fırtınasında gerçek listeye göre gürültü isabeti (%97.3). Azaltma %5'ten fazla düşer, isabet geriler ya da incident sayısı üç katına çıkarsa test kırmızı olur.
- `baseline.json` kabul edilen puan; `tests/test_coverage.py` bir dosyanın puanı düşerse kırmızı olur, iyileşme `--update-baseline` ile kaydedilir. Sistem sayfasında "Parser kapsaması" kartı (genel oranlar, biçim tablosu, temel çizgiye göre Δ).

### Sanal ad, kayıt ol, Apple kaldırıldı (v1.15)

- `./watchover.sh edge on watchover.local`: Mac adı Bonjour ile duyurur, sertifika ad + IP'ye kesilir; meslektaşlar `https://watchover.local` yazar.
- Giriş sayfasında **Kayıt ol** geri geldi (varsayılan açık; eski `config.json`'lar `version` 2 göçüyle açılır). Kayıt olan hesap görüntüleyici rolüyle hemen açılır ve giriş yapar (`/auth/register` `token` döner); yetkiyi yönetici artırır. SMTP varsa önce e-posta doğrulaması (`/auth/options` `verify`). Beklemedeki hesap giriş denerse 403 `pending_approval`; parola kuralı ve alan hataları Türkçe gösterilir.
- Operasyon metrik kutuları eşit yükseklikte (`.grid.tiles`). Sistem › Veritabanı tablo listesi süzgeçli, "sadece dolu" seçenekli ve daraltılmış (ilk 8 satır, satır sayısına göre sıralı; `DbTables`).
- Apple ile giriş kaldırıldı; Google, Microsoft (Entra ID) ve OIDC kaldı.

### Şirket ağına açılış: `edge` profili (v1.13)

- `./watchover.sh edge on <ad-veya-IP>`: Caddy konteyneri 443/80'de, TLS kendi CA'sından (kök sertifika `./watchover.sh edge cert` ile meslektaşlara dağıtılır) ya da `./certs/` altındaki şirket sertifikasından. Arayüz + API ve `/ingest` tek adres altında; düz 8501 edge açıkken yayınlanmaz (Mac'teki Streamlit launcher 8501'de kalabilir). macOS Docker Desktop adımları (sabit IP / DNS, güvenlik duvarı, uyku, sertifika dağıtımı, ajan `--ca`) rehberin 8. bölümünde.

### Streamlit'te olup React'te eksik kalan özellikler geri getirildi, tema seçimi (v1.11)

- Operasyon: KPI kartları ve metrik kutuları tıklanabilir; her biri kartların altında kapanabilir bir ayrıntı paneli açar (hız, ERROR+, erişilebilirlik / SLA / hata bütçesi / ihlal dakikası, p95, CPU / bellek / disk / GPU sunucu bazında, log dosyaları ve kuyruk). Ortam ve sunucu süzgeci çip olarak, ortam özet çipleri, biletler ve log dosyaları kartları. Mevcut düzen korunur; panel kapatılınca sayfa eski haline döner.
- Veri setleri: örnek veri setleri düğmeleri, uzak kaynaktan çekme (HTTP / MCP, `POST /api/datasets/fetch`), sütun eşleme JSON'u (`mapping`), karşılaştırma penceresi; incident penceresinde flashcard bloğu, öneriden aksiyon oluşturma, özel aksiyon, benzer dersler, geri bildirim formu (`/feedback`), LLM istemi / açıklama (`/prompt`, `/explain`), postmortem, haritada göster; gürültü sekmesinde ısı haritası ve düşürülen gruplar; bu setin aksiyonları sekmesi.
- Bilgi tabanı: belge yükleme (`POST /api/knowledge/docs/upload`) ve LLM ile kural önerisi. Sistem: bakım kartı (canlı akışı temizle, anlık görüntü budama, zip ile dağıtım, git güncelleme). Yeni README sayfası (`/readme`).
- Veri kaynakları: Streamlit'teki Kaynaklar sayfası React'te ayrı menü öğesi olarak geri geldi (katalog, test, gelişmiş alanlar). Veri setlerinde çekme türleri görünür düğmeler: HTTP / JSON, MCP sunucusu, Canlı akış (tampon → veri seti), Kayıtlı kaynak (Elasticsearch / Loki / Splunk / Graylog'dan pencere çek). Açık tema yumuşatıldı: saf beyaz yerine kırık beyaz yüzeyler, kurşuni metin.
- Tema: TR / EN'in yanında 🌙 koyu · 🖥 sistem · ☀️ açık düğmeleri; seçim tarayıcıda saklanır, sistem seçeneği işletim sistemi tercihini canlı izler. Açık tema aynı renk token'larını yeniden tanımlar, bileşenler değişmez.
- API: `live` uçlarında virgülle ayrılmış çoklu sunucu süzgeci, `/live/discoveries`; `datasets` uçları `samples`, `fetch`, `mcp-tools`, `actions`, `feedback`, `related`, `prompt`, `explain`, gürültü `heat` + `demoted`; `system` uçları `snapshots/prune`, `deploy`, `git-update`, `readme`. Test `test_restored_features_api`.

### Faz 3: kalan sayfalar taşındı, Streamlit kapatıldı

- Yeni sayfalar: Hata haritası (`/api/map`, motorun etkileşimli SVG'si iframe içinde; veri seti ya da canlı akış, incident / ortam / sunucu süzgeci), Watchover'a sor (`/api/assist/ask`, kaynaklar ve bağlam, 👍 / 👎, bilgi tabanına kaydetme; LLM yoksa deterministik özet), LLM (`/api/llm`: sağlayıcı / adres / model / anahtar, bağlantı testi, model listesi; Ollama sekmesi: kurulu / bellekteki modeller, önerilenler, indirme ilerlemesi, silme; Kalite: çağrı günlüğü, kıyas koşusu), ITSM (`/api/itsm`: demo / ServiceNow / Jira / OneDesk / generic, ticket'lar canlı sinyal, metrik aşımı ve incident'larla puanlanır). Var olan sayfalara eklenenler: veri setinde log arama (`alan:terim`, `-terim`, tırnak, regex) ve iki veri setini karşılaştırma; Operasyon'da log dosyaları kartı ve satır kuyruğu, SLO raporu indirme (15 dk … 30 gün); Bildirimler'de SMTP / SMS kanal ayarları ve test gönderimi; Sistem'de ayarlar (kendini izleme, uyarılar, öğrenme aralığı, alıcı portu / anahtarı, dış adres, kurum adı, dil), güncelleme komutları, yeniden başlatma, anlık görüntüye geri dönüş; Bağlantılar'da keşfedilen sunucular; girişte e-posta doğrulamalı kayıt (`/api/auth/register` + `/verify`, SMTP ve `auth_self_register` ile).
- Dağıtım: `dashboard` konteyneri artık `python -m watchover.api --port 8501` çalıştırır, canlı alıcı (8600) ve kendini izleyen ajan aynı süreçte; sağlık denetimi `/api/health`. Streamlit `legacy` profiline alındı (`./watchover.sh legacy on`, 8502), `app.py` ve testleri depoda duruyor, bir sonraki büyük sürümde silinecek.

### Yeni arayüz (React): Streamlit'ten çıkışın 2. fazı

- `web/` altında React 18 + TypeScript + Vite; bağımlılıklar react-router, TanStack Query, recharts, lucide ikonları. Tasarım Streamlit arayüzünün devamı (koyu lacivert, cam yüzeyler, turkuaz vurgu, çip sekmeler, daraltılabilir kenar çubuğu) ama gerçek yönlendirme (her sayfanın URL'si), anlık veri yenileme (5 sn), telefon / tablet düzeni ve yetkiye göre menü ile. On iki sayfa hazır (yukarıdaki tablo). Derlenmiş paket API konteyneri tarafından `:8000` altında sunulur; `./watchover.sh api on` yeterlidir. SSO akışı API'ye taşındı (OIDC, PKCE; Apple form_post ve ES256 istemci sırrı). Giriş sayfasında Google / Microsoft düğmeleri her zaman görünür, yapılandırılana kadar pasiftir (Apple girişi v1.15'te kaldırıldı: geliştirici üyeliği ve genel alan adı gerektiriyordu); Sistem › *Giriş sağlayıcıları (SSO)* kartı (`GET/PUT /api/system/auth`, sırlar maskeli) istemci bilgilerini, dış adresi, izinli e-posta alanlarını ve otomatik hesap açmayı yönetir; geri dönüş adresleri kartta gösterilir. Faz 3'te kalan sayfalar da taşındı (yukarıda).

### HTTP API: Streamlit'ten çıkışın 1. fazı

- Karar: motor kalır, arayüz FastAPI + React'e geçer. Faz 1 bu sürümde: `src/watchover/api/` motorun tamamını JSON uçları olarak açar (giriş ve e-posta MFA, RBAC, veri seti yükleme işleri, incident / sinyal / kanıt / postmortem, canlı akış, anomaliler ve adımları, aksiyonlar, playbook, ajanlar, kaynaklar, envanter, bilgi tabanı, bildirimler, kullanıcı / roller, sistem). Docker'da `./watchover.sh api on` ile `:8000` (`/api/docs`), yerelde `pip install -e ".[api]"` ve `python -m watchover.api`. Streamlit arayüzü aynen çalışmaya devam eder. Faz 2 React sayfalarını bu uçların üstüne kurar, faz 3 Streamlit'i kapatır. Bilinen sınır: canlı tampon süreç belleğindedir; dashboard ve API ayrı süreçler olduğundan API'nin canlı uçları ancak alıcı API'ye taşındığında (`WATCHOVER_API_RECEIVER=1`) dolar.

### Anomali takibi (Operasyon sayfası)

- Operasyon sayfasının altında **Anomali takibi**: KPI satırı (açık, üzerinde çalışılan, bugün çözülen, tarama sayısı), durum filtresi, anomali başına kart (tür çipi, ortam, gözlenen / normal / skor / tekrar, ilk-son görülme, sorumlu, bağlı aksiyon, *normale döndü* rozeti) ve genişleyince dakikalık geçmiş grafiği, **operasyonel adım listesi** (kutucuklar; ilk kutu anomaliyi üstlenir), sorumlu / not alanları, Üstlen / Çöz / Yok say / Yeniden aç / Aksiyon oluştur düğmeleri. Eşik yoktur: her sunucu kendi normaline göre yargılanır (`anomaly.py`). Yetki `act.anomaly` (admin ve operatör); izleyici yalnız görür. Sistem › Bildirimler'de yeni koşul *Yeni anomali açıldı*.

### PostgreSQL: tek ürün veritabanı

- v1.5'ten itibaren her şey tek bir PostgreSQL veritabanında: kullanıcılar, roller, bilgi tabanı ve kurallar, aksiyonlar, playbook, ajanlar, kaynaklar, envanter, bildirim alıcıları / kuralları / uyarı geçmişi, SLO rollup'ları, öğrenme koşuları. `db.py` ortak katmandır; `ActionStore` ve `Playbook` ayrı `.db` dosyaları yerine aynı bağlantıyı kullanır (MCP sunucusu dahil). Docker'da compose PostgreSQL 16 konteynerini yanına kurar; dizüstünde `DATABASE_URL` boşsa gömülü SQLite dosyaları çalışmaya devam eder ve `python -m watchover.migrate` ile taşınır (uygulama PostgreSQL'e ilk bağlanışında `/data` yanındaki dosyaları kendisi bir kez kopyalar). Sürüm anlık görüntüleri PostgreSQL'de tabloların JSON dökümüdür (`db.json.gz`), geri dönüş tabloları o dökümle doldurur; VACUUM her iki motorda çalışır; Sistem › Durum veritabanı motorunu ve bağlantıyı (parolasız) gösterir. Gerçek PostgreSQL'de bulunup düzeltilen lehçe farkları: `ON CONFLICT ... DO UPDATE` içinde nitelenmemiş sütun (`slo_minute.total`), `"user"` gibi ayrılmış sözcük içeren sütunların döküm / kopyada tırnaklanması, `LIKE '%…'` yanında `%` kaçışı, `Decimal` / `memoryview` dönüşleri. Testler `WATCHOVER_TEST_DATABASE_URL` verildiğinde bütün depoları ve uygulamayı gerçek PostgreSQL üzerinde koşturur (`tests/test_postgres.py`; GitHub Actions bir `postgres:16` servisiyle koşar).

### Docker ile çalıştırma ve merkezi yayın

- Kurumsal kurulum tek sunucuda ve tamamen Docker'da: paketi (zip) sunucuya kopyala, `./watchover.sh install`; imaj orada kaynak koddan derlenir, GitHub ya da imaj kaydına bağımlılık yoktur (Actions yayını yalnızca isteğe bağlı bir kolaylıktır). Kapalı ağ için `save` / `load`. İki konteyner: dashboard (alıcı ve kendini izleyen ajan dahil) ve PostgreSQL; `--profile mcp` ile üçüncüsü kendi MCP sunucumuzdur (API anahtarı, `./datasets` klasörü, sağlık denetimi). `docker compose up -d` ile kurulum, `docker compose pull && docker compose up -d` (ya da `make docker-update`) ile güncelleme; kod imajın içindedir, veri `watchover-data` biriminde kalır. Sürüm sabitleme ve geri dönüş `.env` içindeki `WATCHOVER_TAG` ile. GitHub Actions her `main` gönderiminde ve her `vX.Y` etiketinde imajı `ghcr.io/bannercheck/watchover`'a yayınlar (önce testler). Uygulama Docker'da olduğunu bilir: Sistem › Güncelleme zip / git yerine compose komutlarını gösterir, başlık çipi imaj etiketini taşır, yeniden başlatma konteynerin yeniden başlatma politikasına bırakılır. Adım adım rehber: `docs/KURULUM_DOCKER.md` (PDF olarak da iletilir).

### Sürümleme ve sürüm notları

- Numaralandırma: büyük sürüm **v1, v2** (ürün sahibinin kararı), küçük güncelleme **v1.1, v1.2** (her devreye alınan adım), düzeltme **v1.1.1**. `python -m watchover.release minor "özet"` sürümü artırır, `CHANGELOG.md`'ye tarih, sürüm ve madde satırlarını ekler; `--commit` commit'i ve `vX.Y` git etiketini oluşturur, `--push` gönderir. Sistem › Güncelleme sayfası sürüm notlarını listeler (geçerli sürüm işaretli), başlık çipi `Watchover v1.0` biçimindedir, anlık görüntüler sürüm numarasını taşır. `watchover version` terminalden geçerli sürümü ve son notları yazar.

### Sekmeler: çip görünümü

- Uygulamadaki her sekme şeridi (Sistem, Kullanıcılar, Bildirimler, Veri setleri, LLM, Envanter…) durum çipleriyle aynı dildedir: nokta işaretli cam haplar, hepsi aynı yükseklikte (36 px), etikete göre yana genişler, seçili sekme teal → mavi degrade; şerit sığmazsa yatay kayar, dar ekranda da satır kırmaz. Streamlit'in react-aria sekmeleri `data-testid="stTab"` ile hedeflenir, alt çizgi göstergesi gizlenir.

### Kenar çubuğu: cam gezinti ve daraltma

- Sol menü keskin liquid glass paneldir; her sayfa girdisi cam bir karttır (seçili sayfa teal → mavi degrade ve sol şeritle vurgulanır). Üstteki « tutamacı menüyü sola gizler, izleme alanı tüm genişliği kullanır; » tutamacı geri açar. Slogan ve sürüm damgası kenar çubuğundan kaldırıldı; sürüm, motor, git ve senaryo kimlikleri Sistem sayfasının başlık çiplerinde durur.

### Duyarlı tasarım (telefon, tablet, dizüstü, geniş ekran)

- Yerleşim ekran genişliğine göre kendini ayarlar: geniş ekranda tam ızgara; 1180 px altında (tablet) kart satırları sarılır ve üçte bir sütunlar kullanılır, 1000 px altında iki sütunlu ayar satırları (kanal kartları, kişi / grup formları) alt alta gelir; 760 px altında (telefon) kartlar ikişer, iki ve üç sütunlu satırlar ile başlık satırları tam genişlikte alt alta dizilir, sekme şeritleri yatay kayar, kenar çubuğu ⟩ düğmesiyle açılır. Giriş ekranı 900 px altında tek sütuna iner. Dokunmatik cihazlarda tıklama alanları büyütülür; şeffaflık azaltma tercihi cam efektini kapatır.

### Roller ve yetkiler, kayıtta e-posta doğrulaması, bu makinenin izlenmesi

- **Kullanıcılar sayfası** (sol menüde 👥): hesap satırları birer formdur; ad, rol, durum (etkin / devre dışı / beklemede) ve isteğe bağlı yeni parola **Kaydet** ile tek seferde yazılır, **Sil** onay ister. Üstte hesap, yönetici, doğrulama bekleyen ve kilitli sayıları; sekmeler Hesaplar, Roller ve yetkiler, Hesap ekle.
- **Roller ve yetkiler** (`src/watchover/rbac.py`, Kullanıcılar › Roller ve yetkiler): yetki kataloğu üç grupta toplanır. Sayfalar (gezinti menüsünde ne görünür), işlemler (simülasyon anahtarı, sunucu bağlama, SLO raporu indirme, log dışa aktarma, kaynak ve envanter düzenleme) ve sistem sekmeleri (Durum, Bakım, Güncelleme, Giriş, Bildirimler, Güvenlik günlüğü); Kullanıcılar sayfası `page.users` yetkisiyle açılır. **Yönetici** her yetkiye sahiptir ve daraltılamaz; **Operatör** sistem yönetimi dışındaki her şeyi yapar; **İzleyici** yalnız izler. Onay kutusu matrisiyle roller düzenlenir, varsayılana döndürülür, özel roller (ör. `dba_lead`) eklenir ve kullanıcı listesindeki açılır kutudan atanır. Uygulama gezintiyi izinli sayfalarla sınırlar, izinsiz düğmeleri pasifleştirir ve Sistem sayfasında yalnız izinli sekmeleri gösterir.
- **Kayıtta e-posta doğrulaması:** kendi kendine kayıt açıkken yeni hesap `pending` durumunda oluşturulur ve kayıtlı adrese 6 haneli kod gider; kod doğrulanınca hesap etkinleşir ve oturum açılır. Doğrulanmamış hesap giriş yapamaz; yarım kalan kayıt aynı adresle tekrar denendiğinde yeni kod gönderilir. SMTP tanımlı değilse kayıt kapalıdır (yönetici Kullanıcılar sayfasından hesap açar). İlk hesap (kurulumun ilk anı) doğrulamasız yönetici olur.
- **Bu makinenin izlenmesi** (`src/watchover/selfmon.py`): uygulama açılınca paketle gelen `agent.py`, kurulu makinede `--auto --metrics` (Linux'ta `--journal`) ile yerel alıcıya bağlanır: her 15 saniyede CPU, bellek, disk (varsa GPU), keşfedilen uygulama logları ve sistem günlüğü. Ajan kayıt defterine makine adıyla kaydedilir, token'ı şifreli saklanır; `data/self-agent.pid` sayesinde yeniden başlatmada eski ajan değiştirilir, ikinci bir kopya oluşmaz. Sistem › Durum'daki **Bu makineyi izle** anahtarı kapatıp açar (`self_monitor`); `WATCHOVER_SELFMON=0` ortam değişkeni tamamen kapatır (testler). Operasyon sayfası ilk dakikadan itibaren gerçek veriyle dolar.

### Giriş kapısı, yerleşik yönetici, sırların şifrelenmesi, sürüm geçmişi

- **Giriş kapısı:** kimse giriş yapmadan uygulamanın hiçbir bölümü görünmez. Giriş sayfası ortada tek cam karttır: e-posta, parola, **Beni hatırla** onay kutusu, gradyan **Devam et** düğmesi, "veya" ayracı ve altında tam genişlik sağlayıcı düğmeleri; kartın üstünde marka rozeti, altında gürültü → sinyal → incident → aksiyon şeridi. **Beni hatırla:** girişte rastgele bir belirteç üretilir, yalnız sha256 karması `remember_tokens` tablosuna yazılır ve tarayıcıya `wo_remember` çerezi (30 gün, SameSite=Lax, HTTPS'te Secure) verilir; sonraki açılışlarda çerez sessizce oturum açar, çıkış yapmak belirteci iptal edip çerezi siler, parola değişince kullanıcının tüm hatırlanan oturumları biter. **E-posta ile MFA:** parola ya da kimlik sağlayıcı doğrulamasından sonra yönetici olmayan her hesaba, kayıtlı e-posta adresine (SSO hesaplarında Google / Microsoft / Apple adresi) uyarı SMTP kanalı üzerinden 6 haneli tek kullanımlık kod gönderilir (`otp_codes`: yalnız karma, 5 dakika, 5 deneme, tek kullanım); giriş kartı kod adımına geçer (yeniden gönder / vazgeç), hatırlanan oturumlar yeniden sormaz, yöneticiler muaftır, SMTP tanımlı değilse giriş MFA'sız sürer ve güvenlik günlüğüne not düşülür; Sistem › Giriş › Yerel hesaplar kartındaki anahtarla açılıp kapatılır (`mfa_email`, varsayılan açık). Giriş ekranında yalnız marka rozeti ve kart bulunur. Google, Microsoft ve Apple düğmeleri logolarıyla her zaman görünür; yapılandırılmamış olanlar pasiftir ve ipucu Sistem › Giriş sekmesini gösterir. Sağlayıcılar: **Google**, **Microsoft 365 / Exchange** (Microsoft Entra ID; tenant, uygulama kimliği, istemci sırrı), **Apple** (Sign in with Apple; Services ID, Team ID, Key ID ve .p8 anahtarından ES256 imzalı istemci sırrı üretilir, `openid` kapsamı) ve genel **OpenID Connect**; hepsi Sistem › Giriş sekmesinden açılır ve `.streamlit/secrets.toml` içine `[auth.<sağlayıcı>]` blokları olarak yazılır. Kimlik sağlayıcının doğruladığı e-posta ile hesap eşleştirilir (`_sso_provider` issuer'dan sağlayıcıyı seçer). Tam sayfa giriş ekranı (liquid glass; sol tarafta logo, gürültü → sinyal → incident → aksiyon akışı ve dört özellik kartı, sağda cam giriş kartı) kenar çubuğunu da gizler. `WATCHOVER_SKIP_SETUP=1` yalnız test ve geliştirme içindir.
- **Yerleşik yönetici:** ilk açılışta hesap yoksa `admin@watchover.local` hesabı sabit bir başlangıç parolasıyla oluşturulur (parola `auth.py` içindeki `INITIAL_PASSWORD` sabitidir; giriş ekranında ve belgelerde gösterilmez, kuruluma ayrıca iletilir). İlk girişte uygulama açılmadan yeni parola istenir (`must_change`); hiç giriş yapılmamış bir yönetici her açılışta başlangıç parolasına geri anahtarlanır, değiştirilmiş parolaya dokunulmaz. Kendi kendine kayıt varsayılan olarak kapalıdır; hesapları yönetici Kullanıcılar sayfasından açar. Terminalden `watchover user add siz@sirket.com --admin` kendi adresinizi yönetici yapar; `promote`, `password`, `unlock` komutları kilitli veya parolası unutulmuş hesapları kurtarır. Parolalar hiçbir zaman düz metin tutulmaz, yalnız scrypt karması (N=2^15, r=8, p=2, rastgele tuz) saklanır.
- **Sırların saklanması** (`src/watchover/vault.py`): SMTP / SMS / LLM / OIDC / Google / alıcı anahtarı gibi sırlar `data/config.json` içine ve çekme kaynağı kimlik bilgileri `sources` tablosuna **şifreli** yazılır (Fernet: AES-128-CBC + HMAC-SHA256). Anahtar `data/.vault.key` dosyasında 0600 izniyle durur; yedeklerde bu dosya olmadan sırlar açılamaz. Sistem › Güvenlik günlüğü sekmesi durumu gösterir.
- **Sürüm geçmişi** (Sistem › Güncelleme › Sürüm geçmişi): her zip / git güncellemesinden önce ve VACUUM'dan önce otomatik anlık görüntü alınır (`data/versions/<id>/`: `code.zip` kod ağacı, `knowledge.db` / `actions.db` / `playbook.db` kopyaları, `meta.json` ile sürüm, git, motor, neden). **Şimdi anlık görüntü al** elle alır; **Geri dön** onaydan sonra önce mevcut durumu saklar, seçilen kodu açar ve veritabanlarını geri kopyalar. Her dağıtımın (zip, git, geri dönüş) ardından `admin.deploy_finish` önceki sürümün önbelleklerini temizler (`clear_caches`: kod ağacındaki `__pycache__` ve `*.pyc`, `.pytest_cache`, Streamlit disk önbelleği; `CACHE_PROTECTED` listesindeki sanal ortam, `data/`, `models/`, `.ollama`, `.cache` ve veritabanı dosyalarına hiçbir zaman dokunulmaz, indirilmiş LLM modelleri kalır) ve `st.cache_data / cache_resource` boşaltılır, uygulama otomatik olarak sert yeniden başlatılır (`watchover restart --hard`: durdur, önbellekleri sil, temiz başlat; launcher yoksa süreç çıkar ve servis yöneticisi yeniden açar). Son 12 görüntü tutulur. PostgreSQL'e geçişte veritabanı kısmı pg_dump ile yapılacaktır (sonraki adım).

### E-posta ve SMS uyarıları, kenar çubuğunda hesap kartı

- **Hesap kartı** kenar çubuğunda her zaman görünür: giriş yapan kullanıcının adı, e-postası ve rolü (yönetici / operatör / izleyici) ile çıkış düğmesi; kimse giriş yapmamışsa "Giriş yapılmadı" ve **Giriş yap** düğmesi; hiçbir sağlayıcı açık değilse "Giriş kapalı · tek operatör · yerel" kartı ve yine **Giriş yap** düğmesi (diyalog, sağlayıcıların Sistem › Giriş sekmesinden açıldığını söyler ve mevcut yerel hesabın giriş yapmasına izin verir).
- **Bildirimler** (`src/watchover/notify.py`, Sistem › Bildirimler sekmesi): dört alt sekme. **Kanallar:** SMTP (host, port, STARTTLS / SSL / düz, kullanıcı, parola, gönderen) ve SMS ağ geçidi (hazır sağlayıcılar: **Netgsm, İleti Merkezi, Verimor, Mutlucell, JetSMS (Biotekno), Posta Güvercini, Twilio**; **Turkcell Kurumsal Mesaj, Türk Telekom Kurumsal SMS, Vodafone Kurumsal Mesaj** için sözleşmeyle verilen müşteriye özel uç nokta URL'si ve gövde şablonu girilir; genel HTTP şablonu: URL, yöntem, kimlik doğrulama, Content-Type, gövde. Form her sağlayıcının kendi alanlarını gösterir: kullanıcı / parola / mesaj başlığı, Twilio için Account SID + token. Alıcı numaraları sağlayıcının istediği biçime çevrilir: `+905…` / `905…` / `05…`; Netgsm, İleti Merkezi ve Mutlucell'in HTTP 200 ile döndürdüğü hata kodları da hata sayılır) ile her ikisi için **Test gönder**. **Alıcılar:** kişiler (ad, e-posta, telefon, grup etiketleri) ve gruplar (ad, isteğe bağlı dağıtım listesi adresi, not). **Kurallar:** koşul (erişilebilirlik SLO altında, dakikadaki ERROR+ eşiği, CPU / bellek / disk / GPU sınırı, ajan N dakika sessiz, çekme kaynağı hata veriyor, canlı öğrenmeden yeni incident deseni), eşik (0 = varsayılan), ortam, seviye, kanallar, hedefler (`ops, @dba, ayse@sirket.com, +905551112233` biçiminde grup adı / kişi adı / adres / numara karışık), bekleme süresi; **Şimdi değerlendir** düğmesi. **Teslimat günlüğü:** her gönderim ve hata (zaman, seviye, başlık, kanal, alıcılar, sonuç, ayrıntı).
- Motor `AlertEngine` uygulamayla birlikte başlar ve her dakika kuralları canlı akışa karşı değerlendirir; aynı koşul (ör. `slo:prod`, `metric:db-01:cpu`) bekleme süresi dolmadan tekrar gönderilmez, başarısız denemeler de bekleme süresine sayılır (tekrar fırtınası olmaz). Üst anahtar (`alerts_on`) motoru kapatır. Sırlar (SMTP parolası, API token) `data/config.json` içinde 0600 izniyle durur; Durum sekmesinde 🔔 servis kartı motorun çalışma sayısını ve son kontrolü gösterir.

### Sunucu bağlama, otomatik log keşfi, Log dosyaları kartı

- **Sunucu bağla** düğmesi Operasyon sayfasının başlığındadır: Ajanlar panelini bir pencerede açar (kayıt, tek satır komut, filo anahtarı, canlı durum). Kurumsal kullanımda sunucu ekleme yeri burasıdır.
- **Otomatik log keşfi** (`agent.py --auto`, kurucu varsayılanı `--logs auto`): ajan sunucudaki uygulamaları profil tablosuyla bulur ve log dosyalarını izler: SAP NetWeaver ABAP (dev_w*, dev_disp, dev_rfc*, dev_ms, dev_enq*, available.log, sapstart.log), SAP NetWeaver Java (defaultTrace, applications.log, dev_server*, *.jvm, std_server*.out), SAP HANA (indexserver / nameserver / xsengine / alert trace), SAP Host Agent, Oracle Database (alert_*.log, listener.log; `$ORACLE_BASE` dahil), Oracle E-Business Suite (`$INST_TOP`, conc/log, opmn, oacore, fs1 / fs2), WebLogic, Tomcat, JBoss / WildFly, WebSphere, nginx, Apache httpd, PostgreSQL, MySQL / MariaDB, MongoDB, Redis, Kafka, Elasticsearch, Docker konteyner logları, Kubernetes düğümü (`/var/log/containers`, kubelet), Linux sistem logları, macOS, Windows IIS, Windows'ta SAP ve Oracle, `C:\ProgramData\*\logs`. Profil başına en yeni 40 dosya, toplam 200 dosya sınırı. `--discover` yalnız listeyi basar. Windows'ta Olay Günlüğü (`wevtutil`, System + Application, uyarı ve üstü) akıtılır; syslog dosyası olmayan Linux'ta systemd günlüğü (`journalctl -f -o json`) akıtılır. Ajan başlarken keşfettiği uygulamaları ve dosya listesini bir keşif olayıyla bildirir.
- **Log dosyaları kartı** (altyapı kartlarının beşincisi): her olay okunduğu dosyanın adını taşıdığı için kart pencerede ERROR / CRITICAL üreten dosya sayısını tüm dosyalara karşı gösterir. Ayrıntı: sunucu / ajan / dosya / olay / hata / servis / son hata tablosu, ajanların keşfettiği uygulamalar, bir dosyayı seçince tampondaki satırlar (yalnız hatalar seçeneği, ERROR kırmızı, WARN sarı) ve **dışa aktarma** (`.log` indirme). Kubernetes: ajan her düğüme kurulur ya da kümeye Loki / Elasticsearch kaynağı bağlanır.

### Etkin model seçimi

`model_switcher()`: Ollama'nın indirdiği **tüm** modeller listelenir (boyutlarıyla); LLM sayfası › Modeller sekmesindeki "Hangi model yanıtlasın" kutusundan ve "Watchover'a sor" başlığının yanından etkin sohbet modeli ve embedding modeli seçilir. Seçim `llm_model` / `llm_embed` ayarlarına kaydedilir ve analiz açıklamaları, sohbet ve kural önerileri hemen o modelle çalışır; satırlardaki "Kullan" düğmesi de kalıcıdır.

### Tek satır kurulum, ilk açılış sihirbazı, ajan kimliği

> Adım adım devreye alma (dashboard, alıcı, sunuculara ajan, yeni bir sistemi dahil etme, güncelleme, sorun giderme): **[docs/KURULUM_REHBERI.md](docs/KURULUM_REHBERI.md)**.

- **macOS / Linux, sudo'suz:** `curl -fsSL https://raw.githubusercontent.com/Bannercheck/Operation_Monitoring_tool/main/scripts/install.sh | bash -s -- --with-ollama`. Betik Python'ı bulur (yoksa macOS'ta Homebrew ile kurar), uygulamayı `~/Library/Application Support/Watchover` (macOS) ya da `~/.local/share/watchover` (Linux) altına klonlar, venv kurar, istenirse Ollama'yı ve modelleri indirir, `watchover` komutunu (`start|stop|status|open|update|logs|agent`) bağlar, macOS'ta LaunchAgent / Linux'ta kullanıcı systemd servisi ile girişte başlatır ve tarayıcıyı açar. Downloads/Desktop altındaki gizlilik korumasına takılmamak için uygulama kullanıcı kütüphanesine kurulur. Sunucu kurulumu için `scripts/install_linux.sh` (root, `/opt/watchover`, sistem servisi) ayrı durur.
- **İlk açılış sihirbazı:** ayar dosyası (`WATCHOVER_HOME/config.json`) yoksa uygulama dört adımlı sihirbazı gösterir: çalışma alanı (dil, ad, alıcı portu, simülatör) → yerel model (Ollama keşfi, sohbet / embedding modeli seçimi, eksikleri ilerleme çubuğuyla indirme ya da harici uç) → ajanlar (ilk toplayıcıların kaydı) → bitir (özet, demo veri seti). Ayarlar diske yazılır (0600) ve yeniden başlatmada korunur; LLM ve Canlı alım sekmelerindeki "Ayarları kaydet" de aynı dosyaya yazar. Bağlantı ayarlarından sihirbaz yeniden çalıştırılabilir; testler ve otomasyon için `WATCHOVER_SKIP_SETUP=1`.
- **Ajan kimliği** (`agents.py`): her toplayıcı kaydedilir (ad, ortam, site, etiketler) ve bir kez gösterilen kendi token'ını alır (`wo_…`, yalnız sha256'sı saklanır). Alıcı token'ı kayda çözer ve her olayı **kayıtlı** ad / ortam / site ile damgalar; ajanın gönderdiği X-Agent başlığı kimliği değiştiremez, yanlış yapılandırılmış bir ajan başka sunucu adına veri basamaz. Son görülme, IP ve olay sayısı izlenir; token iptal edilir, yenilenir, ajan silinir. Bilinmeyen ya da iptal edilmiş `wo_` token'ı ortak anahtara düşmez, 401 alır. Eski ortak anahtar `legacy` olarak çalışmaya devam eder. Ajan tarafında `--token` (`WATCHOVER_TOKEN`).

**Güncelleme (kaldır-yükle gerekmez).** Kod klasörü git ile klonlandıysa `watchover update`; zip olarak indirildiyse `watchover update ~/Downloads/watchover.zip`. Her iki yol da yalnız kodu değiştirir, bağımlılıkları kurar ve uygulamayı yeniden başlatır; `data/` altındaki ayarlar, ajan token'ları, bilgi tabanı ve `.env` korunur.

### Sunucuya ajan kurulumu: ne gerekir, ne gerekmez

Sorunun cevabı: **IP/port gerekir, Prometheus gerekmez.** Sunucular veriyi Watchover'ın çalıştığı makinedeki alıcıya (varsayılan TCP 8600) gönderir; alıcı adresi sihirbazın 3. adımında ve Bağlantı ayarları › Canlı alım › Ajanlar panelinde seçilir (LAN IP'si, makine adı ya da DNS adı; `local_addresses()` adayları listeler, seçim `public_host` olarak kalıcıdır) ve her kurulum komutunun içine yazılır. Bu makinede gelen TCP 8600 açık olmalıdır.

Sunucuda gereken: python3 (3.9+), o adrese ağ erişimi, servis için sudo, sunucunun kendi token'ı. Gerekmeyen: Prometheus, Zabbix, Docker, pip, git. Akış:

1. Ajanlar panelinde sunucuyu kaydet (ad, ortam, site, izlenecek log dosyaları) → tek seferlik token ve **tek satır komut** çıkar.
2. Sunucuda komutu çalıştır: `curl -fsSL http://<alıcı>:8600/agent/install.sh | sudo bash -s -- --url http://<alıcı>:8600/ingest --token wo_… --logs "/var/log/syslog,/var/log/app/*.log"`. Alıcı `agent.py` ve `agent-install.sh` dosyalarını kendisi sunar (`GET /agent.py`, `GET /agent/install.sh`); sunucunun GitHub'a erişmesi gerekmez. Kurucu python3'ü doğrular (root ise kurar), `/opt/watchover-agent` altına ajanı indirir, `agent.env` (0600) yazar, **bağlantı testini** yapar (`agent.py --test`: adres/port/token'ı kanıtlayan bir merhaba olayı), systemd (Linux) / launchd (macOS) servisini kurar; `--uninstall` ile kaldırır, `--no-service` ile yalnız dosyaları kurar.
3. Panelde satır ilk pakette yeşile döner (5 sn'de bir yenilenir): çevrimiçi / 2 dk'dır sessiz / ilk paket bekleniyor / iptal.

Ajan (`agent.py`, yalnız standart kütüphane, `--metrics` + tekrarlanabilir `--tail` glob'ları aynı süreçte): N saniyede bir CPU / bellek / disk / GPU, log dosyalarını `tail -f` gibi izler (`tail -F` gibi: yeniden adlandırılan ve yerinde sıfırlanan dosyayı yakalar, yarım satırı tamamlanana dek bekler, yeni eşleşen dosyaları 30 sn'de bir alır; olmayan yol beklenir), ulaşamadığında `--spool` dizinine (0700) biriktirip alıcı dönünce tek iş parçacığıyla sırayla gönderir (401/403 ve kalıcı 4xx asla biriktirilmez), `--test` ile kendini doğrular. Varsayılan log listesi `/var/log/syslog,/var/log/messages` (Debian ve RHEL aileleri). Prometheus / Zabbix'ten veri **okunmaz**; genel bir HTTP/JSON ucu veya MCP aracı varsa Bağlantı ayarları › Veri kaynağı'ndan alarm listesi çekilebilir.

**Toplu kurulum: sunucular kendini kaydeder.** Sunucu başına token kopyalamak yerine Ajanlar panelindeki **filo kayıt anahtarı** (`wk_…`) kullanılır: her sunucuda aynı komut çalışır (`… | sudo bash -s -- --url … --enroll-key wk_… --env prod`), kurucu `POST /enroll` ile sunucuyu makine adıyla kaydettirir, alıcı o sunucuya özel `wo_…` token'ı üretir ve kurucu bunu `agent.env` içine yazar. Anahtar yalnız kayıt yapar, veri gönderemez (`/ingest` onu reddeder); aynı ad ikinci kez kaydolamaz (409), anahtar panelden yenilenir ya da kapatılır. Ansible / Terraform / cloud-init gibi araçlarla dağıtım için tasarlandı.

**Güvenlik sınırları.** Alıcı düz HTTP'dir: güvenilir LAN/VPN içinde tutun ya da önüne TLS reverse proxy koyun (arayüz ve sihirbaz bunu uyarı olarak gösterir). İlk ajan kaydedildiği andan itibaren alıcı token'sız gönderimi reddeder (açık mod yalnız hiç ajan yokken). Gövde sınırı 16 MB (413), bozuk `Content-Length` 400, bağlantı zaman aşımı 30 sn, ortak anahtar sabit zamanlı karşılaştırılır. Dashboard arayüzü `install.sh` ile yalnız `127.0.0.1`'e bağlanır; dışarı açmak için `data/.env` içine `WATCHOVER_UI_ADDRESS=0.0.0.0` yazın (önüne TLS/SSO koyun). `data/live/events.jsonl` 64 MB'ta döndürülür, 3 nesil tutulur.

### LLM: her sağlayıcı, yerel çalıştırma, kalite ölçümü

- **Sağlayıcıdan bağımsız istemci** (`llm.py`, yalnız stdlib): OpenAI uyumlu sunucular (Ollama `/v1`, vLLM, LM Studio, LocalAI, OpenLLM, OpenAI, Groq, Mistral, Together, Azure), **Ollama yerel API** (`/api/chat`, `/api/embeddings`), **Anthropic** Messages, **Google Gemini** generateContent. `provider=auto` URL'den tahmin eder. Her çağrı süresi ve sonucuyla bilgi tabanındaki `llm_calls` tablosuna yazılır.
- **Yerel model** (`ollama.py`): olağan portlarda Ollama'yı bulur, ilk açılışta bağlantıyı kendisi doldurur; LLM › Modeller sekmesi kurulu ve bellekteki modelleri gösterir, önerilen modelleri ilerleme çubuğuyla indirir (`/api/pull` akışı), siler. Öneri: sohbet `qwen2.5:7b-instruct` (Türkçe), `llama3.1:8b`, `gemma2:9b`; yalnız CPU'da `qwen2.5:3b-instruct`; embedding `bge-m3`.
- **Linux sunucuya Docker'sız kurulum**: `sudo bash scripts/install_linux.sh --with-ollama` → sistem paketleri, `/opt/watchover` altında venv, isteğe bağlı Ollama (resmi kurulum betiği) + modeller, `.env` doldurma, `watchover.service` systemd birimi (:8501, `journalctl -u watchover -f`). Seçenekler: `--dir --user --port --models "…" --no-service`.
- **Kalite sekmesi**: başarı oranı (hatasız çağrı), gecikme p50 / p95, **kaynak doğruluğu** (cevaptaki `[INC-3]` / `[L12]` / `[dosya:satır]` atıflarının modele gönderilen bağlamda var olma payı; geçersiz atıf sayısı halüsinasyon ölçerine en yakın şey), atıf oranı, **kök neden testi** (yüklü veri setinin her incident'ı için kanıt + karıştırılmış aday sinyaller; modelin seçimi deterministik motorla karşılaştırılır, uyum oranı raporlanır; `llm_eval.py`), cevap onayı (sohbette 👍/👎), kural kabulü (modelin önerdiği kurallardan onaylananlar), tür bazında çağrı sayıları, model bazında tablo, son çağrıların gecikme grafiği.
- **Operasyon kartları canlı**: her kart artık son 15 dakikanın satır içi grafiğini (SVG sparkline, eşik / hedef çizgisi, nabız noktası) taşır ve 2 saniyede bir yenilenir; ayrıntı yine modal pencerede.

### Performans: büyük dosyalar

- Ayrıştırma ve analiz sonucu değiştirmeyen önbelleklerle hızlandırıldı: aynı zaman damgası dizesi, aynı önem derecesi değeri, aynı host / servis çifti ve aynı mesaj bir kez çözülür, parmak izi (şablon, önem, servis) anahtarı başına bir kez hashlenir. 700 bin satırlık 46 MB log: ayrıştırma 4,8 s, analiz 3,1 s, profil 2,2 s (önce toplam 22 s). Sonuç kimlikleri (`sonuç …` damgası) değişmez; testler bunu üç örnek veri setiyle doğrular. Tarayıcıdan aktarım hızı ağa bağlıdır; yükleme arka planda sürdüğü için beklemek gerekmez.
- **SAP izleri:** NetWeaver Java (ListFormatter) kayıtlarında satırın ilk karakterine göre dağıtım ve yığın izi devam satırları için regex'siz hızlı yol; 300 bin kayıtlık 87 MB `defaultTrace` 33,8 s → 8,3 s. Biçim algılama ve satır sondajı dosyanın yalnız ilk 400 KB'ını okur.
- **Paralel ayrıştırma:** ZIP / TAR / çoklu yüklemedeki dosyalar 8 ve üzeri çekirdekli makinelerde (3+ dosya, 32 MB üstü) ayrı süreçlerde ayrıştırılır; sonuç sırası ve kimlikleri değişmez. Ayrıştırılmış olayların işçi süreçten geri aktarımı ayrıştırmanın kendisi kadar sürdüğünden az çekirdekte kazanç yoktur ve kapalı kalır; `WATCHOVER_PARALLEL=1` zorlar, `=0` kapatır.
- **Canlı ekran maliyeti:** Operasyon paneli her 2 saniyede canlı tamponu sorgular; sorgular yalnız pencere içindeki yeni ucu tarar (50 bin olayın tamamını değil) ve aynı tik içinde bir kez hesaplanır (`LiveStore.cached`). Sürüm damgaları ve git bilgisi önbellektedir.

### Tekrarlanabilirlik: iki makine farklı sonuç gösteriyorsa

Motor deterministiktir: saat, yerel saat dilimi, hash sırası, dosya sırası, `\` / `/` yol ayracı, CRLF ve BOM sonucu değiştirmez (`tests/test_determinism.py` bunları üç ayrı süreçte ve farklı `PYTHONHASHSEED` ile doğrular). Aynı **girdi** kimliği + aynı **motor** kimliği her makinede aynı **sonuç** kimliğini vermek zorundadır. İki makine farklı incident sayısı gösteriyorsa sırayla:

1. **Motor kimliği farklı mı?** Kenar çubuğundaki `motor xxxxxxxxxx · git xxxxxxx`. Farklıysa makinelerden biri eski kod çalıştırıyor: `git pull` yapılmamış, eski zip açılmış ya da paket `pip install .` ile (`-e` olmadan) kurulup sonra kaynak güncellenmiş. Uygulama repo `src/` kopyasını her zaman öne alır ve eski kurulum saptanırsa uyarı basar; komut satırı için `pip install -e .` şart.
2. **Girdi kimliği farklı mı?** Veri seti satırındaki `girdi xxxxxxxxxx`. Farklıysa aynı dosyalar yüklenmemiştir: yan tablo (bağımlılık / envanter) eksik → yoğunluk modu yerine parmak izi modu; alarms.csv yalnız → tekilleştirme yok; farklı sürüm paket. Dosya raporundaki `sha` sütunu hangi dosyanın farklı olduğunu gösterir.
3. İkisi de aynı ve sonuç farklıysa bu bir motor hatasıdır: `watchover <paket> --json` çıktılarını karşılaştırıp issue açın.; filo kayıt anahtarı (`enroll_key` / `rotate` / `disable`, `self_enroll` → `/enroll`) |
