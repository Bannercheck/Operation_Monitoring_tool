[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "watchover"
version = "1.27.1"
classifiers = ["Programming Language :: Python :: 3", "Programming Language :: Python :: 3.9", "Programming Language :: Python :: 3.10", "Programming Language :: Python :: 3.11", "Programming Language :: Python :: 3.12", "Programming Language :: Python :: 3.13"]
description = "Universal SRE ingestion + correlation engine: noise -> signals -> explained incidents -> tracked actions"
readme = "README.md"
requires-python = ">=3.9"
dependencies = ["streamlit>=1.42", "pandas>=1.5", "python-dateutil>=2.8", "Authlib>=1.6.4", "cryptography>=42", "psycopg[binary]>=3.1"]

[project.optional-dependencies]
dev = ["pytest>=8"]
mcp = ["mcp>=2"]
postgres = ["psycopg[binary]>=3.1"]
api = ["fastapi>=0.110", "uvicorn>=0.23", "python-multipart>=0.0.18", "httpx>=0.27"]
train = ["torch>=2.2", "transformers>=4.44", "peft>=0.12"]

[project.scripts]
watchover = "watchover.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
