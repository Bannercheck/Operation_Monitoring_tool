# Watchover - çalışma zamanı bağımlılıkları (Python 3.9+)
# Kurulum: pip install -r requirements.txt  ve ardından  pip install -e .
streamlit>=1.35
pandas>=1.5
python-dateutil>=2.8
psycopg[binary]>=3.1                  # PostgreSQL sürücüsü (DATABASE_URL); yoksa gömülü SQLite kullanılır

# Geliştirme / test
pytest>=8

# İsteğe bağlı: MCP sunucusu (python mcp_server.py) için. Python 3.10+ ister.
# mcp>=2
