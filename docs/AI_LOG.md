# AI Kullanım Kaydı

Sadece geliştirme adımları. Format: araç + sürüm · tarih · yapılan iş.

| Araç + sürüm | Tarih | Yapılan düzeltme / geliştirme |
|---|---|---|
| Claude Fable 5.1 | 2026-09-13 | Proje iskeleti: app.py, src/watchover paketi, docs, Makefile, pyproject |
| Claude Fable 5.1 | 2026-09-13 | Format detector: dosya / ZIP / GZ / TAR.GZ / klasör açma, json / jsonl / csv-tsv / syslog / kv / text tespiti |
| Claude Fable 5.1 | 2026-09-13 | Parser'lar (6 format), zaman damgası ve severity normalizasyonu, sütun → rol auto-mapper |
| Claude Fable 5.1 | 2026-09-13 | Analiz motoru: fingerprint, burst tespiti, korelasyon, kök neden, skor + faktör gerekçesi, postmortem, LLM prompt paketi |
| Claude Fable 5.1 | 2026-09-13 | Dataset profiler ve SQLite aksiyon takibi |
| Claude Fable 5.1 | 2026-09-13 | Streamlit dashboard: Özet, Sinyaller, Incident'lar, Aksiyonlar |
| Claude Fable 5.1 | 2026-09-13 | Sentetik demo veri seti üreticisi ve 11 test (pytest + Streamlit AppTest) |
| Claude Fable 5.1 | 2026-09-13 | Python 3.9+ desteği, 3.9 ve 3.11 üzerinde test |
| Claude Fable 5.1 | 2026-09-13 | Yükleme sınırı 1 GB, koyu tema (.streamlit/config.toml) |
| Claude Fable 5.1 | 2026-09-13 | Arayüz yeniden tasarımı: huni kartları, incident pencereli aktivite grafiği, WHY THIS SIGNAL paneli, yayılım Gantt'ı, faktör grafiği, tek tıkla aksiyon, kanban |
| Claude Fable 5.1 | 2026-09-13 | TR/EN dil anahtarı; anlatı, gerekçe ve öneriler iki dilde (i18n.py) |
| Claude Fable 5.1 | 2026-09-13 | Performans: hızlı zaman damgası yolları, tek birleşik maske regex'i, örneklemli varlık çıkarımı (300k satır 45 sn → 15 sn) |
| Claude Fable 5.1 | 2026-09-13 | Özet sayfası: metin yerine grafikler (zamana göre seviye, servisler, kaynak türleri) ve oynatılabilir canlı log akışı |
| Claude Fable 5.1 | 2026-09-13 | Açılış sayfası sadeleştirildi: yükleme alanı, demo butonu, ikonlu pipeline şeridi |
| Claude Fable 5.1 | 2026-09-13 | Sol menüde Parser paneli: dosya başına format, güven, satır, roller; sütun → rol eşleme düzeltme formu |
| Claude Fable 5.1 | 2026-09-13 | Streamlit 1.50 uyumluluğu: `wide()` yardımcısı ile width / use_container_width seçimi |
| Claude Fable 5.1 | 2026-09-13 | Gerçek veri şekilleri: iç içe JSON kayıt listesi, Türkçe başlık ipuçları, kv satır öneki, zaman damgası olmayan satırların 1970'e düşmesi düzeltildi, otomatik zaman kovası; sade sol menü, yükleme anında ilerleme, parser tablosu Özet'te |
| Claude Fable 5.1 | 2026-09-14 | Tıklanabilir metrik kartları ve detay panelleri; sol menüde Bağlantı paneli (HTTP API + MCP istemcisi); motor MCP sunucusu (`mcp_server.py`, streamable HTTP / stdio); Dockerfile + docker-compose; 4 bağlayıcı testi |
| Claude Fable 5.1 | 2026-09-14 | Metrik kartları yeniden tasarlandı: ikon, vurgu rengi, gradyan, bağlam satırı, karta yapışık Detay şeridi |
| Claude Fable 5.1 | 2026-09-14 | Canlı alım: HTTP alıcı (API anahtarlı `POST /ingest`), halka tampon + JSONL spool, yerleşik simülatör, `agent.py` (tail / file / simulate); açılış sayfası ve Canlı sekmesinde 2 sn'de bir yenilenen canlı grafikler; sol menü Connection Settings (veri kaynağı, canlı alım, LLM) / Datasets / README; OpenAI uyumlu yerel LLM istemcisi ve incident'ta LLM ile açıkla; README kullanım kılavuzu; 4 yeni test |
| Claude Fable 5.1 | 2026-09-14 | Açılış operasyon merkezi: demo butonu kaldırıldı; SLO/SLA kartları (erişilebilirlik, p95, hata bütçesi), host bazlı CPU/bellek/disk metrikleri (alıcı + simülatör + `agent.py --metrics`), ITSM entegrasyonu (ServiceNow, Jira SM, OneDesk, genel REST, demo) ve ticket ↔ sinyal/metrik/incident ilişkilendirme; auto-mapper sayısal sütunu mesaj seçmiyor; 3 yeni test |
| Claude Fable 5.1 | 2026-09-14 | Sol menü sayfa gezinmesi (Operasyon, Datasets, ITSM, Connection Settings, README); ana sayfa yükleme alanı olmadan önce metriklerle açılıyor; GPU metriği (simülatör, ajan nvidia-smi, eşik); log analizi Datasets sayfasında; ITSM yapılandırma + tam tablo + ticket detayı kendi sayfasında |
| Claude Fable 5.1 | 2026-09-14 | Çoklu veri seti kaydı: her set ayrı tutulur, aktif set dropdown ile seçilir, kaldırılabilir; `compare.py` ile iki setin karşılaştırma bölümü (KPI deltaları, seviye dağılımı, göreli zaman çizgisi, ortak / sadece A / sadece B sinyaller, incident'lar); 2 yeni test |
| Claude Fable 5.1 | 2026-09-14 | README yalnızca uygulama kılavuzu olarak yeniden yazıldı: sayfalar, veri akışları, her .py dosyası fonksiyon düzeyinde, testler, teknolojiler; yarışma takvimi ve demo akışı docs/PLAN.md'ye taşındı |
| Claude Fable 5.1 | 2026-09-14 | Log anlamlandırma: ham satır saklama, incident origin / timing / recovery tespiti (restart, kendiliğinden, durdu, devam ediyor), sinyal ve incident'ta Nereden / Neden / Ne yapmalı / Zaman ve düzelme kartları, tıklanabilir kanıt satırı → tam kayıt + ham satır, kendiliğinden düzelenler için otomatik done aksiyon kaydı ve Aksiyonlar bölümü; demo veri setine restart olayı; 1 yeni test |
| Claude Fable 5.1 | 2026-09-14 | Playbook / hata kütüphanesi: SQLite'ta kalıcı hata desenleri (nerede, ne zaman, kaç kez, düzelme türleri, çözüm notu, runbook), her analizde otomatik kayıt, bulanık eşleşme, Playbook sayfası, incident'ta "Daha önce görüldü" kartı; kanıt tablosuna satır seçme kutusu; 1 yeni test |
| Claude Fable 5.1 | 2026-09-14 | Incident flashcard (ne oldu / neden / nerede / ne zaman / nasıl çözüldü / ne yapmalı / daha önce görüldü) Özet panelinde ve Incident sekmesinde satır seçimiyle; ITSM ilişkilendirme okunur etiketler (⚠ / 📈 / 🚨) ve düz cümle gerekçelerle, genel kelimelerle sahte eşleşme engellendi; boş incident başlığı için ham satır yedeği |
| Claude Fable 5.1 | 2026-09-15 | Parser'a environment (prod/test/dev/staging/uat/qa; sütun, değer tahmini ya da host/mesajdan çıkarım) ve origin (hata kaynağı sütunu) rolleri; Özet'te Ortamlar kartı ve detay paneli (ortama göre hata dağılımı, hata kaynakları); Sinyaller'de ortam filtresi; sinyal/incident/flashcard kartlarında ortam ve kaynak; 1 yeni test |
| Claude Fable 5.1 (Cowork) | 2026-09-15 | Operasyon sayfasına ortam çipleri, tıklanabilir sunucu adları (tek host görünümü + sunucu kartı) ve ortama göre kartlar; `LiveStore` env/host filtreleri, `env_summary`, simülatör ortam etiketi, `agent.py --env`; `test_live_env_and_host_filters` |
| Claude Fable 5.1 (Cowork) | 2026-09-15 | Operasyon filtresi çiplerden açılır listelere (Ortam / Sunucu, `host · ortam` etiketi) ve "Filtreyi temizle" düğmesine çevrildi; EN etiketler "Busiest" → "Most active" |
| Claude Fable 5.1 (Cowork) | 2026-09-15 | Operasyon sayfası: üst filtre kartı kaldırıldı, sağda 🔎 popover; "Ortama göre" kartları tıklanabilir oldu → seçili ortamın sunucu kartları → tek sunucu görünümü (ekmek kırıntısı, seçimle geri çıkma); `ENV_ORDER` ile sabit kart sırası |
| Claude Fable 5.1 (Cowork) | 2026-09-15 | Operasyon sayfası: kart/düğme gezinmesi kaldırıldı; kartların sağına Kapsam kutusu (ortam listesi + sunucu radyo listesi) eklendi, seçim 10 kartı ve grafikleri daraltır; servis seviyeleri için hesaplama dayanağı notu; uzun değerler için küçük yazı tipli kart |
| Claude Fable 5.1 (Cowork) | 2026-09-15 | Operasyon sayfasındaki 10 kart tıklanabilir: `ops_tile` + `ops_detail_panel` (metrik grafiği ve host tablosu; erişilebilirlik / SLA / bütçe için oranı düşüren hata kalıpları, servis-host kırılımı, dakika bazlı grafik; p95 için servis gecikme tablosu ve en yavaş istekler; olay/dk ve ERROR+ listeleri); `LiveStore.slo_detail()`; `test_slo_detail_explains_cards` |

## Etkinlik günü

| Araç + sürüm | Tarih | Yapılan düzeltme / geliştirme |
|---|---|---|
| Claude Fable 5.1 (Cowork) | 2026-09-16 | Depo AO Hackathon 2026 Enterprise şablon yapısına getirildi: README şablon bölümleri (Proje / Problem / Çözüm / Ekip / Kurulum / Kullanım / Teknoloji / Demo / Lisans), AI_JURI.md, submission.json, .env.example, .gitignore, CLAUDE.md şablon başlıkları, docs/plan.md + fazlar.md + mimari.md (PLAN.md / DECISIONS.md devredildi), prompts/ (9 prompt), demo/ (8 ekran görüntüsü) |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | requirements.txt eklendi; README Kurulum bölümüne Windows (PowerShell) adımları |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | Hata haritası sayfası: `graph.py` (mesajlardan bağımlılık bağları, incident ilişki bağları, sunucu → servis bağları, etki zinciri, DOT üretimi), `page_map()` (kaynak / incident / ortam seçimi, kartlar, graphviz çizimi, düğüm detayı), flashcard'da "Haritada göster", `test_error_map_from_demo` |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | Hata haritası etkileşimli oldu: kendi SVG çizimi (`layout`, `to_html`; kaydır / yakınlaştır, düğüme tıklayınca odak + komşu vurgusu + detay paneli), canlı kaynakta 5 sn'de bir yenilenen fragment ve korunan seçim, ortam / sunucu filtresi kök neden ve etkilenenleri de daraltıyor, sunucu filtresi eklendi, `st.iframe` kullanımı |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | S-A1 paketi: `tables.py` (bağımlılık + envanter yan tabloları), `pipeline.dedupe` (alarm_id ile json/csv tekilleştirme), `scenario.SEVERITY_MAP` (1 = bilgi … 5 = kritik), Türkçe bağımlılık kalıpları, `MAX_INCIDENTS` kart bütçesi, kök nedende bağımlılık yönü ve karşı hipotezler, `noise_audit()` |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | `storm.py` yoğunluk kümeleyici (servis × 5 dk sıcak hücreler, bağımlılık / kabin bağlama, arka plan budama, küçük küme eşiği); kök neden için `CAUSE_RANK`, ilk neden-tipi alarm ve kabin ağ olayı kuralları; sentetik S-A1 üretici `samples/make_alarm_storm.py` + doğrulama etiketleri; `test_alert_storm_density_clustering` |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | Arayüz: flashcard'da karşı olasılıklar, "neden bu grup" (sıcak hücreler) ve sahip; kart başına otomatik açık ilk aksiyon (`record_first_actions`, `scenario.OWNERS`); Gürültü denetimi sekmesi (KPI, nedenler, servis × zaman ısı haritası, küçük gruplar); haritada tanımlı bağımlılık okları; README S-A1 bölümü, AI_JURI, fazlar |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | `enrich.py` + CLI `--enrich`: envanter ve bağımlılık tablolarıyla zenginleştirilmiş tek dosya veri seti (30 sütun, .summary.json); `tables.from_observations` ile tek dosyadan bağımlılık / envanter geri kurma; tırnak-duyarlı CSV ayırıcı tespiti; ham şiddet değeri (`severity_raw`) korunuyor |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | Kart puanı yeniden kuruldu: olay düzeyi patlama (tüm alarmların tepe dakikası vs üye servislerin olay dışı medyanı), veri setine göre normalize etki alanı, envanter iş kritikliği faktörü (ağırlıklar 0,30 / 0,20 / 0,20 / 0,15 / 0,15); çapraz doğrulamada bulunan session-service yavaş yanma olayı bilinen kısıt olarak AI_JURI'ye yazıldı (tırmanma dedektörü denemesi yanlış birleştirme yaptığı için geri alındı) |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | `storm.extract_slow_burns`: normal kümeleme sonrası şiddeti tırmanan neden-tipi zincirler (≥ 4 ERROR+, ≥ 2 sunucu, 15 dk) ayrı kart; o servise yönelik timeout'lar diğer kümelerden geri alınır; bağımlılık bağı kullanmaz → köprü yok; S-A1'de 5. kart (session-service bellek sızıntısı) |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | AI_JURI.md şablona göre yeniden düzenlendi: 5 kart tablosu, çapraz doğrulama ile bulunan düzeltmeler, tamamlanma tablosu zorunlu gereksinimlere göre |
| Claude Fable 5.1 (Cowork) | 2026-09-16 | `docs/bulgular.md`: S-A1 paketi için özet + ayrıntılı bulgu raporu (motor çıktısından üretilen sayılarla); gc / heap kök nedenleri için Türkçe ilk aksiyon önerileri |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | `parsers/sap_parser.py`: SAP log ailesi (ABAP dev trace, HANA trace, NetWeaver Java defaultTrace, java.util.logging, JVM GC, tp / R3trans / SUM, transport ALOG / SLOG, SM21 dışa aktarımı) uzantıdan bağımsız okunuyor; format tespitine `sap_score`; düz metin parser'ına 4 yeni zaman biçimi ve SEVERE / FINE seviyeleri; `samples/sap_logs.zip` + `test_sap_log_family` (33 test) |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Gerçek SAP dosyalarıyla (deploy.0.log 7 MB, available.log, bootstrap/datcol.jvm, class_prefetch.lst) doğrulama: ListFormatter `#2.0<BS>#` sürümü ve `<!--LOGHEADER-->`, boş satırda biten çok satırlı kayıt, msg id / kategori / uygulama / kullanıcı alanları; erişilebilirlik günlüğü, JVM özellik anlık görüntüsü ve class listesi biçimleri; `score_burst` sessiz dakikaları listelemeden medyan alıyor (4 yıllık günlükte 60 sn+ → 1,3 sn); 34 test |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Ürün adı Watchover oldu: Python paketi `src/watchover`, CLI komutu `watchover`, pyproject / Makefile / Docker, arayüz başlığı, README / AI_JURI / mimari / plan / bulgular / submission; eski ad tüm dosyalardan kaldırıldı |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Yeni tema: lacivert palet, turkuaz / mavi vurgu, Inter + JetBrains Mono, Streamlit tema ayarları (kenar rengi, köşe, kenar çubuğu, minimal araç çubuğu), kart / KPI / logo CSS'i, Altair grafik teması; Türkçe'ye duyarlı büyük harf (İLİŞKİ, ERİŞİLEBİLİRLİK); TR metinlerdeki İngilizce kalıntılar (samples, total, events, host, Datasets, Connection Settings) Türkçeleştirildi; 4 ekran görüntüsü yenilendi |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Farklı makinelerde farklı incident sayısı raporu: motor 4 hash tohumu, Windows yol ayracı, CRLF, BOM, cp1254 ve tüm S-A1 günü commit'leriyle test edildi, her yerde 5 kart (eski kodlar 4 / 147); sebep kod kopyası ya da girdi farkı. `stamp.py` ile motor / senaryo / git / girdi / sonuç kimlikleri kenar çubuğu, veri seti satırı, CLI ve summary.json'da; app.py repo `src/`'yi öne alır ve eski `pip install .` kopyasını uyarır; `tests/test_determinism.py` (37 test); sürüm 0.3.0 |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Veri setleri özeti yeniden tasarlandı: 5 + 7 kart ve 12 Detay düğmesi yerine tek indirgeme hunisi kartı (oranlı çubuklar), tek olgu şeridi ve `st.pills` ayrıntı seçici; Türkçe baş harf büyütme (`cap`); ekran görüntüsü yenilendi |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | UX: ayrıntı görünümleri `st.dialog` modal pencerelerinde (veri seti özeti ve operasyon kartları; operasyonda fragment içinden `st.rerun(scope="app")` ile açılır), incident seçimi açılır liste, kenar çubuğu gezinti listesi + TR/EN anahtarı + durum kartı, operasyon başlığında durum çipleri ve ⓘ dayanak ipucu; yeni **Arama** sekmesi (alan:değer, -hariç, tam ifade, regex; facet hapları, filtre çipleri, satır kartında + / − filtre düğmeleri, vurgulu ham satır, incident bağı; önbellekli vektörel arama); "huni" / "parmak izi" gibi zorlama çeviriler kaldırıldı |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Öğrenen katman: `knowledge.py` (örüntü başına tek ders, hash tekilleştirme, belge parçalama, hibrit arama, float16 embedding, karar → kural önerisi → onay → `apply_rules` scenario katmanı; SQLite / PostgreSQL), `assistant.py` (kaynak zorunlu bağlam, LLM ya da deterministik derleme), `llm.chat_messages` / `embed`; motorda NOISE_TYPES / NOISE_TEMPLATES / EXTRA_DEPENDENCIES kancaları ve `n_rule` gerekçesi; arayüzde Watchover'a sor sayfası (sohbet, bilgi tabanı, kurallar), incident kartında Daha önce görüldü + karar paneli, embedding ayarı; 5 yeni test (42) |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Modelin kendi kural önerisi: `assistant.rule_context` / `propose_rules` / `validate_rules` (JSON çıkarımı, alarm tipi ve servis doğrulaması, 0-6 aralığı, `llm:<model>` kaynaklı öneri, insan onayı şart), Kurallar sekmesinde düğme ve 🤖 rozeti; Watchover logosu (`assets/`: göz + radar süpürmesi SVG, PNG türevleri) kenar çubuğu, sekme simgesi, sohbet avatarı, sayfa başlığı ve README'de; 43 test |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Sağlayıcıdan bağımsız LLM istemcisi (OpenAI uyumlu, Ollama yerel, Anthropic, Gemini; çağrı metrikleri), `ollama.py` model yöneticisi (keşif, indirme akışı), `llm_eval.py` (kaynak doğrulama, kök neden testi), bilgi tabanında `llm_calls` / `answer_feedback` / `eval_runs`; yeni LLM sayfası (bağlantı, modeller, kalite), sohbette 👍/👎; operasyon kartları canlı sparkline'lı; `scripts/install_linux.sh` + systemd; 3 yeni test (46) |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Kalıcı ayarlar (`settings.py`, config.json 0600), dört adımlı ilk açılış sihirbazı (çalışma alanı, yerel model + indirme, ajanlar, bitir), ajan kimliği (`agents.py`: kayıt, tek seferlik token, sha256, iptal / yenile, son görülme; alıcı kimliği çözer ve olayları kayıtlı ad / ortam / site ile damgalar; ajan `--token`), Bağlantı ayarlarında Ajanlar paneli ve kaydet düğmeleri, `scripts/install.sh` tek satır macOS / Linux kurulumu + `watchover` launcher + LaunchAgent / user systemd; 3 yeni test (49) |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Ajan kurulumu netleştirildi: alıcı adresi seçimi (`local_addresses`, `public_host`), Ajanlar panelinde kayıt → tek satır kurulum komutu → canlı durum (çevrimiçi / sessiz / bekleniyor); alıcı `agent.py` ve `agent-install.sh` dosyalarını sunuyor; `agent.py` bağımsız stdlib ajan (spool, rotasyon, glob, --test, hello); `scripts/agent-install.sh` (python3 kontrolü, agent.env 0600, bağlantı testi, systemd / launchd, --uninstall); sihirbaz 3. adım metni; 2 uçtan uca test (51) |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Ajan/alıcı sertleştirme: `agent.py` `tail -F` davranışı (rename + copytruncate rotasyonu, yalnız tam satır, olmayan yol uyarısı), spool 0700/0600 + yazma hatası tolere, kilitli drain, kalıcı 4xx düşürme, macOS bellek hesabı, iş parçacığı ölünce çıkış; `agent-install.sh` tırnaklı env, systemd yalnız çalışıyorsa + restart, macOS root LaunchDaemon, eski pid; `install.sh` venv/ensurepip kontrolü, systemd olmayınca arka plan, çıkış kodu; alıcı ilk ajandan sonra token zorunlu, 16 MB gövde sınırı, Content-Length/timeout, hmac, kayıtlı ortam üstün, events.jsonl rotasyonu; UI alıcı yeniden başlatma (port/anahtar değişince), sihirbaz port özeti, HTTP uyarısı, varsayılan `/var/log/messages`; launcher 127.0.0.1; docker 8600; 3 yeni test (54) |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | `page_assist` (Watchover'a sor sohbeti) LLM sayfası commit'inde yanlışlıkla silinmişti: geri getirildi, yanıtlara 👍/👎 geri bildirimi eklendi (`rate_answer`); her sayfayı render eden smoke testi eklendi (55); `watchover update [file.zip]` ile git olmadan zip'ten güncelleme; README güncelleme bölümü |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | `watchover update` izleme bilgisi olmayan `main` dalında çalışmıyordu: launcher artık depo/dal adını bilir, origin yoksa ekler, yerel değişiklikleri stash'ler ve `origin/<dal>` üzerine geçer; sandbox'ta doğrulandı |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Kendi kendine kayıt: filo anahtarı (`agent_settings`, `wk_…`, yenile / kapat), alıcıda `POST /enroll` (401 / 409, anahtar veri gönderemez), `agent-install.sh --enroll-key [--name --site --tags]` token'ı alıp `agent.env`'e yazar, Ajanlar panelinde toplu kurulum bloğu; 1 yeni test (56) |
| Claude Fable 5.1 (Cowork) | 2026-09-18 | Bağlantı ayarlarında `AttributeError: enroll_key` (kod güncellenmiş, süreç yeniden başlamamış, önbellekteki eski sınıf örneği): `knowledge()` / `agents()` sınıf değişince yeniden kurulan tekil örnek; launcher launchd / systemd farkında `start / stop / restart / status`, `--server.fileWatcherType none`, `update` sonrası launcher kendini yeniler, `stop` çıkışı bekler; `docs/KURULUM_REHBERI.md` (dashboard, alıcı, ajan, yeni sistem dahil etme, güncelleme, sorun giderme) + PDF; README bağlantısı |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Kaynaklar sayfası: `sources.py` (Elasticsearch / OpenSearch / Loki / Splunk / Graylog / HTTP çekicileri, kanonik satır eşleme, imleç, `SourceStore`, `Poller`, test), sol menüde Kaynaklar (katalog, ekle / test / kaydet, canlı durum), poller açılışta; 3 test (59) |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Operasyon sunucu filtresi çoklu seçim (`st.pills`, `host_match`, metrik ortalaması, birleşik olaylar); `report.py` SLO raporu (yönetici özeti, KPI, SVG grafik, dağılımlar, incident'lar) ve SLO detay penceresinde dönem seçimli HTML dışa aktarma; 1 test (60) |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Detay penceresi çoklu sunucu kapsamını kaybediyordu (fragment içindeki pills durumu tam yeniden çalıştırmada düşüyor): seçim düz oturum anahtarına yansıtılır ve widget ondan yeniden tohumlanır; pencere alt başlığında kapsam gösterilir; demo/25-27 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | SLO raporu haftalık / aylık ve tüm yapı: `history.py` dakika rollup'ları (ingest kancası, upsert, saklama), `report.py` figür sözlüğü (canlı veya geçmiş, kova bazlı seri, kaynağa dağılım); canlı öğrenme `autolearn.py` (`LiveLearner`, `learn_runs`, eğitim seti JSONL), LLM › Kalite bloğu, `learn_min` ayarı, geri bildirimle yanıt metni saklanır; sunucu filtresi onay kutuları; 1 yeni test (61) |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Envanter sayfası ve modülü (`inventory.py`: CRUD, CSV içe / dışa aktarma ve şablon, ad / takma ad / IP çözümleme, olay zenginleştirme kancası, keşfedilen sunucular, motor biçimi); `Analysis(..., inventory)`; canlı öğrenme envanterle analiz eder; sayfa açıklamaları ℹ️ popover'a taşındı (`info()` yardımcı); 1 yeni test (62) |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Liquid glass tema: `GLASS` CSS katmanı (yarı saydam cam yüzeyler, blur, ışık lekeli zemin, cam girişler / düğmeler / diyalog / sekmeler / tablolar / kaydırma çubukları, azaltılmış saydamlık desteği), `config.toml` renkleri; README sayfasında bozuk wordmark base64 gömülerek düzeltildi; demo/32-33 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Kurumsal sadeleştirme: SLO detay pencerelerinin başlıkları yalın ad (açıklama `od_*_info` olarak ℹ️ arkasında), pencere alt satırı kısaltıldı, sayfa sloganları nötr bölüm listesine çevrildi, sohbet karşılama açıklaması ℹ️'ye taşındı |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Kaynaklar kataloğu: platform metinleri tek cümlelik kurumsal Türkçe / İngilizce, kart başlığı büyütüldü ve açıklama metni küçültüldü (`.card.cat` stilleri) |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Simülasyon modu Operasyon sayfasında anahtar (varsayılan kapalı, kapatınca `LiveStore.purge`); Sistem sayfası (`admin.py`: sürüm, servis durumu, önbellek temizleme, zip / git güncelleme, yeniden başlatma, veritabanı); kullanıcı girişi (`auth.py`: yerel hesaplar scrypt, roller, alan adı kısıtı, ilk hesap yönetici) ve OIDC SSO ayarı (`secrets.toml [auth]`); giriş sayfası, kenar çubuğu kullanıcı satırı; launcher `WATCHOVER_LAUNCHER` / `WATCHOVER_LOG`; 4 yeni test (66) |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Operasyon rozeti anahtara bağlandı: simülasyon açıkken SİMÜLASYON, kapalıyken CANLI VERİ ya da veri gelmiyorsa CANLI · VERİ BEKLENİYOR |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Google ile giriş ve çoklu sağlayıcı (`auth.py`: yerel + Google + OIDC, `write_secrets` [auth.google]/[auth.oidc]); parola güvenliği (scrypt N=2^15 r=8 p=2, parametreli karma ve otomatik yükseltme, sabit zamanlı doğrulama, politika, 5/15 dk kilit, `auth_events` günlüğü); giriş paneli kenar çubuğu Hesap kartından diyalog olarak, kilit ekranı; Sistem sayfası yeniden tasarım (KPI, servis kartları, açıklamalı bakım kartları, sağlayıcı kartları, güvenlik günlüğü); `Authlib` bağımlılığı; ayar geçişi `auth_mode` → sağlayıcı bayrakları; testler (66) |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Ajan otomatik log keşfi (`--auto` / `--discover`: SAP ABAP / Java / HANA / Host Agent, Oracle DB, EBS, WebLogic, Tomcat, JBoss, WebSphere, web, veritabanları, Kafka, Docker, Kubernetes düğümü, Linux / macOS / Windows sistem; Windows Olay Günlüğü, systemd günlüğü; keşif olayı), kurucu varsayılanı `auto`; `LiveStore.files / file_lines / discoveries`; Operasyonda Log dosyaları kartı ve ayrıntı penceresi (tablo, satırlar, dışa aktarma); Sunucu bağla düğmesi ve penceresi; 1 yeni test (67) |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Etkin model seçici: indirilmiş tüm Ollama modelleri arasından sohbet ve embedding modeli seçimi (LLM › Modeller ve Watchover'a sor başlığı), ayara kaydedilir; Kullan düğmesi kalıcı |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Telefon / tablet uyumu: medya sorguları (tablet sarma ve üçte bir sütun, dar kenar çubuğu; telefonda ikişer kart, iki sütunlu satırlar alt alta, küçük başlıklar, kaydırılabilir sekmeler, tam genişlik diyalog, dokunmatik düğme ve 16 px giriş), kenar çubuğu küçük ekranda kapalı başlar; iPhone ve iPad görünümleri Playwright ile doğrulandı |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | E-posta ve SMS uyarı altyapısı (`notify.py`: SMTP, Twilio / Netgsm / HTTP şablonlu SMS, kişi + grup + dağıtım listesi alıcıları, altı koşullu kurallar, bekleme süreli gönderim, teslimat günlüğü, dakikalık `AlertEngine`), Sistem › Bildirimler sekmesi (Kanallar / Alıcılar / Kurallar / Teslimat günlüğü, test gönderimi, ℹ️ açıklama), Durum sekmesinde uyarı servis kartı; kenar çubuğu hesap kartı her zaman görünür (giriş kapalıyken "tek operatör · yerel" ve Giriş yap diyaloğu), `tests/test_notify.py` (4 test), README bölümü |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | SMS sağlayıcı kataloğu: Netgsm, İleti Merkezi, Verimor, Mutlucell, JetSMS (Biotekno), Posta Güvercini, Twilio hazır; Turkcell / Türk Telekom / Vodafone kurumsal SMS için müşteriye özel uç nokta şablonu; sağlayıcıya göre alan gösteren form, numara biçimlendirme (`format_number`), XML kaçışı, 200 içindeki hata kodlarının yakalanması (`fail`), test eklendi |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Tam sayfa giriş kapısı (liquid glass giriş ekranı, kenar çubuğu gizli, giriş olmadan hiçbir veri görünmez), yerleşik yönetici `admin@watchover.local` + zorunlu parola değişimi, kendi kendine kayıt varsayılan kapalı; `vault.py` ile ayar ve kaynak sırlarının Fernet şifrelemesi (anahtar `data/.vault.key`), Güvenlik sekmesinde durum; `admin.py` sürüm geçmişi (güncelleme ve bakım öncesi otomatik anlık görüntü, elle görüntü, geri dönüş, budama) ve Sistem › Güncelleme arayüzü; testler (giriş kapısı, bootstrap, vault, geri dönüş), README, demo 49–52 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Duyarlı tasarım tamamlayıcıları: telefonda başlık satırları ve üç sütunlu satırlar tam genişlik, tablette (≤1000 px) iki sütunlu ayar satırları alt alta; giriş ekranı, Bildirimler sekmesi ve sürüm geçmişi iPad / iPhone görünümleriyle doğrulandı; README bölümü, demo 54–55 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Giriş ekranı düzeltmeleri: İngilizce terimlerde Türkçe İ hatası giderildi (`upper` İngilizce sözcükleri korur), başlangıç parolası ekrandan kaldırıldı (rastgele parola `data/initial-admin.txt`, 0600, değişince silinir), Google / Microsoft 365-Exchange (Entra ID) / Apple (Sign in with Apple, ES256 istemci sırrı) girişleri logolu düğmeler ve Sistem › Giriş kartlarıyla, parola değiştirme kartı ölçeklendi; testler, README, kurulum rehberi, demo 57–59 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Giriş ekranı: ipucu kutusu kaldırıldı, Google / Microsoft / Apple düğmeleri logolarıyla her zaman görünür (yapılandırılmamışsa pasif), yönetici başlangıç parolası sabit ve dokunulmamış yönetici her açılışta yeniden anahtarlanır; testler, README, kurulum rehberi, demo 60 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Dağıtım sonrası temizlik: `admin.clear_caches` (kod ağacındaki `__pycache__` / `*.pyc`, `.pytest_cache`, Streamlit disk önbelleği; venv hariç) + `deploy_finish` ile otomatik sert yeniden başlatma; launcher `restart --hard` (durdur, önbellekleri sil, temiz başlat) ve `update` sonunda bunu kullanır; uygulama içi zip / git güncellemesi ve geri dönüş kendiliğinden tamamlanır; test, README, kurulum rehberi |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Giriş sayfası referans düzene göre yeniden kuruldu (ortada tek cam kart, marka rozeti, gradyan Devam et, veya ayracı, logolu tam genişlik Google / Microsoft / Apple düğmeleri, akış şeridi); Beni hatırla: `remember_tokens` tablosu (sha256, 30 gün), `wo_remember` çerezi, sessiz giriş, çıkışta ve parola değişiminde iptal; Playwright ile çerez / yeni sekme / çıkış doğrulandı; test, README, kurulum rehberi, demo 61–62 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | E-posta ile MFA: `otp_codes` (karma, 5 dk, 5 deneme), parola veya SSO doğrulamasından sonra kayıtlı adrese kod, giriş kartında kod adımı (yeniden gönder / vazgeç), yöneticiler muaf, hatırlanan oturumlar sormaz, SMTP yoksa günlüğe not; Sistem › Giriş MFA anahtarı; giriş ekranından akış şeridi kaldırıldı; `clear_caches` için `CACHE_PROTECTED` (modeller, data, veritabanları korunur) ve test; sahte SMTP ile uçtan uca doğrulama; README, kurulum rehberi, demo 63–64 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Terminalden hesap yönetimi: `watchover user list / add --admin / promote / password / unlock / enable / disable / delete` (`useradmin.py`, launcher `user` komutu, `Users.unlock`); kurulum rehberine giriş yapılamıyor satırı; test |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Roller ve yetkiler (`rbac.py`: yetki kataloğu, yerleşik yönetici / operatör / izleyici, özel roller, onay kutusu matrisi, gezinti / düğme / sistem sekmesi kapıları); kayıtta e-posta doğrulaması (`pending` hesap, kod ile etkinleştirme, SMTP yoksa kayıt kapalı); bu makinenin izlenmesi (`selfmon.py`: yerleşik ajan `--auto --metrics`, şifreli token, pid dosyası, Durum kartı ve anahtar, `WATCHOVER_SELFMON=0`); `tests/conftest.py`; testler, README, kurulum rehberi, demo 65–68 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Kullanıcılar ayrı sayfa (👥): satır başına form ve Kaydet (ad, rol, durum, yeni parola, kilit açma), onaylı Sil, KPI'lar, Roller ve yetkiler ile Hesap ekle sekmeleri; `page.users` yetkisi; Sistem sayfasından Kullanıcılar sekmesi kaldırıldı; yerleşik yönetici eski kurulumlarda da oluşturulur (`bootstrap`), form iç boşluğu (`stForm` padding); testler, README, kurulum rehberi, demo 69–71 |
| Claude Fable 5.1 (Cowork) | 2026-09-19 | Kenar çubuğu keskin liquid glass: cam panel, gezinti girdileri cam kartlar (seçili vurgu ve sol şerit), daraltma tutamacı her zaman görünür ve cam; kenar çubuğu alt yazıları (slogan ve sürüm damgası) kaldırıldı, sürüm / motor / git / senaryo damgası Sistem sayfası başlık çiplerine taşındı; demo 72–73 |
| Claude Fable 5.1 (Cowork) | 2026-09-20 | Tüm sekme şeritleri (st.tabs) durum çipleri gibi cam hap görünümüne çevrildi (react-aria sekme DOM'una göre `stTab` seçicileri, nokta işareti, seçili teal); LLM sayfasında model değiştirirken `llm_model` widget anahtarı hatası giderildi (değişiklik `_pending_settings` ile bir sonraki çalıştırmada, widget'lardan önce uygulanır), regresyon testi; demo 74–75 |
| Claude Fable 5.1 (Cowork) | 2026-09-20 | Proje tüm geçmişiyle (97 commit) `Bannercheck/Operation_Monitoring_tool` deposuna `main` dalı olarak taşındı; kurucu betiği, submission.json ve CLAUDE.md yeni depo ve `main` dalına güncellendi |
| Claude Fable 5.1 (Cowork) | 2026-09-20 | Launcher `update`: depo adresini origin'den alır (taşınan depo), hackathon dalından yapılmış kurulumlar `main` varsa kendiliğinden `main`'e geçer ve launcher yeni değerlerle kendini yeniler |
| Claude Fable 5.1 (Cowork) | 2026-09-20 | Sürümleme: `release.py` (major / minor / patch, `CHANGELOG.md`, git etiketi), sürüm v1.0'a çekildi ve geçmiş notlar (v0.1–v1.0) yazıldı, Sistem › Güncelleme'de sürüm notları, `watchover version`, CLAUDE.md sürümleme kuralı; sekmeler aynı yükseklikte ve yana genişleyen haplar, şerit yatay kayar; test, README, demo 76–77 |
| Claude Fable 5.1 (Cowork) | 2026-09-20 | Windows kurulum rehberi (dashboard kurulumu, çalıştırma, güncelleme, Windows sunucuya ajan ve Görev Zamanlayıcı, sorun giderme) `docs/KURULUM_WINDOWS.html` (PDF olarak da üretildi) |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Veri seti yükleme: SAP `stdout0` / `dev_w0` gibi aynı adlı veya boş dosyalarda sonsuz yeniden yükleme döngüsü giderildi (yüklemeler `file_id` ile bir kez işlenir), çoklu dosya yükleme (birlikte tek veri seti veya ayrı ayrı), boş dosya atlama ve hata mesajı; regresyon testi; sürüm v1.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Arka planda veri seti yükleme: `start_load_job` (ayrı iş parçacığında ayrıştırma + analiz + profil), `collect_load_jobs` (sonuç betik iş parçacığında kaydedilir; kenar çubuğu durum parçası ve Veri setleri paneli 2–5 sn'de bir toplar), ilerleme aşamaları, hata bildirimi; yükleme sırasında sayfalar arasında gezinti; regresyon testi; sürüm v1.2 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Yükleme hızı: zaman damgası, önem derecesi, ortam ve şablon çözümlemeleri memoize edildi, parmak izi anahtar başına bir kez hashlenir, kayıt döngüsünde ortam host/servis çiftine göre ve öznitelik hızlı yolu, çoklu yüklemede sıkıştırma yok; 46 MB / 700 bin satır 22,3 s → 10,1 s, sonuç kimlikleri üç örnek veri setinde birebir aynı; sürüm v1.2.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Donma giderme: canlı tampon sorguları pencereli tarama (son 15 dk için 50 bin olayın tamamı yerine yalnız yeni uç) ve 2 sn'lik tik başına memoize (`LiveStore.cached`), Operasyon paneli aynı sorguyu bir kez hesaplar; motor / senaryo / git damgaları önbellekte, `version_info` git alt sürecini dakikada bir çalıştırır; çoklu dosya / ZIP ayrıştırma ProcessPool ile paralel (8+ çekirdek ve 32 MB üstü; ölçüm: 4 çekirdekte sonuç aktarım maliyeti kazancı yiyor, o yüzden kapalı), `WATCHOVER_PARALLEL` anahtarı; sürüm v1.3 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Yükleme ilerlemesi: ayrıştırma aşamasında gerçek yüzde (`pipeline.ingest(progress=…)`, işlenen satır / bayt payı, dosya adı), analiz ve profil aşamalarında ayrıştırma süresine oranla tahmin, ilerleme çubuğu + kalan süre; kenar çubuğu DURUM satırında yüzde, yükleme sırasında 2 sn yenileme; ölçüm: arka plan iş parçacığı ana iş parçacığını en fazla 56 ms geciktiriyor (GIL sorunu yok); test; sürüm v1.3.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | SAP NetWeaver Java izleri 4 kat hızlı: satırın ilk karakterine göre dağıtım, yığın izi devam satırları için regex kaskadı olmadan hızlı yol, alan desenleri önceden derlendi, logger → servis eşlemesi memoize; biçim algılama ve 50 satırlık sondaj dosyanın tamamını bölmüyor (ön ek); ilerleme çubuğu her dosya başında ilerler (%0 bekleme giderildi); 300 bin kayıtlık 87 MB iz 33,8 s → 8,3 s, örnek veri seti sonuç kimlikleri aynı; sürüm v1.3.2 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Docker dağıtımı: yeni Dockerfile (root olmayan kullanıcı, tini, sağlık denetimi, /data birimi, gömülü git kimliği), compose (dashboard + postgres / mcp profilleri, kaynak sınırları, log döndürme), `.env.docker.example`, GitHub Actions imaj yayını (ghcr.io, amd64 + arm64), Makefile hedefleri; uygulama Docker farkındalığı (`admin.in_docker`, Sistem › Güncelleme compose kartları, imaj etiketi çipi, yeniden başlatma), `stamp.git_rev` WATCHOVER_GIT_REV; `docs/KURULUM_DOCKER.md` + HTML / PDF; test; sürüm v1.4. Not: bu ortamda Docker daemon'u yok, imaj derlemesi kullanıcı tarafında ilk kez çalışacak; compose dosyası `docker compose config` ile doğrulandı |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | PostgreSQL ürün veritabanı oldu: ortak `db.py` katmanı (psycopg 3, kilit, yeniden bağlanma, `dump` / `restore` / `copy_table`), `ActionStore` ve `Playbook` paylaşımlı veritabanına taşındı (MCP sunucusu dahil), `migrate.py` (SQLite → PostgreSQL komutu + uygulamanın ilk açılışta otomatik kopyalaması), admin anlık görüntü / geri dönüş PostgreSQL'de JSON döküm, VACUUM her motorda, Sistem › Durum veritabanı etiketi; gerçek PostgreSQL 16 ile bulunan lehçe hataları düzeltildi (upsert nitelenmemiş sütun, `"user"` tırnaklama, `%` kaçışı); compose'da zorunlu `postgres` servisi + `depends_on`, `.env` örnekleri, `psycopg` çekirdek bağımlılık, CI'da postgres servisi, `make docker-migrate` / `docker-backup`; `tests/test_postgres.py` (6 test, yerel PostgreSQL 16 ile yeşil); Docker rehberi yeniden yazıldı (bağımlılıklar, arayüze erişim, PostgreSQL, yedek, taşıma) + HTML / PDF; sürüm v1.5 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | MCP sunucusu Docker'da: `http_app()` ile `MCP_API_KEY` koruması (Bearer / X-API-Key, 401), `/health`, `WATCHOVER_DATASETS` ile göreli veri seti yolları; compose mcp servisine anahtar, `./datasets` bağlaması ve sağlık denetimi; `.env` örneği, Docker rehberi bölüm 7 (istemci ayarı), README; test (`test_mcp_server_http_api_key`); sürüm v1.5.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Kurumsal, GitHub'sız Docker kurulumu: `watchover.sh` (install / update / start / stop / status / logs / mcp / backup / restore / user / migrate / save / load / shell; `.env` üretimi rastgele sırlarla), compose varsayılanı yerel derlenen imaj (`watchover:<sürüm>`), Makefile hedefleri script'e bağlandı, `.gitignore` / `.dockerignore` (backups, datasets); rehber baştan yazıldı (sunucu gereksinimleri, kurulum, işletim, sunucu izleme ve log çekme yolları, PostgreSQL, MCP, TLS, kapalı ağ, yedek, sorun giderme) + HTML / PDF; sahte Docker ile script testi; sürüm v1.6 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Anomali takibi: `anomaly.py` (sunucu başına dayanıklı taban çizgisi medyan + MAD, z-skor; errors / rate / silence / pattern / metric türleri; tekrar tazeleme, normale dönüş işareti; operasyonel adım listesi, üstlenme, aksiyon bağı, durum akışı; arka plan taraması), `act.anomaly` yetkisi, bildirim kuralı koşulu `anomaly`, Operasyon sayfasında bölüm (KPI, filtre, kartlar, grafik, adım kutuları, sorumlu / not, düğmeler), TR / EN çeviriler ve yerelleştirilmiş başlıklar; `metric_stats` anahtar adı (`per_minute`) düzeltmesi; README, rehber; testler (`test_anomaly.py`, 5 test); sürüm v1.7 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | `test_ops_page_shows_anomaly_section` tam pakette dile bağlı kalmasın diye başlık denetimi TR / EN'den bağımsız yapıldı |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | `watchover.sh` macOS'ta sunucu IP'sini `ipconfig getifaddr en0/en1` ile bulur (`hostname -I` boş dönünce banner boş kalıyordu); sürüm v1.7.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | FastAPI katmanı (faz 1): `src/watchover/api/` (app fabrikası, `services.py` Streamlit'siz motor kurulumu + `DatasetRegistry` arka plan yükleme, `security.py` stdlib JWT + RBAC bağımlılıkları, 13 router, OpenAPI), `python -m watchover.api`; pyproject `api` ekstrası, Dockerfile, compose `api` profili, `watchover.sh api on\|off`, `.env` örneği, rehber 7b, README; `tests/test_api.py` (5 test grubu, TestClient); sürüm v1.8 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | `test_api` ortam değişkenlerini MonkeyPatch ile geri alır (tam pakette `test_mcp_server_tools_direct` API'nin aksiyon dosyasını görüyordu) |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | React arayüzü (faz 2): `web/` Vite + React 18 + TypeScript (api istemcisi, auth bağlamı, TR / EN sözlük, tasarım belirteçleri, 8 ortak bileşen, cam kenar çubuklu yerleşim, 12 sayfa); API'de OIDC / SSO akışı (keşif, PKCE, imzalı state, userinfo / id_token, `#sso=` dönüşü) ve `web/dist` SPA sunumu (`_app` tembel özniteliği düzeltmesi); Dockerfile node derleme aşaması, compose / watchover.sh / .env metinleri; Playwright ile uçtan uca tur (giriş, parola değişimi, simülasyon, anomali adımı, demo seti, incident penceresi, roller, sistem, telefon görünümü); testler (`test_oidc_flow_with_stubbed_provider`, `test_spa_is_served_when_built`); rehber 7b, README; sürüm v1.9 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | SSO tamamlama: giriş sayfasında Google / Microsoft / Apple düğmeleri her zaman (logolu, yapılandırılmamışsa pasif), API'de Apple (ES256 istemci sırrı, form_post geri dönüşü, `user` alanından ad), `/auth/providers` `configured` + geri dönüş adresi, ayarlar Streamlit ile aynı anahtarlar (`auth_*`, `ms_*`, `apple_*`), otomatik hesap açma `auth_self_register`; Sistem sayfasında Giriş sağlayıcıları kartı (`GET/PUT /api/system/auth`, maskeli sırlar); testler; rehber 7b sağlayıcı tablosu; sürüm v1.9.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Faz 3: kalan sayfalar React'e taşındı (Hata haritası, Watchover'a sor, LLM + Ollama, ITSM; veri seti arama ve karşılaştırma; log dosyaları ve SLO raporu; SMTP / SMS kanalları; sistem ayarları, güncelleme, yeniden başlatma, geri dönüş; keşfedilen sunucular; e-posta doğrulamalı kayıt), API'de karşılık gelen router'lar ve uçlar, `services` LLM / gömme / alıcı / kendini izleme; Docker'da dashboard konteyneri API + React (8501, alıcı 8600), Streamlit `legacy` profili (8502); `watchover.sh legacy on\|off`; test (`test_phase3_*`); rehber ve README; Playwright turu; sürüm v1.10 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Ollama model listesi: `bge-m3:latest` ile önerilen `bge-m3` eşleşmediği için kurulu model 'İndir' görünüyordu; `:latest` soneki iki tarafta da yok sayılır (React ve Streamlit), biten indirmeler listede görünür olunca ilerleme çubuğu kalkar; sürüm v1.10.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | React'e taşınırken kaldırılan Streamlit özellikleri geri getirildi: Operasyon'da tıklanabilir KPI / metrik kartları ve kapanabilir ayrıntı panelleri (hız, ERROR+, erişilebilirlik / SLA / bütçe, p95, CPU / bellek / disk / GPU, log dosyaları + kuyruk), ortam / sunucu çipleri, biletler; veri setlerinde örnekler, uzak çekme (HTTP / MCP), sütun eşleme, incident flashcard / aksiyon / geri bildirim / benzer dersler / LLM istemi / postmortem, gürültü ısı haritası; bilgi tabanı belge yükleme ve LLM kural önerisi; sistem bakım kartı; README sayfası. Tema düğmesi (koyu / sistem / açık) TR / EN yanında, `.light` token'ları. API uçları ve `test_restored_features_api`; rehber 7b; sürüm v1.11 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Veri kaynakları sayfası React'e geri getirildi (katalog, kaydetmeden test `POST /sources/test`, gelişmiş alanlar, liste işlemleri; menüde `/sources`, Bağlantılar sekmesi `?tab=`), veri seti çekme türleri HTTP / MCP / Canlı akış / Kayıtlı kaynak (`POST /datasets/from-source`), açık tema yumuşatıldı (kırık beyaz yüzeyler, kurşuni metin), `Field` ipucu; test; sürüm v1.12 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Şirket ağına açılış: compose `edge` profili (Caddy, `docker/Caddyfile`: tls internal ya da ./certs, 80→443, güvenlik başlıkları, /ingest → alıcı), `watchover.sh edge on|off|cert` (düz port localhost'a), `.env` anahtarları, `env_get` satır içi yorumları atar, ajan `--ca` / `--insecure`, uvicorn proxy başlıkları; test `test_edge_on_off`; rehber bölüm 8 macOS adımlarıyla yeniden yazıldı; sürüm v1.13 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Mac'te `edge on` 127.0.0.1:8501 çakışması (launcher Streamlit portu): edge açıkken düz port yayınlanmıyor, `docker-compose.edge.yml` (`ports: !override`, 8600 kalır) `COMPOSE_FILE` ile devreye giriyor, `edge off` kaldırıyor; test ve rehber güncellendi; sürüm v1.13.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Caddyfile: HTTP/3 kapatıldı (compose yalnız TCP 443 yayınlıyor, ilan edilen UDP 443 tarayıcıyı bekletebilir); sürüm v1.13.2 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Caddyfile `default_sni` / `fallback_sni`: IP adresiyle bağlanan istemciler SNI göndermediği için TLS 'internal error' alıyordu; rehbere not; sürüm v1.13.3 |
| Claude Fable 5.1 (Cowork) | 2026-09-21 | Logo React arayüzüne: `Logo` / `Wordmark` bileşeni (inline SVG, gradyan yazı), kenar çubuğu (daraltılmış durumda yalnız işaret), giriş kartı (ışıltı), README sayfası, favicon ve apple-touch-icon, tema rengi; sürüm v1.14 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Sanal ad: `edge on ad[,ad2,ip]` (Caddy çoklu site adresi `WATCHOVER_EDGE_HOSTS`, IP her zaman sertifikada, `.local` adlar macOS'ta `dns-sd -P` ile Bonjour duyurusu, `edge off` durdurur); Kayıt ol düğmesi geri (varsayılan açık; SMTP yoksa beklemede hesap + yönetici onayı, `/auth/options` `verify`, `/auth/register` `approval`); Apple girişi API, React, testler ve rehberden kaldırıldı; testler; sürüm v1.15 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Kayıt ol: hesap görüntüleyici rolüyle hemen açılır ve giriş yapar (SMTP varsa önce e-posta doğrulaması), `register(role=)`, eski ayar dosyaları için `version` 2 göçü (`auth_self_register` açık), beklemede hesaba 403 `pending_approval`, hata metinleri Türkçe, formda kural ipucu; Operasyon metrik kutuları eşit yükseklik; sürüm v1.15.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Sistem › Veritabanı tablo listesi: ad süzgeci, sadece dolu tablolar, ilk 8 satır + daha fazla / daralt, satır sayısına göre sıralama (`DbTables`); sürüm v1.15.2 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Parser v2 adım 1: 38 dosyalık biçim korpusu (`samples/corpus/`), `coverage.py` (üretim pipeline ile zaman / seviye / sunucu / servis kapsama ölçümü, puan, CLI `--json` / `--update-baseline`, `baseline.json`), gerileme testi `test_coverage.py`, `GET /api/system/coverage` + Sistem › Parser kapsaması kartı; gürültü oranı da raporda (`measure_noise`: huni, azaltma, elenen %, alarm fırtınası gürültü isabeti, gerileme kuralları); ilk ölçüm puan 44.6, demo azaltma 70.5×; sürüm v1.16 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Sistem › Parser kapsaması kartı iki sütunlu ızgaradan çıkarılıp tam genişliğe alındı; sürüm v1.16.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Parser v2 adım 2: metin parser'ı yeniden (30+ zaman biçimi, CRI / Redis ön ekleri, seviye sözcükleri, parantez uygulama adı, katlama, PostgreSQL / SQL Server / EBS / Dynamics kuralları), syslog RFC 5424 + Cisco ASA + PAN-OS, yeni `access` / `cef` / `winevt` / `w3c` parser'ları, OTel ve CloudTrail, `family.py` (25 aile, ERP: Oracle EBS, Dynamics AX / 365 / NAV, SAP), JSON kayıtlarında metinden seviye çıkarımı, `_sev_src`, auto_map dolu sütun tercihi, ERP korpus dosyaları (5), seviye ölçütü sıkılaştırıldı; puan 44.6 → 80.8; testler; sürüm v1.17 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Envanter sayfası React'e (`/inventory`, tam form, keşfedilenler, CSV şablon ucu); incident detayı: yan panel hataları yutulur, `_json_safe`, kök neden yoksa başlıktan; modal hatayı gösterir; rozetlerde `lang=en` (CRİTİCAL → CRITICAL); Hata haritası kök neden kartı sayı + kısaltılmış liste, KPI değer ve alt satırları tek satırda kısalır; sürüm v1.17.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Parser v2 adım 3: `grok.py` (Logstash uyumlu desen dili, 90+ temel parça, 27 satır paketi, test, kullanıcı desenleri `grok.json`, `detect`, korpus eşleşme sayısı), `GrokParser`, algılayıcıda grok adımı, syslog sunucu adı guard'ı büyük / küçük harf duyarsız, `/api/grok` (kütüphane, temel parçalar, test, ekle / düzenle / sil, ayrıştırılamayan satırlar), Parser desenleri sayfası, `page.parser` / `act.parser` yetkileri ve yerleşik rol göçü, 9 yeni korpus dosyası, testler; sürüm v1.18 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | LLM incelemesi: `POST /datasets/{key}/review` (kapsam incident / veri seti / log satırları, kanıt paketi, tr / en sistem istemi, grounding), `LlmReview` bileşeni incident kartında ve veri seti "LLM incelemesi" sekmesinde; test; sürüm v1.19 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Yazı ölçeği küçültüldü: gövde 14→13 px, h2 20→17, h3 16→14, KPI değeri 28→22, tablo 12.5, düğmeler 13, kartlar ve modal daha sıkı; incident listesi satırları 13 / 11.5 px; sürüm v1.19.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Menü sadeleştirme (Bildirimler ve README Sistem sekmesi, `unless` kuralı), Sistem kartları `Collapsible` + üstten hizalı ızgara, `NotifyBell` (açık anomaliler, görülmemiş rozeti, sekme başlığı, tarayıcı bildirimi, panel), korpus `.log` dosyaları depoya alındı (`!samples/corpus/*.log`); sürüm v1.20 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Menüde kalan eski Bildirimler öğesi kaldırıldı (Sistem sekmesine taşınmıştı); sürüm v1.20.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | v1.20.1'de menü dizisinde eksik virgül (derleme hatası) düzeltildi; sürüm v1.20.2 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Watchover'a sor menüden çıkarılıp sağ alt sohbet balonuna taşındı (`ChatWidget`: ask / rate, veri seti seçimi, ortak geçmiş, tam sayfa bağlantısı); sürüm v1.21 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Eşik uyarıları: `AnomalyTracker._scan_thresholds` (son değer ≥ `thr_cpu/memory/disk/gpu`, anahtar `threshold:env:host:metric`, düzelince cleared), `threshold_settings()`, ayarlar + Sistem › Ayarlar alanları, zilde metrik etiketi; testler; sürüm v1.22 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Parser v2 adım 4: `drain.py` (Drain kümeleme, `TemplateStore` kalıcı, canlı akış + veri seti beslemesi, `propose_grok` tipli desen, `QUANTITY` parçası), `profiles.py` (öğrenen sütun eşlemesi, kullanıcı > auto, pipeline kancası), `/grok/templates` ve `/grok/profiles` uçları, Parser sayfasında iki yeni sekme; testler `test_drain.py`; sürüm v1.23 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Kurumsal hafıza: `memory.py` (canlı pencere öğrenicisi, anomali ve çözüm beslemeleri, arka plan reindex), `knowledge.py` hash gömme + `embed_model` + `add_event` + `reindex`, `/memory/*` uçları, LLM incelemesine hafıza bölümü, Hafıza sayfası; Bağlantılar'dan çoklanan Kaynaklar/Envanter sekmeleri kaldırıldı, Parser desenleri LLM sayfasına sekme oldu; testler `test_memory.py`; sürüm v1.24 |
| Claude Opus 4.8 (Cowork) | 2026-09-22 | Zafiyet taraması: `scanner.py` (danışma kataloğu eşleşmesi, yapılandırma kontrolleri, opsiyonel port erişilebilirlik kontrolü), `data/advisories.json`, `/scan/*` uçları, Zafiyet taraması menüsü; kapsamlı red team: SPA path traversal, SSRF/`file://`, must_change zorlaması, notify SQLi, grok+arama ReDoS, sıkıştırma bombası, OIDC open redirect, ITSM kimlik sızıntısı, yetki yükseltme, MCP sertleştirme, güvenlik başlıkları, Docker paket yükseltmeleri, commit edilmiş sırların kaldırılması; testler `test_scanner.py`, `test_security.py`; sürüm v1.25 |
| Claude Opus 4.8 (Cowork) | 2026-09-22 | CPU'da yerel LLM: `docker-compose.yml` `llm` profili (ollama servisi, kalıcı model hacmi, CPU/bellek/thread sınırları), `watchover.sh llm on/off/pull/list`, `.env.docker.example` LLM ayarları; test `test_docker_script.test_llm_on_off`; sürüm v1.26 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Kendi model hattı: `autolearn.training_examples/training_bundle` (anomali+çözüm kaynakları, tekilleştirme, eval ayrımı), `training.py` (export CLI, Modelfile), `scripts/train_lora.py` (CPU PEFT LoRA, dry-run, kalite kapısı), `/llm/training-stats|export`, LLM › Kalite kartı, `watchover.sh llm export|import`, `train` extra; testler `test_training.py`, api + docker script; sürüm v1.27 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | `watchover.sh llm` mevcut LLM bağlantısına dokunmaz: `on` yalnız bağlantı yoksa gömülü Ollama'yı seçer, `import` modeli eklemekle kalır, yeni `use <model>` seçer, `off` yalnız kendi ayarını geri alır; ilk yüklemede `<html lang>` UI diliyle eşitlenir (İNSTALLED → INSTALLED); README/KURULUM ifadeleri; docker script testleri; sürüm v1.27.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Gözetimsiz öğrenme: `autotrain.py` (eşik/zaman/bayrak tetikleme, koşu kaydı, aday model, kalite kapısı, terfi), `lora.py` (trainer kütüphanesi; betik ince sarmalayıcı), `ollama.py` blob/create/copy/delete, `Dockerfile.trainer` + compose `trainer` servisi ve hacimler, `train_*` ayarları, `Services.llm_cfg("ops")`, `/llm/autotrain` uçları, LLM › Kalite otomatik öğrenme paneli; testler `test_autotrain.py`, api, docker script; sürüm v1.28 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Dinamik boyutlandırma: `watchover.sh size_env` (docker info NCPU/MemTotal → LLM/trainer tüm CPU'lar, bellek tavanları, dashboard/PG makineyi aşmaz; `WATCHOVER_SIZING=manual` korur), compose varsayılanları küçültüldü, `.env.docker.example`; docker script testleri (auto + manual); sürüm v1.28.1 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Faz 1 ölçek: `LiveStore.attach_db` (live_batches, DB'den pencere, cap, prune), `capacity()` ve uyarılar, saklama ayarları (settings + system PUT anında uygular), `Services.apply_capacity_settings`, status.capacity, Sistem › Kapasite paneli ve ayar alanları, `watchover.sh backup --keep`, KURULUM Faz 1 bölümü; testler `test_capacity.py`, api, docker script; sürüm v1.29 |
| Claude Fable 5.1 (Cowork) | 2026-09-22 | Yük testi `scripts/loadtest.py` (ERP karışımı, 100 ajan, kapasite/tarama ölçümü) ve ölçüme dayalı optimizasyon: kaynak başına biçim/aile tespit önbelleği (`ingest_bytes(hint)`, `FAMILY_HINT`), tarama başına tek pencere okuması, şablon memo, gelecek tarih kelepçesi, olay/sn ve tampon süresi düzeltmesi; `docs/LOAD_TEST.md` + JSON raporlar; testler `test_loadtest.py`; sürüm v1.30 |
